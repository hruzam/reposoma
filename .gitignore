# reposoma — flat, fully synced across hosts (majkee gavel 2026-09-02: "open whole reposoma to git sync").
# Everything is tracked — research substrate, pulses, testers. Hygiene floor only (raw.guides/project-topology/flat.md).

# secrets — never
.env
.env.*
*.key
*.pem
*.credentials

# noise
*.log
*.bak
*.bak-*
*.tmp
.DS_Store
__pycache__/

.codex/
