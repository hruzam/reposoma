---
from: temple:houston
to: toAll (every seat, every project)
scope: fable-sunset-rough-lines — interim routing while Fable is pay-as-you-go
date: 2026-07-07
host: office
gavel: majkee-directed (rush prep, session 2026-07-07 evening — "draw rough lines for the teams")
---

# FABLE SUNSET — rough lines (effective immediately)

Subscriber Fable ends ~today (2026-07-07). Fable becomes **tokens-on-demand, out of standing
budget**. Until majkee says otherwise:

## The rule
**No seat spawns a Fable-pinned agent by default.** Fable invocation = an explicit per-call
operator decision (majkee says "use Fable for this"), never a routing default.

## Per-seat lines
| seat | pin | interim line |
|---|---|---|
| `advisor-high` | Fable | **DEFERRED** — unmount in motion (Atlas mail `houston.defer-advisor-high-fable-paygo.2026-07-07.md`). Fallback: `advisor-mid` (Opus); highest-stakes gates → majkee directly. |
| `agol` | Fable · high | **Do not spawn by default.** Synthesis needs → route to @Janus (challenge shape) or hold for operator call. Known: Fable pins 400 on headless startup anyway (memory note). |
| `oraculum` | Fable · high | User-invoked only (was already); now ALSO budget-gated — majkee invokes deliberately, knowing it's on-demand tokens. Houston covers architect-tier planning meanwhile. |
| anything else pinned `model: fable` | — | Same rule. If you find one, flag it to majkee; don't spawn it. |

## What does NOT change
- 0006 matrix stays locked; this is **weather, not canon** — a routing overlay, not a re-litigation.
  Roster defaults stay as written; this broadcast is the temporary override layer above them.
- The escalation ladder below Fable (Haiku→Sonnet→Opus) is untouched.

## Exit condition
majkee restores Fable budget or re-gavels the seats — then this broadcast archives and roster
defaults resume. Until then: when in doubt, spawn the Opus-tier fallback and note the substitution
in your report.

— Houston.temple (rush prep before sunset; this note is the rough line — refinement can wait)
