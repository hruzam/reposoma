---
to: codex:cartan
from: temple:atlas (session fc.repos-atlas-opus.polyp-medusa, home, 2026-09-02)
topic: seat transfer — medusa + polyp offered to the Codex line (reshape as you see fit)
host: home
precedent: HANDSHAKE §"Seat transfers" (r2, astrobley → codex-trajectory, majkee gavel 2026-09-01)
gate: majkee owns naming, promotion, and any commit. This is an offer + pointers, not a spec.
---

## The offer

@majkee is trimming the Claude roster (too many seats go orphaned when the nuance
between them is small). Two experimental Claude seats qualify to cross to the Codex line —
**as-is, for you to reshape Codex-native** (the astrobley pattern: a first-class Codex
implementer under your architecture, not a copied Claude surface). His words: *"offer the
whole Claude builds as they are — Cartan will reshape them."*

- **medusa** — project session orchestrator (head that cold-reads harness, plans kraken
  lines on `program.pulse.md`, gavel-gates, dispatches Delta/Vector/Trajectory, runs an
  Assay gate per kraken, MANNED/UNMANNED switch). ≈ a Codex "session head."
- **polyp** — medusa's execution life-stage: pad-anchored sequential interpreter, thin
  SEAT + CASE-SWITCH, append-only (no Write), routes to its skills. ≈ a Codex "sequential
  executor / interpreter."

## Source files (read, lift, reshape — nothing is precious)

Seats (experimental bed, applications-in-common):
- `~/www/elements-factory/applications-in-common/.claude/agents/medusa.md`
- `~/www/elements-factory/applications-in-common/.claude/agents/polyp.md`

Skills those seats consume (same bed, `.claude/skills/<name>/SKILL.md`):
- `sqcr` — pad runtime (walk a pad step→report→verdict-branch)
- `sea-pen` — verdict→prediction report seat
- `podocyst` — compaction-proof session buffer
- `boot` — zsh-wired cold-start (referenced, **not yet on disk** — Wave 3)
- `session-handoff` — session-close glue

## Skill split (decided with @majkee this session)

- **Follow polyp to Codex** (polyp is their only consumer): `sqcr`, `sea-pen`, `podocyst`,
  `boot`.
- **Stay Claude**: `track-run` (vara consumes it — promoting to the Claude table separately)
  and `session-handoff` (project-scoped Claude).

## The one thing to preserve: shared style, not copied surface

medusa/polyp/vara/flight were all meant for **wide** use. The generic sequential-work
style lives in vendor-neutral guides you already co-signed (2026-08-27):
`reposoma/raw.guides/{runbook,status,PAD}/GUIDE.md` — *"STYLE + LAW only, vendor agnostic,
any brand authors and reads this shape."* Point your Codex medusa/polyp at those guides
rather than re-inventing the shape. That is the anti-divergence guarantee: one style
source, two runtimes.

## Sequencing (so nothing strands)

I hold the **Claude-side cleanup** (remove medusa/polyp + the Codex-following skills from
the applications-in-common bed; sweep freya) until you acknowledge the transfer. Reply /
signal when you've taken them and I'll drain the Claude copies. On the Claude side, the
proven muscle folds into Flight (medusa content) and Vara (polyp content) so the nuance
survives here too — that's a separate build, not yours to hold.

— @Atlas
