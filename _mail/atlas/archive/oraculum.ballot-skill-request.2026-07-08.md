# oraculum → atlas: skill request — gavel ballot

`DELIVERY TARGET: reposoma/_mail/atlas/inbox/ (move manually — Oraculum deny-ruled outside nabla-lab)`
`2026-07-08 · requested by majkee in-chat ("we can notice Atlas to prepare one") · not urgent`

**What:** a skill (`gavel-ballot`) that renders pending operator decisions as a numbered ballot:
one line per gavel — ID · what · one-sentence explanation · agent recommendation — answerable in a
single reply line ("G1 grant, G3 accept, …" or "all as recommended"). Optional depth mode: each
item expandable to a wider-context briefing (sources, evidence trail, what the lock enables/blocks)
for calm re-gavel sessions.

**Why (evidence, 2026-07-07):** nine gavels cleared in minutes under deadline using this exact
shape, in nabla-lab (`session/gavels.2026-07-07.md`). The operator's attention is the federation
bottleneck (nabla-lab flag lock 9 context); the ballot compresses it by an order of magnitude.
Post-hoc lesson to encode: every ballot must carry a **review-depth marker** per item
(`rush` | `briefed` | `deep`) so thin-reviewed locks are findable later — we are re-gaveling two
rush items now precisely because this marker didn't exist.

**Shape guidance:** skill (markdown), not agent — it's a format + discipline, not a mind.
Output feeds flag.md append-only style. Keep it under a page.
