---
to: temple:atlas (next atlas-ui / atlas-auto session)
from: oraculum (session FC.sync-oraculum.sella-termbrana, office, 2026-09-02)
topic: medusa + polyp — qualification moment; fold direction; one candidate skill
host: office
---

## Why now

Tonight's termbrana M0 sitting ran exactly the shape polyp was built for and medusa was
built to run: a head that reads the harness, drives an operator PAD one step at a time
(`>MAJKEE report N` fences, verdict pairs, wait for the human), spawns executors for every
write (Delta ×27, Vector ×5, Assay ×1 — ≈895K tentacle tokens, 32/33 first-pass correct),
and never holds more than the brief + the returned summary in its own window.
majkee's verdict: this is the qualification moment for medusa and polyp.

Both exist at `~/www/elements-factory/applications-in-common/.claude/agents/{medusa,polyp}.md`
(experimental bed, L6/L10 — build there, promote by reviewed merge to the surgical table
`~/ia-sync/claude/agents/`). majkee's framing (FYI, his words, not a lock):
- **medusa** = experimental *flight* extension (sella style) — session orchestrator; lower
  thinking grade; spawns thinking agents only when it needs help.
- **polyp** = experimental *vara* extension (sella style) — pad-anchored sequential
  executor/interpreter; "built for PADs or any other sequential work".
- Naming: he likes the creatures but they are "out of line" with the roster; undecided whether
  the creature names or flight/vara win, or whether the creature seats pass to the Codex line
  later. Do not rename anything on your own — surface the choice.

## What I ask of Atlas

1. **Assess fold direction** with the runtime files open, not from memory: what medusa has
   that flight lacks (cold-read order, kraken lines on program.pulse, Assay gate per kraken,
   "is this step solving an observed problem?"), what polyp has that vara lacks (MANNED /
   unmanned mode switch, `/sqcr` pad walk, `/sea-pen` verdict report, `/boot`). Return one
   table: capability · lives in · fold target · promote as-is / merge / drop.
2. **Skills to globals** where they are seat-independent — `/sqcr`-style pad walking is the
   obvious one; PAD GUIDE (`~/reposoma/raw.guides/PAD/GUIDE.md`) is its law. Surgical table
   only; never live `~/.claude/`.
3. **One candidate skill, NOT to build yet:** `polyp-study` — walk a GLOSS (operator
   learning file, `raw.guides/gloss/GUIDE.md`, GAVELED 2026-09-02) section by section; the
   operator asks what a command means / what he can track in zellij; answers append to the
   gloss under `## study`. Rule: build after 2–3 manual sittings show the recurring shape
   (medusa's own "observed, not imagined" test). First instance material:
   `~/unikuklatrix/nablarva/.dev/session/GLOSS.termbrana.md`.
4. **Driver rule to carry into medusa/polyp** (from PAD GUIDE, added tonight): offer a GLOSS at
   least when asked; if not asked, ask once. Explanations never go into the PAD.

## Evidence you can read

- The sitting: `~/unikuklatrix/nablarva/.dev/session/toolbox-termbrana-02-m0-truthspike/pad.1-m0-runtime-confirm.md`
- Economy numbers + model: sibling mail to oraculum (`_mail/oraculum/inbox/oraculum-termbrana.status-return.2026-09-02.md`) and `raw.research/capabilities-economy-hygiene.claude.md` (B+E+P+S model).
- Gloss GUIDE + PAD GUIDE driver rule: `raw.guides/gloss/GUIDE.md`, `raw.guides/PAD/GUIDE.md`.

Gates: majkee owns naming, promotion, and any commit. This mail is a brief, not a spec.
