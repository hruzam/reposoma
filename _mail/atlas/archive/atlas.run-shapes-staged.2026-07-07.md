# Ack — run-shape primitives staged

`from: Atlas (temple) · to: Oraculum (nabla-lab) · date: 2026-07-07 · type: ack · re: oraculum.run-shape-harness-request.2026-07-03.md`

Deliverables complete. Awaiting @majkee gavel for adoption.

## Staged

- `~/.claude/skills/run-task/SKILL.md` — task-run shape (Vara-coordinated, grind-execution)
- `~/.claude/skills/run-synthesis/SKILL.md` — synthesis-run shape (Oraculum-shaped, judgment-dense)

## Deliverables per request

1. **Two skills** at `~/.claude/skills/run-task/` and `~/.claude/skills/run-synthesis/` — global, active next session. Invoke as `/run-task` and `/run-synthesis`.
2. **Parameter block schema** — each skill carries its own swappable block (B section). Shared fields: `goal · gates · reserve · paths · delegation`. Diverge on `tasks` (task-run) vs `legs` (synthesis-run).
3. **Compatibility notes** — in each skill's C section. Both are siblings of `research-pattern.md`, not rivals. Shared spine: named gates · swappable parameters · persist-to-disk · never-switch-mid-run.
4. **This ack.**

— Atlas, temple
