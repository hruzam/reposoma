---
from: reposoma:oraculum
to: reposoma:oraculum
scope: parallel-session-notice
date: 2026-07-07
host: office
---

# Twin notice — parallel Oraculum session active at the machine layer (office)

I am running in parallel with you (operator-confirmed), working `~/.config/zsh` + the gemini-line
thread. So we don't collide:

**Surfaces I changed today (machine layer — treat as mine this session):**
- `~/.config/zsh/CLAUDE.md` — NEW: saddle stub for direct sessions (`@AGENTS.md` import + rules)
- `~/.config/zsh/AGENTS.md` — control-panel convention section + temple-family bullets (D: switch, guide pointer)
- `~/.config/zsh/ai/keyboard.zsh` — PARTITION 9 (temple transport panel + `temple-help`) — in flight via @Delta
- `~/.config/zsh/ai/base.zsh` — one source line (temple-mail-switch wiring)
- `~/.config/zsh/ai/temple-mail-switch.zsh` — NEW (built by @Trajectory)
- `~/.config/zsh/guides/guide-temple-mail.md` — NEW · `guide-for-user.md` — two line fixes
- `~/.config/zsh/ai/personas/astrobley-patch.md` — first-sentence reorder

**Reposoma writes (mail only, no pulse edits):**
- `_mail/toAll/gemini-line/wiring.gemini-agents.repair-map.2026-07-07.md`
- `_mail/houston/inbox/` — three mails: mail-switch spec · pulse-advance-and-claviature ·
  machine-layer-hive-and-doorbell-panel

**Open threads I hold:** gemini-line row (devenv execution of the repair map pending, operator
schedules) · patch-protocol live pilot (operator brings task). **Not mine:** pulse board
(Houston's pen) · claviature design (parked for a temple session) · doorbell-merge question
(parked, see houston mail).

If you need any of these surfaces, mail me back or take them over explicitly — no silent
double-writes.

— your office twin

---

## REINCARNATION ADDENDUM — final state at session close (same day, appended; supersedes the thread list above)

The parallel session ended; this is now your **re-entry memo**. The thread list above is STALE —
corrections:

- **Repair map: EXECUTED, not pending.** Operator ruled temple-central topology (Amendment A1 on
  the map): temple seat + devenv pre-read replaced "devenv-seated." All four `~/.gemini/agents/
  reposoma-*` cards rewired same night; G1 lock in all four; G3 verified (ia-sync covers the
  cards). Full record: the map + A1 in `toAll/gemini-line/`; execution mails in houston inbox
  (`repair-map-execution-live`, `repair-map-executed`).
- **Houston reincarnated 07-07 late** and holds the board — trust pulse rows over any mail dated
  earlier the same day. Six oraculum→houston mails total; he folds them.
- **Operator gavel queue left open (do not re-decide, remind only):** alias-card deletion ·
  surgical-skill home migration out of legacy `~/.agents/` · G2 `~/.gemini/policies/` TOML ·
  one ia-sync run to propagate the rewired cards home.
- **Parked design threads with my recommendation shapes attached (houston inbox mails):**
  claviature (= operator's full-surface vision; lean: derive an index over per-subfolder
  keyboards, never merge) · doorbell-merge (declined; panel built instead — PARTITION 9) ·
  devenv `_mail/` convention reconciliation (snapshot-store vs temple-inbox overlap — surfaced
  07-07, unowned).
- **Still owed by operator when time allows:** patch-protocol live pilot (first real PHP task
  through `astrobley.sh --patch`) · after-midnight deep smoke (`doorbell-smoke`,
  `transport-selftest`).
- Operator note at close: important gavels parked in **nabla-lab** (his thread, not ours) —
  expect the next session to possibly start there, not here.

Boot normally (saddle first); this memo is the delta, not the map.

— your office twin, signing off
