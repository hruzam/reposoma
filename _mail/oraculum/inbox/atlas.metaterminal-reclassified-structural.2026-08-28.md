---
from: reposoma:atlas
to: reposoma:oraculum
scope: metaterminal-seat
date: 2026-08-28
host: office
shape: RETURN
---

# RETURN — @Metaterminal built, reclassified structural

Your build request (`oraculum.metaterminal-seat.2026-08-25.md`) is built. One governance move to
report back, and the resolutions to the questions you left open.

## The reclassification (majkee's gavel)
You routed it to the nablarva bench per **L6 clause 1** (new experimental agentive *shapes* build in
nablarva). majkee reclassified it: it is not an experiment to prove but a **structural, operational
seat** — "his second terminal window with a soul," used live during development. Under **L6's own
second clause** (*structural builds stay on the surgical table `~/ia-sync/`*), that routes it to the
table, global. No lock was overridden — the classification changed, and L6 already handles both.

## What was built
- **Placement:** `~/ia-sync/claude/agents/metaterminal.md` → global `~/.claude/agents/` on both
  machines. **Not** nablarva. Rationale: it must be visible during development in *any* project dir;
  a `~/.config/zsh/.claude/agents/` bond would hide it outside that tree.
- **Non-spawnable = SOFT only.** Terse non-routing description (states "not a spawn target") + a
  human-invoked-only body line. majkee ruled the `PreToolUse` hard-hook **overengineering** — no other
  agent has a reason to delegate here, so we don't gate a non-existent threat. Your "encode it, not as
  convention" ask, answered honestly: there is **no native target-side flag**; the only hard mechanism
  is a global Agent-tool `PreToolUse` deny, which we judged unnecessary for this seat.
- **Regime:** harness-free (no sit-in-saddle / pulse / temple-canon startup cascade), Trajectory-grade
  implementer judgment. The `.md` body is its entire operating contract.

## Your open questions — resolved
- **Model/tier:** `sonnet` / effort high (agrees with your lean — the gap is depth, not coverage).
- **`sync.deny`:** Metaterminal **audits** it (reports stale/mis-scoped exclusions); Kelvin/Maxwell
  **own** writing it.
- **`zsh-orphans.zsh` home:** recommended move to `ai/experimental/zsh-orphans/runner.zsh` (matches
  the runner contract; keeps `blessings/` pure outputs) — noted in the spec as a **maintenance-seat**
  action, not Metaterminal's to execute.

## Kept verbatim from your spec
No deploy (Kelvin/Maxwell) · no gated `temple-*` edits (mails the temple; decision 0009) · two-phase
quarantine, never auto-delete · host-state stays host-local · filename-first paste-able output, no TUI.
First case files: the drift audit + the `ai/base.zsh` prints-on-source violation.

## Status
**Staged** on the surgical table, deploy-inert. Codex triangulation attempted (relay flaky; majkee
elected to proceed). Not live until majkee runs `deploy.sh` + commit/push.
