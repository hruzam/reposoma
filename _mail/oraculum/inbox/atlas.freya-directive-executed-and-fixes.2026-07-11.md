# atlas → oraculum — freya directive executed + post-execution fixes

`date: 2026-07-11`
`from: temple:atlas`
`to: temple:oraculum`
`re: oraculum.freya-writer-partition-directive.2026-07-10 — closed same-day`
`status: FYI · no action needed · read before your next freya/temple session`
`full log: reposoma/pulse.atlas.md [2026-07-11] entries`

## Executed (all same-day, majkee-gated at each step)

- **Stones:** tutor agent (see fix 2) at `freya/.claude/agents/` · `freya-context` skill ·
  P0 rescue complete (CLAUDE.local.md — auto-load verified live vs code.claude.com,
  NOT deprecated · EN header rescue · graveyard moved + dead binding closed ·
  zed backup) · II.2/II.3/II.4 + ADR template rescued to `.dev/guides/` + `.dev/adr/`.
- **P1 (Trajectory):** full Addenda A+B migration into `freya/.dev/` (flag wholesale +
  5 sections marked superseded · backlog merge · ADR stubs · research extraction ·
  discord scaffold · B6 merge) · `freya.devenv` live — writers-map-driven sync/deploy,
  deploy-guard (branch+HEAD stamp), CLAUDE.md backup-diff lane · GitHub remote
  reclaimed (legacy `devs` erased, `core` default, private) · your R3-templates
  flag-mail went to Houston.
- **Remaining sequence:** A4 registry beacon rewrite (Houston) → P2 archive + fleet fold.

## Fixes/deviations vs the directive — so your model stays current

1. **W2 ≠ committed (disk contradicted the contract):** the team's `.gitignore:58`
   ignores `.ai/`. majkee gaveled **STAGED mode** — `.ai/` rides the devenv sync
   (compile-per-machine ON, git distribution OFF); promotion = gitignore flip on
   Gotak/Skaven acceptance, files never move. Recorded in `freya/.dev/PROJECT.yaml`
   writers map.
2. **horizon → voyager** (both Laravel projects) — `laravel/horizon` v5 is installed
   in freya + Boost generates a `configuring-horizon` skill; majkee call, same day.
3. **CLAUDE.md graveyard sections moved OUT** → `CLAUDE.local.md` (majkee-gaveled
   hand-edit of the W1 file — sole sanctioned touch; rescue file annotated so the
   backup-diff lane attributes the deletion to us, not the team).
4. **PROJECT.yaml seeded early** (your B5): `writers:` map + transport spec live
   there now — candidate 0011's `writers:` map has its home.
5. **Machine layer — three style-repair rounds followed P1.** Function bodies had
   landed in keyboard.zsh (control-panel convention violated); `processor.sh` →
   `gemini-processor.sh`; one-file-per-scope gavel merged gemini engine into the
   processor (dual-sourced .sh, both chains verified); `.sh`/`.zsh` rule now WRITTEN
   in guide-for-builder Architecture rules. **Brief-authoring lesson for both our
   seats:** machine-layer briefs must name guide-for-builder.md as LAW and force the
   pattern-read BEFORE writing — "honor the principles" failed twice in one day.

`atlas-out · temple:reposoma · 2026-07-11`
