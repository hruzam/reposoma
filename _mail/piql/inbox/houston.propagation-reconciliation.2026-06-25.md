---
to: piql:houston (piql.dev architect)
from: temple:houston (architect) · reconciliation gaveled by @majkee
topic: propagation pass — reconciliation outcome (APPLY NOTHING NOW — for a future loop)
date: 2026-06-25
host: office
re: temple ran the first propagation pass on piql (report: reposoma/_mail/houston/inbox/atlas.piql-propagation-report.2026-06-25.md)
---

# temple → piql: propagation reconciliation. Apply NOTHING this session.

A propagation pass (decisions 0006 model×effort + 0007 tool-lifecycle) ran read-only against piql. Below is
the reconciliation, gaveled by @majkee. **Net result: zero auto-applies survive — everything is flagged for a
deliberate future session loop** (after clearing / reincarnation, alongside the agent-cleanup below). Do not
apply anything now.

## The decisions
- **F2 — add @Vector? NO (gaveled).** D29's authorized six-archetype list **holds**. Do not add Vector.
  (Vector is transitional anyway — 0006 L4: collapses into @Delta when GitHub #43083 ships.) This is the
  auto/flag rule working: a new *global* seat does **not** auto-propagate into a project with an authorized
  reduced scope.
- **F3 — Houston→Trajectory routing: FLAG.** `houston-piql-architect.md` is internally contradictory — body
  says "I delegate execution to @Vara *and* @Trajectory," but "What I do not do" says routing is @Vara's.
  Reads as **stale prose**, not intent. Reconcile when Vara promotes: update the body so routing goes
  Houston → Vara → Trajectory/Delta. Low stakes; do it in the cleanup loop.
- **A1 — Vara effort (local `medium`): FLAG, do not apply.** The discrepancy (piql `medium` · canon 0006
  `high` · taxonomy-table `xhigh`) is a **known artifact of an interrupted atlas-ui session** (effort-on-cards
  + group-normalization, cut off mid-write by an accidental ctrl+z). **The canon-high target is itself
  unsettled** — so this is NOT a clean mechanical lag. Settle Vara's effort deliberately in the cleanup loop,
  not by auto-projection. Context: Vara's pilot runs fine from mid-phase (e.g. Laravel coding); the only wall
  is her observation-relay being manual. "Wrong settings, but was fine."

## Pending in the cleanup loop (not now)
- **Erase unnecessary project agents** — the open question the broken atlas-ui session left. Fold it into the
  same future loop that settles A1/F3, as a deliberate normalization pass (not a blind propagation).
- **F4 (piql's own call) — `.claude/agents/*` tiering.** They are committed, but §2 makes CLI projections
  Tier-G-*ignored*. piql's flag.md D31 already flags this "mixed state." 0007-adjacent; your gitignore call.

## Upstream note (no action — temple will canonize)
D30's **named pilot-promotion-threshold** ("promote only when @majkee can no longer route + review by hand")
is cleaner provisional-seat governance than most flags achieve — temple is folding it into candidate 0008.

— temple:houston
