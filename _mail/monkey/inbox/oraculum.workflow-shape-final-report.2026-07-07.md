`from: Oraculum (temple) · to: majkee · date: 2026-07-07 · type: FINAL REPORT + gavel list (the stone, closed) · host: office`
`thread: workflow-shape 2026-07-07 — X (majkee) → 2 blind Ys (Houston · Oraculum) → fold → cross-check PASSED → 2 refinements folded`
`sources: _mail/oraculum/inbox/houston.workflow-shape-{Z-request,Y,synthesis}.2026-07-07.md · _mail/houston/inbox/oraculum.workflow-shape-{Y,triangulation}.2026-07-07.md`

# FINAL : Multi-seat workflow shape — the fahrplan + your gavels

## The one-line result
**The rigid-vs-gray question was the wrong axis — both blind passes independently found the same
answer: keep the loop gray, rigidify only a thin state contract, and dissolve your re-entry pain
with a write-side discipline + a read-side board.** The stone worked: convergent invention
(the route-record device) from two clean-blind seats, cross-check passed, deltas resolved by
adoption. This is as settled as a pre-gavel thread gets.

## What the stone settled (needs no debate, only your gavel)
- The loop stays **gray**: A/B orchestration, token dial, Janus/Epoch entry remain judgment calls.
- The pain is **observability at re-entry**, not routing. The handoff cost moves to the other
  side of the gap: **the seat pays at suspension; you only read at return.**
- Design metric for everything here: what it removes from *your* head.

## THE SPINE — 4 thin invariants (everything else stays gray)
1. **Single-holder** — every live thread has exactly one named holder seat at any moment;
   `holder` is a live field, since holdership drifts mid-line.
2. **Suspension-writes-state** — any seat going quiet writes 3 fields before dark:
   `position / next step / blocked-on-majkee?`. Enforced at pause, never reconstructed at return.
3. **Route stamp** on every drawn task-line — `route: A(orchestrate)|B(cards) ·
   in-loop:[Janus?/Epoch?] · basis: coupling×judgment×reversibility`. Records the decision;
   never codifies it.
4. **One board, one-screen as SIGNAL** (Houston's refinement, folded): overflow *warns* "stop
   opening threads," it does not forbid. Watch clause (mine): the signal must render as visible
   pressure — an overflow count above the fold, echoed in the rolling memo — or the governor
   dies as wallpaper.

## THE FAHRPLAN (execution order — sequencing is load-bearing)
- **Step 0 · NOW, zero build:** adopt the spine as discipline across all seats. Nothing to
  install; it lands the moment you gavel.
- **Step 1 · Per-context board (MVP):** mail-side; emerging home = `pulse.claude.md` (you already
  wired it into Flight's tactical saddle — suspension rows populate it). Ships with Step 0.
- **Step 2 · Cross-project board:** the MariaDB mail-fold projection — registry-level UNION over
  every seat's log. **Upgraded from "reprioritization" to load-bearing** (Houston's refinement 2):
  this is what actually dissolves the >3-day cross-project lose-track. It was queued
  wait-for-the-wall; your X *is* the wall. Must render an already-disciplined log (Step 0 first)
  or it ships a confidently-wrong board. delta-sql advises schema (its 07-03 reposoma schema work
  feeds straight in); @Trajectory/@Delta build; **Houston routes it post-gavel — the spine's
  first live route+holder stamp.**
- **Standing guards:** the board is a *rendered projection, never a second source of truth*
  (0010: DB = read-projection only; 0004: point-never-copy). And G-C: no process rigidification
  beyond the spine — buffering-cycle stays a craft tool.

## GAVELS REQUESTED (@majkee)
- **G1** — adopt the 4-invariant spine (one-screen as signal, with the visible-pressure watch).
- **G2** — two-scope board: per-context MVP on `pulse.claude.md` now; cross-project projection as
  the target. Confirm `pulse.claude.md` as the board home, or name another.
- **G3** — green-light the mail-fold task-line (Step 2), routed by Houston with the first live
  route stamp.
- **G4** — declare G-C citable: no rigidification beyond the spine (so future sessions can point
  at it instead of re-litigating).
- Recording vehicle (00NN row vs guide card) = Houston's call post-gavel; you gavel substance,
  he drafts the record.

## My suspension row (the discipline, practiced — I go dark after today, Fable budget)
- **position:** stone closed (this report) · audit thread paused at §F gavels G1–G8 pending
  (report 07-03) · §E rider on record: adr-guard-FIRST is load-bearing — snapshot H1/H2 broken
  state as fixtures if order must break · schema + transport-future directives filed 07-03,
  delta-sql testing complete, awaiting routing.
- **next:** after reincarnation — resume the 07-03 job line: audit §E execution (post your
  gavels) + fold today's spine gavels into it. Also pending: my own def's stale memo path
  (`_mail/to-monkey.*` → `_mail/monkey/inbox/houston.monkey-not-forget-this.*`) — Fork B,
  deliberately left for you to navigate manually; cheap fix whenever you choose.
- **blocked-on-majkee:** G1–G4 above + the audit's G1–G8 (07-03). Nothing else blocks.

— Oraculum, 2026-07-07. Thread closed on all three seats. Receiver owns this message.
