---
to: the next Houston (temple master) · @majkee
from: Houston.temple (2026-07-09 — saddle run: "T2 confirmed" → sequence locked → T1 designed, @Janus-challenged, @Delta-verified; STOPPED at the gavel bundle, Force 4)
topic: HANDOFF — oraculum-mail task line: T2 GAVELED (wait-for-the-wall) · sequence LOCKED · T1 read-side binding spec + deliverable-0 interface contract DRAFTED, REVISE-folded, disk-verified · gavel bundle waiting on majkee before any dispatch
date: 2026-07-09 (supersedes houston.monkey-not-forget-this.2026-07-08.md — that file stays on disk; most-recent wins)
host: office
note: the LIVE-THREAD BOARD is `pulse.claude.md` (§LIVE BOARD). This memo points, never copies.
---

# You are here (rolling memo — read after decisions/index.md + registry/index.md)

majkee gave one word — **"T2 confirmed."** That unblocked the oraculum-mail task line. This session ran the
head of the line to the gavel edge and stopped there (Force 4). Ledger 0001–0010 UNCHANGED. No commits (no
canon locked — everything is DRAFT pending majkee's gavel). Spawns this session: @Janus + @Delta, both
Opus/Sonnet — outside the Fable sunset overlay (still in force; check your own line before spawning Fable
seats).

## LOCKED THIS SESSION
- **T2 — schema execution-timing = wait-for-the-wall** (gaveled by majkee's "T2 confirmed"). The MariaDB
  projection is ONE implementation of the T1 interface; build it only when a real wall fires (board
  full-rewrite friction = the W-G3 step-2 argument / notifier un-park), never pre-built. O1 discipline.
- **Sequence T1–T6 LOCKED** (was PROPOSED in the 07-08 memo).

## T1 — DESIGNED, CHALLENGED, VERIFIED (gavel-ready, NOT dispatched)
Two DRAFT artifacts on disk under `temple/tools/`:
1. **`twin-readside.binding-spec.draft.md`** — the read-side boot-step adoption spec. Binds each twin's boot
   doctrine to the `temple-mail-inbox <origin>:<seat>` INTERFACE (R-b — never the layout). Invariant =
   presence-only, ask-first inbox read on boot; craft (wording/placement) left to the twin (sovereign,
   Force 4). Gate = twin boot CITES the contract + invokes the call (not a layout glob) → doorbell re-rings
   stop accumulating. Supersedes Action A of the pre-0010 `twin-propagation.read-side-step` draft (stays as
   history; its OQ5 resolved by 0010; Action B = out of T1 scope).
2. **`temple-mail-inbox.contract.draft.md`** — deliverable-0, added after @Janus. The restored, citable
   interface contract (the thing `tools.md:16` was supposed to point at). **@Delta-verified against the
   on-disk `ai/temple-mail-inbox.zsh`:** `<origin>:<seat>` positional; filename-only listing under an
   `INBOX … — N unread` header; empty→silent exit 0; errors exit 1 (usage/address/unknown-project) & 2
   (mapped-but-dir-missing); read-only; layout encapsulated (R-b holds by construction). Disk-truth,
   gavel-ready.

**@Janus verdict = REVISE, folded.** The weakest assumption: an interface with no citable contract makes
R-b a slogan (five twins would each reconstruct it from the example string → divergence = the second
parallel system R-b bans). Fix = deliverable-0 + a gate that requires a contract citation, not just the call
string. Q1→A1 (ride the mail transport, don't extend 0008 L4) · Q2 (majkee-by-hand first adoption) · Q3
(the temple-owns-the-kit / twin-lands-it split is Force-4-clean) all confirmed; Q4 dissolved.

## THE GAVEL BUNDLE (blocked-on-majkee — the ONLY thing owed before T1 dispatch)
1. Gavel the contract doc `temple-mail-inbox.contract.draft.md`.
2. Re-point `tools.md:16` (tool #6 spec home) to it — closes the dangling pointer.
3. Gavel the binding spec `twin-readside.binding-spec.draft.md`.
→ THEN dispatch: build the adoption KIT (@Trajectory/@Delta) + majkee-by-hand first adoption per twin
(piql first — lightest, already reconciled; then vacuole/freya/subai/reposoma.devenv). Twin boot files are
sovereign — nothing writes them without this gavel.

## FINDINGS THAT FEED THE REST OF THE LINE
- **Dangling pointer (T4/T5):** `tools.md:16` points its temple-mail-inbox spec home at
  `transport-and-doorbell.run2-and-circuit.md`, CUT in the 07-08 ④a-tail trio cut → gone. Concrete hit for
  **T4** (soft-flag office check) and exactly the class **T5**'s audit-card amendment names ("local dangling
  pointers"). The gavel's re-point (bundle item 2) fixes THIS instance; T5 generalizes the guard.

## CARRIES UNCHANGED (from 07-08 / 07-07 memos — read them for detail)
- T3 write-DSN + fold script — spawns ONLY when a T2 wall fires (per the ruling). delta-sql holds the tested
  `CREATE TABLE`s (`mail_messages` + `projection_meta`, drop-and-recreate, `_ro` DSN).
- T4 soft-flag check (office) · T5 audit-card amendment (Houston drafts → majkee gavels) · T6 mail hygiene
  (@Delta file-moves). **schema-directive** is now archivable once the gavel bundle lands (T2 was its
  brief-before-sequencing gate).
- majkee's lane: registry pass ③b+④b (incl. G4 archive of `registry/projects/`) · claviature design session
  (+@Janus) · track-back gavel → @Delta build · Fable-budget exit.
- Fable-sunset routing overlay (no Fable spawns by default) · CapCom = Sonnet·low · O4 normalization pass ·
  Atlas next incarnation (advisor-high unmount + 4 stale cards).
- Lessons: never rescope the gate · **trust disk over report** (this session: @Delta read the actual script;
  @Janus caught the dangling pointer) · adr-guard keys on `NNNN-*.md` only.

## INBOX STATE (houston)
keep-4 (advisor-high-wiring · schema-directive · transport-future-design · audit addendum) +
`oraculum.repair-map-routing-correction.2026-07-07.md` (Oraculum's own session — do NOT act; archive when
she closes it). schema-directive → archivable once the gavel bundle lands.

## Suspension row (spine)
- **position:** T2 gaveled → sequence locked → T1 read-side binding spec + deliverable-0 contract DRAFTED,
  @Janus REVISE folded, @Delta disk-verified. STOPPED at the gavel bundle (Force 4).
- **next:** majkee gavels the 3-item bundle → dispatch adoption kit + majkee-by-hand first adoption (piql
  first) → T1 gate (twins stamp; re-rings stop) → then T4/T5/T6.
- **blocked-on-majkee:** the gavel bundle · registry pass · claviature session · track-back gavel ·
  Fable-budget exit. Veto windows: standing (silence = consent, accepted 07-07).

## Housekeeping
- `_mail/` gitignored; the 07-08 memo stays on disk as history — THIS file is current.
- Board updated this session: oraculum-mail row rewritten + new 07-09 log entry.
- No commits (no canon touched — DRAFTs + _mail + pulse are unversioned/live surfaces).

— Houston.temple, 2026-07-09 (T1 designed to the gavel edge · next incarnation: saddle order is
decisions/index → registry/index → THIS memo → the board · safe to power off)
