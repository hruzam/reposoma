---
to: the next Houston (temple master) · @majkee
from: Houston.temple (2026-07-08 — saddle run: Oraculum keep-mail sanitation → fine task line PROPOSED, cut before gavel; next incarnation starts 100% in saddle from THIS file + the board)
topic: HANDOFF — task line T1–T6 drafted from the 3 Oraculum keep-mails · 2 items disk-verified CLOSED (C7 · dispatch-discipline) · sequence NOT locked — awaiting majkee's brief/confirm on T2 · everything else carries from the 07-07 memo
date: 2026-07-08 (supersedes houston.monkey-not-forget-this.2026-07-07.md — that file stays on disk; most-recent wins)
host: office
note: the LIVE-THREAD BOARD is `pulse.claude.md` (§LIVE BOARD). This memo points, never copies.
---

# You are here (rolling memo — read after decisions/index.md + registry/index.md)

Short saddle run. `/goal` → NO ACTIVE GOAL (houston.goal retired 06-29; unchanged). majkee directed
one job: **sanitize the 3 Oraculum keep-mails (07-03) + the impulse into a fine task line.** Done —
the task line is DRAFTED below and boarded, but the session was cut at the gavel question: **the
sequence is PROPOSED, not locked.** Ledger 0001–0010 UNCHANGED. No spawns this session (Fable
sunset overlay still in force — check your own model line against the toAll broadcast before
assuming anything about seats).

## VERIFIED CLOSED THIS SESSION (trust-disk-over-report held)
- **C7 (audit addendum)** — tools.md reachability from Door A: `temple/README.md:29` already links
  `tools/tools.md`. CLOSED; no action.
- **Dispatch-discipline gap (schema-directive item 3)** — Write-less seats' mail filed by spawning
  session as definition-of-done: `temple/system-map.md:140/166`, gaveled 2026-07-03. CLOSED.

## THE FINE TASK LINE (PROPOSED — do not treat as locked; get majkee's confirm first)
Distilled from: `oraculum.reposoma-schema-directive` · `oraculum.transport-future-design` (stays
PARKED, majkee 07-03) · `oraculum.audit-reconciliation-vega` — all three still in houston inbox.

**Our lane, in order:**
1. **T1 — part-3 twin read-side** (0010 residual, amendment R-b). Houston designs the binding spec
   to the `temple-mail-inbox` INTERFACE (never the layout) → @Trajectory/@Delta implement.
   Gate: twins stamp; doorbell re-rings stop accumulating. HEAD OF THE LINE.
2. **T2 — schema execution-timing ruling** (schema-directive item 1, Houston's court). Proposed:
   **wait-for-the-wall** (O1 discipline) — the MariaDB projection is one implementation of the T1
   interface; build only when a wall fires. Live wall candidates: (a) board full-rewrite friction
   (the W-G3 step-2 argument), (b) notifier un-park (see transport-future-design: notifier = first
   live consumer of `mail_messages`). No pre-building. **AWAITING majkee** — his 07-03 mail promised
   a direct brief before sequencing; ask: "was the 07-08 chat the brief, or is more coming?"
3. **T3 — write-DSN + fold script** (jsonl → fold → load → stamp) — spawns ONLY on T2 greenlight →
   @Trajectory/@Delta. delta-sql holds the tested `CREATE TABLE`s ready (schema shape is settled:
   `mail_messages` + `projection_meta`, drop-and-recreate, consumers on `_ro` DSN).
4. **T4 — soft-flag check (office):** are `raw.guides/piql/phase08-gates-BC-manual.md` +
   `piql-bus-usage.md` registered publishes or hand-copies? 10-min @Delta verify vs local
   `config.sync.json`.
5. **T5 — audit-card amendment:** mandate BOTH reachability methods (transitive closure + local
   dangling pointers) · second-brand citations are claims until disk-checked. Houston drafts →
   majkee gavels.
6. **T6 — mail hygiene** (needs file-moves → @Delta): archive transport-future-design (parked;
   un-park triggers now boarded) + audit addendum (+ the Vega exhibit, wherever it sits — not in
   houston inbox anymore). **schema-directive STAYS in inbox until T2 is gaveled.**

**majkee's lane (unchanged from 07-07):** registry pass ③b+④b — now explicitly incl. **G4
full-directory archive** of `registry/projects/` (licensed by 0004:60; re-anchor first; subai
pointer into beacon) · claviature design session (+ @Janus) · track-back gavel → @Delta build ·
Fable-budget exit condition.

## CARRIES UNCHANGED FROM THE 07-07 MEMO (read it if you need the detail)
Reshape COMPLETE ①→G9 · G9 fact-kind taxonomy lock (DEFAULT→roster · OVERRIDE→map edge ·
POLICY→0006-L5 · join=file-stem) · Fable-sunset routing overlay (no Fable spawns by default;
overlay ≠ canon) · CapCom = Sonnet·low · parallel-session advances (mail-switch DONE · gemini-line
repair map awaits a reposoma.devenv-seated session · claviature floor) · O4 normalization pass ·
Atlas next incarnation (advisor-high unmount + 4 stale cards) · lessons (never rescope the gate ·
trust disk · adr-guard keys on `NNNN-*.md` only).

## INBOX STATE (houston)
keep-4 (advisor-high-wiring · schema-directive · transport-future-design · audit addendum) +
`oraculum.repair-map-routing-correction.2026-07-07.md` — **Oraculum's own session handles that one;
do NOT act on it from this seat; archive it when her session closes it out.**

## Suspension row (spine)
- **position:** 3 Oraculum keep-mails sanitized → task line T1–T6 drafted + boarded; 2 items
  disk-verified closed; sequence NOT locked (cut at the gavel question).
- **next:** get majkee's T2 confirm (+ "was that the brief?") → lock the sequence → land T1 design
  (binding spec to `temple-mail-inbox` interface) → dispatch @Trajectory/@Delta.
- **blocked-on-majkee:** T2 confirm/brief · registry pass · claviature session · track-back gavel ·
  Fable-budget exit. Veto windows: standing (silence = consent, accepted 07-07).

## Housekeeping
- `_mail/` gitignored; the 07-07 memo stays on disk as history — THIS file is current.
- Board updated this session: new LIVE BOARD row `oraculum-mail task line` + dated log entry.
- No commits made this session (no canon touched; _mail + pulse are unversioned/live surfaces).

— Houston.temple, 2026-07-08 (task line drawn, gavel pending · next incarnation: saddle order is
decisions/index → registry/index → THIS memo → the board · safe to power off)
