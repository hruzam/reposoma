---
to: the next Houston (temple master) · @majkee
from: Houston.temple (this incarnation)
topic: you-are-here / what changed / what's open — 2026-06-25 (0008 LOCKED · Stage-1 transport BUILT)
date: 2026-06-25
host: office
---

# You are here (rolling memo — read after decisions/index.md + registry/index.md)

## What locked / shipped this loop
- **0007** (tool-lifecycle) + **0008** (propagation & transport) — both **LOCKED 2026-06-25**. `_mail` addressing
  convention gaveled. (0008: re-projection-not-broadcast · flag-by-default + canon-integrity precondition ·
  provisional-seat governance zone · L9 declared protected-set · L10 precedence-as-exception · two thin
  transports. Two @Janus passes folded; Beam B / executor-topology parked to a Stage-1 decision.)
- **0008 Stage-1 transport family BUILT + VERIFIED** (machine layer `~/.config/zsh/`, NOT canon). Reports:
  `temple/tools/transport-and-doorbell.build-report.md` (Run 1: P0 map · A `temple-mail` · B doorbell) +
  `transport-and-doorbell.run2-and-circuit.md` (Run 2: read-side `temple-mail-inbox` · recalibration).
  Recalibration folded onto P0 (one notifier family) — supersedes the 2026-06-19 Vega assignment by @majkee's go.
- **O4 Vara effort** SETTLED `high` (majkee gavel). Confirms 0008 (next piql pass auto-applies medium→high).

## Autonomous circuit — first run, validated (success path)
Ran a bounded build→test→fix→report circuit (`~/.claude/houston.goal`). Caps: trivial 2 · standard 4 · hard 6 ·
ceiling 8. Result: read-side (standard) converged in **3**; recalibration (trivial) in **2**. No ceiling hits,
no escalations, no sudo/install halts. **The FAILURE branch (stop+report / escalate-one-rung) is still
uncharacterized** — needs a deliberately hard task to force a ceiling. Calibration note: trivial=2 ran with
zero margin; consider trivial→3.

## The next action (your top of stack)
1. **Two operator go-live steps (both non-sudo, @majkee's hand):**
   - install the doorbell post-commit hook (one `cp`+`chmod` — in the build-report) → "react on temple changes" live;
   - schedule recalibration (`systemctl --user` timer or crontab).
2. **Houston:** wire the project saddle-boot to call `temple-mail-inbox` (consume the read-side).
3. **0008 Stage-1 decisions, when they come due:** executor-topology (pull-by-resident vs dispatch) · L10
   silence-default (opt-in vs opt-out). Both parked by design until the script writes / tokens are measurable.

## Also open
- **O4 cleanup loop:** erase unnecessary project agents + broader normalization (deliberate, NOT blind propagation).
- **O1** (live-slice, deferred) · **O2** (spectral-engine, parked).

## Housekeeping pending @majkee's hand (no Bash on Houston's seat)
- `mv _mail/houston/inbox/subai.houston.tool-lifecycle-doctrine.2026-06-25.md → archive/` (processed).
- Carry outbound mails to their basements (subai send-back · piql reconciliation).

— Houston.temple, 2026-06-25 (0008 locked · Stage-1 transport built · circuit validated)
