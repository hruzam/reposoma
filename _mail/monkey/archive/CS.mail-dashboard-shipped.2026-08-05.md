CS · 2026-08-05 · mail-palette + startup dashboard SHIPPED (Flight)

commit : reposoma 0cab9ac (core) · ia-sync dd66d22 (main) — palette+dashboard DEPLOYED both machines
task   : dashboard + mail-palette port shipped & working; forward tasks queued (Flight)
context: Flickering mail TUI → stable curses mail-palette (emit-and-exit + wrapper-reloop,
         all-expanded tree, width-wrapping bottom help). Startup dashboard (_dash_header →
         editable system/dashboard.md). Built by astrobley, fixed by Delta, @assay-gated,
         deployed, smoked OK on the vertical monitor. Trajectory card build-bugs repaired.
         astrobley study n=3/n=4 logged (dev-journal.astrobley.md): capable hands, self-verify
         UNTRUSTWORTHY, @assay load-bearing. Full detail → pulse.flight.md top entry.
resume : claude --agent flight (in reposoma) → saddle: pulse.flight.md top entry

── pending / waiting (this session) ──
- command-palette (Alt-k) wrap mirror — same width-wrap as mail-palette; queued, ~15min via Delta
- v2 / A′ palette build — DECIDED (alias trailing-comment = single source · live-rendered help
  panels · exact-regex map); build PARKED. Spec:
  ia-sync/session/command-palette/design.parked.v2-frontmatter-projection.md
- dashboard content — fill system/dashboard.md with real reminders (majkee, anytime)
- Houston gavels the provenance seed —
  _mail/houston/inbox/flight.trajectory-provenance-seed.2026-08-05.md

── forward tasks · Flight-dedicated (by name — majkee explains) ──
1. cold-start-card-as-task system — pending cold-start cards PER PROJECT, wired through the
   registry into ONE collection ("fall") space; content revealed via a terminal CARD-FORMATTER
   (first: survey what formatter is already on the table); usable by majkee AND agents; echoes
   the Claude-harness frontmatter-loading pattern (the side-help idea from the palette-producing
   preincarnation). Open: keep in mail, or a different shared space + its own script?
2. temple system mail revision (other mechanisms).

── discipline ──
On read: the right reincarnation MOVES this file to archive (decision 0010 read-state —
inbox = unread, archive = read). Volatile / drainable (Cinderella) — a taste, not a report.
