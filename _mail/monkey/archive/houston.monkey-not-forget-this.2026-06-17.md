# to the monkey — session close (2026-06-17 → 19) · ephemeral · the next incarnation reads this

The saddle (`AGENTS.md`) sends a fresh incarnation here. This is *where we stopped*. Drain as you go.

## You are here (one breath)
- **Temple saddle LIVE** — `AGENTS.md` (+ `CLAUDE.md → @AGENTS.md`): a fresh incarnation auto-loads it → reads decisions → registry → this memo → in saddle. **Role-neutral** (routes Houston vs specialist vs human; identity = agent def, not the card).
- **subai.devenv (#1): CLEARED 🟢** — building.
- **reposoma.devenv (#2):** §D locked · `PROJECT.yaml` authorized. **Next: P0 go + builder pick.**
- **registry: LOCKED (0004)** — beacons depositing (subai · reposoma.devenv · piql); freya/piql graph still-filling.
- **Canon: 0001–0004.** The one untuned string (O1) stays slack — waiting on data.

## What changed (this session)
- **Temple saddle built** — role-neutral entry card; *identity = agent definition, repo card = shared context.*
- **registry O5 → 0004 LOCKED** — point-never-copy · one-direction · **§4.7 made structural** (no `~/` paths; machine layer reached via 0003). Roster + onboarding-kit (item 8) wired.
- **reposoma.devenv `PROJECT.yaml` authorized** — capability archetypes · constraints→flag breadcrumb · `mcp_profile: none` · 4 pin-at-P0 flags.
- Earlier: goal erased · 0003 machine-provenance · onboarding-kit · zsh lighthouse · subai card.004/005/007 (cleared).

## Open — needs the operator's call
1. **Two new project kicks** — @majkee is writing them; lay intake copies + beacon slots when they land.
2. **piql.dev onboarding** — Atlas prompt ready (handoff: piql Atlas fills `raw.guides/intake/piql.dev.intake.md` from piql's existing files → temple authors `PROJECT.yaml`).
3. **`raw.guides/prompts/`** — proposed home for the temple hand-out prompts (tight invariants / loose craft). Build on his go.
4. **reposoma P0 go + builder pick** (Composer harden Orby's drafts, or Claude-implementer; Claude takes `check` + Synthesizer).
5. **subai spectral seat-grant** · **home Laravel onboarding** (reiterate *fresh*, B3) · reposoma RR-03/RR-04 dispatches.

## FYI (no action)
- **Gemini survived** (tokens/inertia) → **two builds (Gemini + Cursor), NO Claude mirrors** (agents = glue from invariant files). New Gemini seats proposed: **Orby + Vega** researchers (`gemini-3.1-pro-preview`), **Astrobley** (Margaret Hamilton) strong coder.
- O1 un-defers at the first real "needed live runtime truth a file couldn't give" wall.
- Office box. Saddle continuity = same-machine (memo gitignored); cross-machine = a future gavel (track a `saddle.md`) only if you hop mid-temple-work.

## Files to open
`AGENTS.md` (saddle) · `temple/decisions/index.md` (0001–0004) · `registry/index.md` · per-project `PROJECT.yaml` + `flag.md` · reposoma.devenv `CLAUDE.md → ### back to saddle`

---

## ✉ Hand-out — "deposit your beacon" (copy-paste to an architect / Atlas)
> **Deposit your project's lighthouse beacon.** Read `registry/README.md`. Copy `registry/_beacon.template.md` → `registry/<yourproject>.md`, fill the **pointers** (anchors: `PROJECT.yaml` · architecture · decisions/flag · conventions · lighthouse) + `sibling-of:` / `shares:`. **Point, never copy** — paths only, **no `~/` absolute paths** (a machine layer is reached via its decision record, e.g. 0003). Add your row to `registry/index.md`. Carry `host:` (`echo $MACHINE_NAME`) + `updated:`. Report the beacon path.
