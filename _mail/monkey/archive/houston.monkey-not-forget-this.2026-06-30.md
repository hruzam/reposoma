---
to: the next Houston (temple master) · @majkee
from: Houston.temple (2026-06-30 session — task-3 reconcile + commit/hygiene)
topic: HANDOFF — stale propagation prompt RECONCILED + COMMITTED (e736d94) · (b) draft git-durable (e59e49a) · _tester/ untracked+gitignored (0da89d3) · four-part direct-inform brief STILL the live other-task (now "task 1", @majkee steers next) · no goal active
date: 2026-06-30
host: office
supersedes-state-in: houston.monkey-not-forget-this.2026-06-29.md
---

# You are here (rolling memo — read after decisions/index.md + registry/index.md)

No goal active (`~/.claude/houston.goal` = NONE; 0008 goal retired 06-29). This session @majkee directed a
two-item sequence: **(3) reconcile the stale propagation prompt → then (1) the four-part direct-inform brief**
(task 1 = a fresh reopened session). **Item 3 DONE + committed + durable.** Item 1 untouched, still live.

## Git state at end of session (verified clean)
```
0da89d3  chore: untrack _tester/ scratch folder + gitignore it      ← HEAD, working tree clean
e736d94  propagation: reconcile pass prompt to locked 0008          ← task-3 deliverable
e59e49a  advisors: tiered advisor system (low/mid/advanced/high)    ← @majkee's commit; SWEPT IN the (b) draft
1e91d5e  roster: add Maxwell ...                                    ← session-start HEAD
```

## What closed this session (durable)

1. **Stale generic propagation prompt RECONCILED to locked 0008 + COMMITTED (`e736d94`).**
   `temple/tools/propagation-pass.prompt.draft.md` — conformed end-to-end (not just bucket 1):
   - **Polarity → flag-by-default (L2).** Default = JUDGMENT-RESIDUE → flag; auto-apply is the narrow
     exception gated by the **canon-integrity precondition** + format/exact-lag clause + L3/L4/L9 negatives,
     grounded on **asymmetric risk**. The pre-0008 "MECHANICAL → auto-apply, needs no human decision"
     first-bucket — the polarity piql's run FALSIFIED — is gone.
   - **Added the omitted dimensions:** L3 governance-vs-behavioral axis, L4 lint fence, L9 declared
     protected-set (cite-or-not-honored + canon-integrity symmetry), L10 precedence-as-exception. L5
     trust-exclusion + L6 stamp kept and re-cited. De-staled header/lifecycle (status CONFORMED; O3 RESOLVED;
     lines 66–77 "hypothesis/first-runner/lock-as-candidate-0008" → *Operating notes (post-0008)*).
   - **OQ3 honest framing:** this edit **resolves OQ3(a)** (polarity conformed) and **narrows OQ3 to (b)** —
     keep this standalone generic pass vs supersede with the (b) draft's steps / the materializer. The
     keep-branch's work is done because the fix was asked for now; only the file's **fate** is deferred to
     @Janus. Header says so — do NOT read the conforming edit as a vote for "keep."

2. **Bookkeeping committed/cleaned.**
   - The **(b) twin-propagation draft** (`temple/tools/twin-propagation.read-side-step.pass.draft.md`) is
     **git-durable** — it was swept into @majkee's `e59e49a` "advisors" commit (the broad stage that also
     added the new tiered advisor seats). No separate preserve-commit needed.
   - **`_tester/`** (operator scratch folder) was unintentionally swept into `e59e49a`; **untracked + added to
     `.gitignore` (`0da89d3`), kept on disk.** Won't re-add.
   - **06-29 monkey memo archived** (plain `mv` — `_mail` is gitignored, so this is a disk move, not git).

## THE LIVE "OTHER TASK" = task 1 (next — @majkee steers, then @Janus, then gavel)

**piql's four-part direct-inform brief** — `_mail/houston/inbox/houston-piql.temple-routing-direct-inform-partllyViaMajkee.2026-06-29.md`
(forwarded by @majkee). Question to temple: **is `_mail` + doorbell + universal-read-side + git-tracking THE
canonical async "directly inform anybody" mechanism? — yay/nay + optimal shape?** Four parts:
- **1 mail (LIVE) · 2 doorbell (built)** — done.
- **3 universal read-side** — = the (b) draft (boot inbox-read step → 5 twins). Cheap, ask-first. Carries
  **5 open questions for @Janus**; OQ3(a) now closed by this session's reconcile, so OQ3 is narrowed to (b)
  when the pass runs.
- **4 cross-machine carry** — **the delicate one.** reposoma gitignores `_mail` → temple mail can't travel
  cross-machine (this very brief was hand-forwarded — the mechanism describing its own gap; *this session's
  `git mv` failure on the 06-29 memo is fresh live evidence of exactly this*).
  **Architect's read for the gavel (unchanged):**
  - **Necessary-but-not-sufficient:** tracking `reposoma/_mail/inbox` doesn't deliver cross-machine carry
    alone — it still needs the cross-repo **mail physical-staging that 0008 L7 deferred to Stage 1, unbuilt.**
    **KEY: that staging = the Astrobley mail-sync probe** (card ready,
    `_mail/astrobley/inbox/houston.task2-mail-sync-probe.2026-06-27.md`). Part 4 ≈ run the astrobley probe,
    not "flip a gitignore." Flipping alone looks done while the transport is missing (silent gap).
  - **Contract change:** tracking changes reposoma's *"`_mail` is drainable, nothing here is canon."*
    Invariant to protect explicitly: **tracked ≠ canon** (transport durability, not authority).
  - @Janus pre-flagged bare "flip to tracked" as **B-flavored coupling**; piql's safe shape =
    **per-repo, receiver-owns, NOT a central bus, gitignore `archive/`**. Stress-test before yes.
- **Good fences to endorse (brief draws them):** no central mail-bus (stopped at piql) · real-time human
  urgency → live channel not `_mail` · synchronous req/resp → MCP/RPC not `_mail` (consistent with the
  do-nothing RPC mail `piql.office-daemon-gap3-rpc.2026-06-29.md`). Together they partition the transport
  space (mail=async-inform · RPC/MCP=sync · live channel=human urgency).
- **Recommended path:** @majkee yay/nay steer on the four parts → @Janus on the unified question (the (b)
  draft's open questions fold in) → @majkee gavels → dispatch (piql first — lightest).

## Carry-forward queue
1. **Four-part direct-inform decision (task 1)** — THE live item (above).
2. ~~Commit the reconciled propagation prompt~~ — **DONE this session (`e736d94`).**
3. **Commit 0009 + index row 0009 + machine AGENTS.md** — after Janus FINAL-confirm (mind the re-ring warning).
4. **Janus FINAL-confirm 0009** (L5 debt paid by the doorbell-smoke probe).
5. **Astrobley mail-sync probe** — card ready; **= part-4's missing transport** — @majkee dispatches the Gemini seat.
6. **0008 Stage-1 parked:** executor-topology · L10 silence-default — decide on evidence when the pass writes.
7. **O4 cleanup loop · O1 · O2** — unchanged.

## Housekeeping (no Bash on Houston's seat)
- ✅ **Stale generic prompt RECONCILED + COMMITTED** (`e736d94`). No longer carries the pre-0008 auto-first
  bucket; safe to dispatch as the Stage-0 hand-run gate. Only OQ3(b) (its fate) remains, queued for @Janus.
- ✅ **(b) draft git-durable** (in `e59e49a`); **`_tester/` untracked + gitignored** (`0da89d3`, on disk).
- ✅ **06-29 monkey memo archived.** (Archive THIS 06-30 memo too once superseded — disk `mv`, `_mail` is gitignored.)
- harness-check `aihs-stale` fires-check **still owed** (silent-death failure mode; one `aihs-stale` run closes it).
- Two older monkey-inbox items still present (presence-only, ask-first): `castor.agy-agents-comparison.2026-06-19.md`,
  `atlas.gemini-agent-handoff.2026-06-27.md` — neither read this session.

— Houston.temple, 2026-06-30 (task-3 reconcile done+committed `e736d94` · (b) draft durable `e59e49a` · _tester clean `0da89d3` · four-part brief is task 1, @majkee steers next · no goal active)
