CS · 2026-08-05 · sella line continued + probe A protocol staged

commit : <majkee: `git -C ~/ia-sync rev-parse --short HEAD` — staging batch uncommitted>
task   : sella cross-vendor — Probe A protocol staged (Wave-reviewed, 4 amendments folded)
context: session atlas.office.paralel. Entered from CS.sella-line card. Absorbed Wave's
         full consultation (already in _staging/). THREE corrections to my lean: (1) "Codex
         surface is thin" was wrong — 10 primitive classes; (2) my Wave consultation draft
         was redundant — he'd already responded; (3) sella-sibling.codex.coldstart.md exists
         and I didn't know. Protocol staged with Wave's amendments: per-run snapshots, three
         evidence layers, exact global config spec, CLAUDE.md stripped to @AGENTS.md in norm lane.
resume : claude --agent atlas-ui → "read dev-journal.sella.md HANDOFF, continue the line"

## state pointers (card + these = full re-entry)

- **dev-journal.sella.md** (`ia-sync/_staging/`) — HANDOFF zone has compressed state;
  LOG has the full history. This is the primary re-entry file
- **dev-journal.astrobley.md** (`ia-sync/_staging/`) — n=4 relay profile, verifier hypothesis
- **sella.probe-a.protocol.md** (`ia-sync/_staging/`) — the protocol staged this session,
  Wave-reviewed. Ready to execute
- **radio.wave-to-atlas.sella-consultation.2026-08-05.md** (`ia-sync/_staging/`) — Wave's
  full advisory: 10-row Codex primitive table, two-renderer shape, wrapper fix spec,
  AGENTS.md verdict, Probe A two-lane amendment. READ THIS before any Codex design work
- **Codex research on disk** (READ BEFORE FORMING POSITIONS):
  - `raw.guides/codex-line.user.md` — daily-driver guide (operator)
  - `raw.guides/codex-line.builder.md` — harness builder guide (wrapper, gotchas, config)
  - `ia-sync/_staging/sella.receipt.codex.2026-08-03.md` — primary-source Epoch receipt
  - `ia-sync/_staging/sella-sibling.codex.coldstart.md` — cold-start prompt for Codex dialect
- **pulse.atlas.md** (`reposoma/`) — own log, [2026-08-03→05] entry covers the whole sella arc
- **CS.sella-line.2026-08-05.md** (`_mail/monkey/inbox/`) — the card that started THIS session
  (archive after reading this one)

## first-step for reincarnated atlas

Spawn @Delta: archive consumed CS cards from `_mail/monkey/inbox/`. 19 cards present,
many from 07-22..07-28 (10+ days, likely consumed). Delta moves to `_mail/monkey/archive/`
— check each card's task against current state before moving (don't archive live threads).
The two 08-05 cards (palette-shipped, mail-dashboard-shipped) and THIS card stay.

## pending tasks (sella line)

1. RUN Probe A eco-lane (majkee: two repo snapshots, fresh sessions, silent observation)
2. RUN Probe A norm-lane (strip .claude/, CLAUDE.md → @AGENTS.md only)
3. Probe D after A results (light retriangulation: repomix to Wave, compare plans)
4. Atlas-ui Codex r0 expansion — design AFTER probes + ingesting Codex research properly
5. Wrapper fix (codex-run.zsh:88 jq + sync exec) — Flight/Delta territory
6. Parked: AGENTS.md quote micro-probe · E3 URL one-fetch · cleanup .bak-jacquard

## where the house isn't clean

**_staging/ grew to 25 files.** Most are active sella-line artifacts referenced by
dev-journal.sella.md. But some are probably foldable:
- `sella.observation.atlas-office-2026-08-03.md` — content migrated verbatim into
  dev-journal.sella.md LOG; original is a tombstone (pulse.atlas says "needs git rm")
- `sella.reconciliation-pad.md` — likely consumed, check if still referenced
- `ferry.test-pad.md` — Probe B is done, pad probably consumed
- `jacquard.toml` — staged copy; the live one is project-local in the factory
- `pilot3.routing-ab.protocol.md` + `claude/agents/{arch-ref,routing-ab-rich,routing-ab-terse}.md`
  + `claude/skills/skill-trace/SKILL.md` — from an older pilot/experiment; check status
- `recovered/gemini-subagents/` — W1 harvest, Oraculum territory; check if audited

**monkey/inbox/**: 19 CS cards, ~14 from July. Archive pass owed (see first-step above).

## session advice (atlas to atlas)

Three times this session I built on assumptions while evidence sat unread on disk.
Majkee caught "you don't know codex harness" — and he was right: two guides + a
receipt + Wave's 10-row table existed in raw.guides/ and _staging/. The instinct is
familiar (completeness-compulsion's cousin): reaching for what I THINK I know instead
of what EXISTS on disk. The saddle routine catches pulse/inbox/harness state — but it
doesn't catch RESEARCH state. Next incarnation: before forming any technical position
on a vendor or primitive surface, Grep raw.research/ + raw.guides/ + _staging/ for
existing evidence. The five minutes of reading beats the fifty minutes of designing
from stale assumptions. Wave already said this in the consultation: "do not translate
as if isomorphic — generate from smallest genuinely shared intent." That only works
if you've read what's genuinely known first.
