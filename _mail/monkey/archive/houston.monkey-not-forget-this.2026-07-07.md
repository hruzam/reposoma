---
to: the next Houston (temple master) · @majkee
from: Houston.temple (2026-07-07 night — FINAL edition: reshape executed + Fable-sunset rush prep; next incarnation starts 100% in saddle from THIS file + the board)
topic: HANDOFF — reshape queue COMPLETE (6+3 commits, ledger unchanged, appends only) · G9 locked post-Janus (fact-kind taxonomy) · FABLE SUNSET rough lines broadcast (toAll) · parallel-session advances boarded (mail-switch DONE · claviature NEW thread · track-back task awaits gavel) · CapCom settled Sonnet·low
date: 2026-07-07 (night — supersedes both earlier editions of this same file, in place)
host: office
note: the LIVE-THREAD BOARD is `pulse.claude.md` (§LIVE BOARD). This memo points, never copies.
---

# You are here (rolling memo — read after decisions/index.md + registry/index.md)

One day, three arcs: (1) morning gavels (workflow spine + audit delegation), (2) afternoon
**execution of the whole reshape free-hand queue**, (3) evening **Fable-sunset rush prep** —
subscriber Fable ended ~2026-07-07; it is now pay-as-you-go and OUT OF BUDGET. Rough lines for all
teams are broadcast in `_mail/toAll/inbox/houston.fable-sunset-rough-lines.2026-07-07.md` — read it
before spawning anything; **no Fable seat spawns by default** (advisor-high deferred→advisor-mid ·
agol held · oraculum = operator's explicit budget call). Ledger 0001–0010 UNCHANGED all day
(append-only touches: 0007+0008 evidence redirects · 0009 status note — majkee sighted all, standing).

## THE DELEGATION — still holds
majkee's two rules: (1) lighter + fat-grey-zone over cage · (2) reduce; scalpel duplicates; don't
recreate logs. Rail: locked-ADR touches = visible-veto diffs (0002); adr-guard enforces append-only.

## WHAT LANDED 07-07 (verification detail on the board)
Reshape: **①** `a45ec56` guide freshness (aihs-stale→guides by frontmatter) · **②** `915e06d`
evidence → **`temple/evidence/<NNNN>/`** (DEVIATION — never rescope the gate to admit cargo;
rationale in `temple/evidence/README.md`) · **③a** `413fd2d` index rows 0006–0010 → one-liners ·
`6b091ed` 0009 drift-append · **④a** `64f6cea` tools logs cut · **G9** `84bc733` roster/map dedup.
Rush batch (night, @Delta): CapCom roster fix · tools.md adr-guard row committed · ④a-tail trio cut ·
3 Oraculum mails archived — hashes in git log if the batch completed after this memo was cut.

## THE G9 LOCK (hold this — challenged by @Janus, then locked)
Fact-KIND partition: **DEFAULT**→`roster.md` (single live home) · **OVERRIDE**→system-map edge
annotation (edge wins; absence inherits) · **POLICY** (task-class ceilings)→ADR 0006-L5, never
copied · **join = file-stem, not display @name**. Taxonomy headers in both files. 0006 matrix =
locked snapshot, coexists by design (A1). CapCom settled: `Sonnet · effort:low` (majkee 07-07 —
"gate, not arguer"; tier = capability floor, effort = task class).

## PARALLEL-SESSION ADVANCES (Oraculum, boarded from her 3 mails — now in houston archive)
- **temple-mail-switch DONE** — built by @Trajectory to her spec, verified (21 candidates/6
  origins), wired into `ai/base.zsh`, guide at `~/.config/zsh/guides/guide-temple-mail.md`,
  operator field-confirmed. Derive-at-runtime, register-nothing principle held (0004 L4 · 0010 R-b).
- **gemini-line:** repair map PERSISTED (`_mail/toAll/gemini-line/wiring.gemini-agents.repair-map.2026-07-07.md`,
  6 verified breaks) — execution needs a **reposoma.devenv-seated session** (operator schedules).
  Astrobley conformance GREEN. Home-machine lost-days hypothesis CLOSED (window genuinely empty).
- **Machine layer:** `~/.config/zsh/CLAUDE.md` saddle stub now exists (imports @AGENTS.md; rule:
  "decision needed → draft + mail reposoma:houston, never lock from that seat"). Doorbell family
  NOT merged — PARTITION 9 panel keys + `temple-help` instead (split is load-bearing; 0009 L5
  tester must stay separate from testee).
- **NEW THREAD — claviature** (operator vision): global control panel over ALL zsh subfolders.
  Floor exists (Control-panel convention in machine AGENTS.md). Design NOT decided — derived-index
  vs merged-surface (Oraculum + Houston both lean derive; merge rebuilds the killed monolith).
  **One design session + @Janus before any lock.** Park the doorbell-merge instinct next to it —
  same instinct, same answer shape (panel/derive, don't merge).
- **track-back task** (operator work-trace tooling) sits in `_mail/toAll/inbox/task.track-back.build.2026-07-07.md`
  — DRAFT **awaiting majkee gavel** (part-1 placement + ~/sandboxes convention), then @Delta builds.

## OPEN / HELD (next incarnation's queue, in order)
1. **Registry pass (③b+④b as one wave)** — majkee maintains it next session (his new-project prep:
   `registry/index.md` + `registry/psdvsSys.md`). Includes index trim · `registry/projects/`
   retirement (0004) · the index pasted-reply fix. HANDS OFF until he lands it.
2. **Part-3 twin read-side** (bind to `temple-mail-inbox` interface, R-b) — natural next priority;
   doorbell re-rings accumulate on every decisions/ commit until the twins stamp.
3. **Claviature design session** (+ Janus) — when majkee brings it.
4. **track-back gavel → build** (majkee → @Delta).
5. **O4 normalization pass** (couple: erase unneeded project agents · add @AtlasAuto `medium` +
   @Zenith `low` to roster — they're in 0006-L2 but absent from the live home).
6. **Atlas next incarnation:** advisor-high unmount (mail in atlas inbox) + 4 stale settings cards
   (re-fetch + re-synthesize, NOT hand-edit).

## LESSONS / WATCH
- **adr-guard check 1 keys on `NNNN-*.md` only** → index/evidence/live surfaces are outside the
  locked-record contract by construction (load-bearing twice: index compression · evidence home).
- **Never rescope the gate to admit cargo** — move the cargo out of the gate's domain.
- **Trust disk over report text** — every wave disk-verified; ③a caught 0009 contradicting reality.
- **Fable weather ≠ canon:** the sunset broadcast is a routing OVERLAY; roster defaults stay locked
  (0006). When budget returns, archive the broadcast and defaults resume — no re-litigation.
- Board full-rewrite friction → still the live argument for W-G3 MariaDB projection (step 2).

## Suspension row (spine)
- **position:** reshape complete ①→G9 + rush prep done (Fable rough lines broadcast · CapCom
  settled · tools.md landed · trio cut · advances boarded); board + memo current.
- **next:** in-saddle order above — registry pass is majkee's; part-3 is ours; claviature +
  track-back wait on majkee's gavel.
- **blocked-on-majkee:** registry landing · claviature session · track-back gavel · Fable-budget
  exit condition. Veto windows: standing (silence = consent — explained + accepted 07-07).

## Housekeeping
- houston inbox = keep-4 ONLY (audit-reconciliation-vega · reposoma-schema-directive ·
  transport-future-design · advisor-high-wiring). toAll inbox = track-back task (pending gavel) +
  fable-sunset broadcast (standing overlay). All else archived.
- `_mail/` gitignored; both earlier editions of this memo superseded in place.
- Houston ran on Fable tonight (normally Opus) — next incarnation: check your own model line
  against the sunset broadcast before assuming.

— Houston.temple, 2026-07-07 night (three arcs closed · sunset lines drawn · next incarnation is
100% in saddle from decisions/index → registry/index → THIS memo → the board · safe to power off)
