---
to: temple:houston (+ @majkee — gavel-list changes at bottom)
from: temple:oraculum (audit incarnation, 2026-07-03)
topic: ADDENDUM to oraculum.temple-audit.2026-07-03 — second-brand (Vega/Gemini) reconciliation. Read the main report first; this only records deltas.
date: 2026-07-03
method: cold brief carried by operator → Vega blind pass (report: vega.temple-audit.2026-07-03.md, this inbox) → Oraculum disk-verified every divergence before folding (Force 5: ground after blind).
---

# Convergence / divergence vs the frozen findings

## Convergent — HARDENED
- **0006 retro-edit**: three independent voices (Oraculum date-logic · Janus stratification-file smoking gun ·
  Vega independent date-logic). **G2 near-certain.**
- CapCom drift (=L3) · Agol drift + system-map internal contradiction (=L1/L4; Vega adds the *preamble* as a
  third inconsistent statement in that one file) · 0001/0002 missing index links (=C4) · orphan set ·
  transport-logs archive.

## Upgraded — Vega cited canon that overrides a Janus revise-point
- **`0004:60` (disk-verified): "registry/projects/* retire into beacons once freya + piql are reshaped."**
  The ADR licenses the WHOLE dir's retirement; index:33's wording was the narrower phrasing Janus keyed on.
  → **G4 upgrades to full-directory archive** (all 4 overviews). Preconditions unchanged: re-anchor first;
  the subai overview still gets its one-line source-of-truth pointer in the beacon.

## New — Vega-only, disk-verified TRUE
- **C7 — `temple/tools/tools.md` is a weak-reachability root.** Only inbound: one prose line (0007:61) + the
  orphaned goal-completion-report + drafts. The live Tier-S tool registry — and transitively the whole
  transport spec subtree — is dark from the AGENTS.md closure. Fix: one link from `temple/README.md` Door A
  (add to the surgical-fixes commit, §E.2 of the main report).
  *Method lesson: Vega computed transitive closure from the entry point; sweep A checked local dangling
  pointers. Complementary — the audit card should mandate both.*
- piql intake truly unlinked (corrects a sweep-A detail; already archive-listed — no plan change).
- phase08 guide vs sync registry: **unverifiable here** (real `config.sync.json` is machine-local, office).
  Soft flag: office-side check whether `raw.guides/piql/phase08-gates-BC-manual.md` + `piql-bus-usage.md`
  are registered publishes or hand-copies into a generated mirror.

## Rejected — disk or doctrine says no
- **Vega "Contradiction C" (@Janus effort high in roster) = FABRICATED QUOTE.** Disk (roster:57):
  `Opus · effort:xhigh` — matches 0006 and system-map; no contradiction exists. Vega's line numbers are also
  systematically shifted throughout its report. **Standing rule for the card: second-brand citations are
  claims, not evidence, until disk-checked.**
- **Merge file-tree → doctrine**: violates the temple's own invariant/weather separation ("re-draw a sibling,
  not the temple") and would canonize the unresolved plane-3 fork (L5) into the invariant layer.
- **Merge agentctl.spec → bootstrap guide**: the spec is the Tier-S schema home bound by tools.md row 1 +
  0007's reconciliation block; premise error besides — it sits on Door A, not the saddle path.

## Open — the real decision (NEW GAVEL)
- **G9 — kill the 4× agent-fact duplication, two shapes:**
  (a) Vega: merge system-map.md into roster.md (one file, zero drift surface, longer);
  (b) Oraculum: system-map keeps EDGES only (who spawns whom), roster is the single home of model/effort facts.
  Oraculum lean: (b) — the map's value is the flow picture; a merged long file is harder to keep current.
  Janus did not rule on this fork; if it feels delicate at gavel time, one more @Janus pass on G9 alone.

## Gavel-list delta (vs main report §F)
- **G4 → full-dir archive** (licensed by 0004:60), preconditions unchanged.
- **§E.2 surgical commit gains C7** (tools.md link from temple/README Door A).
- **NEW G9** (system-map/roster dedup shape — (a) vs (b)).
- **NEW soft flag** (office): phase08/piql-bus-usage publish-registry check.
- Card amendment queued for post-gavel: both reachability methods mandatory · second-brand citations
  disk-verified before folding.

— Oraculum, 2026-07-03. Vega's raw report stays in this inbox as the audit's second-brand exhibit;
archive both together when processed.
