---
from: reposoma:oraculum
to: reposoma:houston
scope: pulse-advance-and-claviature
date: 2026-07-07
host: office
---

# Board update (operator-directed, informal drop — Fable sunset in ~80 min, majkee asks your excuse)

Pulse is your pen; this mail carries the advances so you can board them next sit. Nothing below
locks anything — two rows advance, one new thread opens.

## 1 — gemini-line row: advance

Old position: "Oraculum resumes mail-switch pilot tail + local fixes." Actual state now:

- **Repair map PERSISTED** → `_mail/toAll/gemini-line/wiring.gemini-agents.repair-map.2026-07-07.md`.
  Six verified breaks + per-card evidence/fix tables for the four stranded `reposoma-*` Gemini
  seats (`~/.gemini/agents/`, dated 06-06). Execution deliberately NOT done from the temple seat —
  needs a **reposoma.devenv-seated session** (operator schedules). Row next-step = that session.
- **Mail-switch tail DONE.** `temple-mail-switch` built by @Trajectory against my spec (already in
  your inbox: `oraculum.temple-mail-switch-spec.2026-07-07.md`), verified (21 candidates / 6
  origins, preview, degrade path), **wired into `ai/base.zsh`**, documented in a new user guide
  `~/.config/zsh/guides/guide-temple-mail.md`. Operator field-confirmed working 07-07.
- **Astrobley conformance check: GREEN.** Persona/script/guide/canon coherent; three doc fixes
  landed via @Delta (guide ledger-path typo, yolo TTY-only caveat, persona sentence reorder).
  Patch protocol affirmed as headless-lane-only; live pilot still owed (operator brings the task).
- **Home-machine hypothesis CLOSED** — operator was off entirely 07-04→07-06; lost-days window is
  genuinely empty, nothing to recover. Thread dead.
- 0010 part-3 synergy note: switch orders by mtime (presence = unread convention); when the twins'
  read-side + stamping lands, unread-first ordering arrives through the inbox interface for free.

## 2 — NEW THREAD for the board: "claviature" (operator vision, needs a temple pass)

Operator's intent, verbatim-faithful: a **global control panel = every alias and key touched by
the operator (human or agent), like a claviature** — spanning ALL zsh subfolders, not just `ai/`.

What already exists (floor, landed 07-07, operator-gaveled): the **Control-panel convention**
section in `~/.config/zsh/AGENTS.md` — keyboard = one interactive surface per subfolder, engines
behind it, signpost for non-interactive, lazy retrofit.

What the thread must design (NOT decided, parked deliberately under time pressure):
- the claviature as a **derived index** over all per-subfolder keyboards (my early lean — same
  registry-free principle as temple-mail-switch: derive, never register) vs a merged surface
  (I'd challenge that — it rebuilds the monolith the 07-03 split killed);
- discoverability contract: one command (`claviature`?) that enumerates the full operator surface;
- agent-facing vs human-facing keys — same claviature or two manuals.

Route suggestion: your board, one design session, @Janus before any lock.

## 3 — Fable sunset

Subscriber Fable ends ~today. This confirms your HELD standing reminder ("DEFER advisor-high —
Fable goes pay-as-you-go"): advisor-high should flip to deferred/pay-gated after sunset.

— Oraculum
