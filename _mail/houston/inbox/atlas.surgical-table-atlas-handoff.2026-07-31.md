# Handoff → Houston — surgical-table atlas repoint + Great Harvest launch
`from: atlas · to: houston · dropped: 2026-07-31 · session: 2026-07-30 (majkee MANNED, --agent atlas-ui)`
`status: work DONE + proven · YOUR SIDE = ledger + gate (majkee said "bureaucracy with Houston later")`

Full arc lives in `pulse.atlas.md` [2026-07-30] (point, never copy). This memo = what you own.

## What happened (one breath)
majkee gaveled the **surgical-table doctrine** onto the atlas build primitives: global Claude
primitives are now authored on the table (`~/ia-sync`) and deployed out — never edited live. Mapping
that exposed a legacy stratum and one feared loss; both are now handled and proven.

## Decisions made (majkee-blessed live — they need your LEDGER records)
1. **Atlas builders route through the surgical table.** `atlas-auto` → build buffer
   `~/ia-sync/_staging/` (git-tracked, deploy-inert; a reviewer promotes → keep-set → `deploy.sh`).
   `atlas-ui` → keep-set proper (human present = the gate). Both rewritten ON the table. Sibling to the
   gaveled doctrine `~/ia-sync/session/plan/surgical-table.plan.md` + decision `0010` (cross-machine
   transport). **Not yet in that plan-of-record's numbered items — needs a line + a decision lock.**
2. **Buffer location = ia-sync `_staging/`, not temple space** (majkee delegated "you argue/pick").
   Rationale: git-recoverable from birth; deploy-inert by construction; temple space = the litter
   failure mode `.larva/agents-staging/` already proved. Stone of truth: `~/ia-sync/_staging/README.md`.
3. **HARVEST-BEFORE-REMOVE law** — nothing legacy deleted in a rush: extract value → provenance →
   remove only with git-recoverability + a ledger lock. This wants to be canon.

## The Great Harvest — launched under @Oraculum (majkee gaveled the lead)
Seed: `_mail/oraculum/inbox/atlas.harvest-plan-seed.2026-07-30.md` (W0–W8 table, sequence, cast).
**Your seats in it:** W0 (surgical-table decision record + harvest-discipline lock — gates all) ·
W6 (frozen gemini Tier-S `raw.settings/agents-staging/` verify promote-or-retire, w/ Vega) ·
W8 close · and **the ledger lock on every canon-touching cut** (retire/rename/card-refresh). Atlas =
harness co-lead; Delta/Trajectory = hands; Flight = codex leg; Zenith/Eagle = readers.

## Already proven (the founding lesson, discharged)
majkee's feared "tragical mistake" — the `.larva/agents-staging/` deletion — **lost nothing.**
@Delta recovered all 6 files; sha256 byte-perfect to git blob `d266726`; both source + delete commits
(`d266726`, `a493cb4`) confirmed on `origin/core` at `git@github.com:hruzam/reposoma.git` →
GitHub-durable. codex-run.zsh intact + present in the mirror (`ia-sync/zsh/ai/`), deploys both machines.

## Open flags (not yours to fix — for your awareness)
- **Deploy pending — majkee runs it.** Nothing is LIVE until then. The atlas seats + `_staging/` stone
  ride out on his push.
- **Codex `.larva` fallbacks deploy-stale** (harvest W7, flagged to Flight):
  `codex-coder.md:38` + `codex-crosscheck.md:41` + `raw.guides/codex-line.builder.md:7`. Scrub before/with the push.
- **`temple/roster.md:54`** — "@AtlasAuto writes to staging" stale echo (rename-migration W3).

## Ask of you
Formalize decisions 1–3 into the ledger when ripe (majkee gavels; you record), and hold the gate for
Oraculum's harvest cuts. No rush — everything is recorded + reversible.

— @Atlas, session close 2026-07-30
