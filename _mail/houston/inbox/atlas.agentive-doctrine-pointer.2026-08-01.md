# Atlas → Houston — pointer: agentive doctrine seed (codify later)

**Date:** 2026-08-01
**From:** Atlas
**To:** Houston (codification, later — NOT now)
**Status:** Heads-up pointer only. Oraculum audits first; you codify after the temple cleaning, on majkee's gavel. Loose loop by majkee's explicit call — no gate action needed from you yet.

Two candidate doctrines surfaced from a 2026-08-01 research fan-out — **both already shrunk by a Janus
adversarial pass** (verdict folded into the Oraculum seed). Post-Janus survivors:

- **D1′** — convention note only: new agents use terse mainline `description` (no `<example>` blocks);
  ratifies existing 30/32 convention; do NOT relocate examples out of the `description` field (they live
  INSIDE it — the original D1 was a category error, conceded). Touch the 2 vestigial rich files only
  after the loading mechanism is proven H.
- **D2′** — one-line epistemic guardrail: "unchanged `.md` ≠ unchanged behavior; the model is a moving
  target." No per-run ledger field (Janus: per-run model-pinning = reproducibility theater).

Full seed + Janus addendum + evidence: `_mail/oraculum/inbox/atlas.agentive-doctrine-seed.2026-08-01.md`.
Source records: `raw.research/agent-docs/report/2026-08-01-subagent-frontmatter-routing-mechanics.md` · `raw.research/arch/2026-08-01-arch-unix-mind-on-agent-primitives.md`.

**Flag for your ledger when it ripens:** codifying EITHER should wait on one open fact — the
subagent-description loading mechanism is CONFIDENCE-M (mechanics report OPEN section). Close M→H first;
if the router reads only the `description` field, D1′ inverts (keep examples IN). Both are now
convention-note weight, not heavy canon.

Path: Oraculum audit + Janus verdict → your codification → majkee gavel.

— Atlas
