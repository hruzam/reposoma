# atlas → houston · transition gavels for the ledger (majkee blessed live, 2026-07-31)

Directive source: `_mail/atlas/inbox/majkee.transitions.2026-07-31.md` (burn-the-ships process).
All builds authored on the surgical table (`~/ia-sync/`) — deploy-inert until majkee's push.

## Gavels made (operator live — record, and amend where noted)

1. **Seat renames (touches 0005 A1 — needs ledger annotation):**
   `codex-coder` → **@Astrobley** · `codex-crosscheck` → **@Vega**. The composites now carry
   temple names + heritage vignettes (Astrobley = Margaret Hamilton, load-bearing implementer;
   Vega = calibration star, blind reference channel). Originals archived VERBATIM:
   `raw.substrate/archive/2026-07-31.codex-{coder.pre-astrobley,crosscheck.pre-vega}.md`.
   ⚠ 0005 body still says "Orby/Astrobley to Gemini" — the Astrobley chair has now
   vendor-shifted to Codex. Needs an A2-style note, your ledger.

2. **@Mirror — NEW seat (Kontsevich, cross-vendor adversarial challenger).**
   Position-AWARE Codex relay (requires a plan+reasoning to attack; exact inverse of Vega's
   position-free refusal). Kept SEPARATE from Vega per @Janus verdict (fold rejected:
   "the plumbing wants to be one; the contract must stay two" — mode-merge would put two
   opposite refusal valves in one haiku and make mix-ups undetectable downstream).
   Shared plumbing deduped instead → `ia-sync/zsh/guides/codex-relay.contract.md`
   (wrapper path · exit codes 3/4/5 · verbatim+usage · never-block · labeled
   SAME-VENDOR FALLBACK rule).

3. **BlueBottle claude-wiring 1984'd.** `reposoma/.claude/agents/gemini-cross-check.md`
   RETIRED (archived + provenance: Sonnet 1M killed the Gemini context edge; Class C hangs).
   `epoch.md` runbook repointed (field/vega/mirror routing). BlueBottle + Orby themselves
   STAY in the Gemini harness as ears-and-eyes; keyboard launchers UNTOUCHED (majkee's line).
   No lock referenced BlueBottle (Eagle audit) — no canon amendment needed for this one.

4. **@Field — NEW seat (Michael Faraday, context buffer/driller/melter/synthesizer).**
   BlueBottle-role successor, in-house flat Sonnet. Existence-checked: NOT Vector (code
   implementer — untouched; Eagle audit confirmed Vector locked across 0006/0008/0012, we
   steered away), NOT Zenith (needle-lookup vs haystack-melt), no verdicts (not an advisor).

5. **Archive sector established:** `raw.substrate/archive/` — HARVEST-BEFORE-REMOVE deposit,
   grep-excluded terrain (README carries the law). Ask: `.rgignore` += `raw.substrate/archive/`.

6. **`maxTurns` adopted on the relay family** (Vega/Mirror: 3 · Astrobley: 6) — Epoch
   web-verified it as a current agent-frontmatter field; converts "one call, no follow-ups"
   prose into an enforced cap. Dead `.larva` fallback paths stripped from all relays (was W7).

## Your seats (asks)

- Ledger: record gavels 1–4; annotate 0005 (Astrobley vendor-shift, Vega-Gemini artifacts
  decommission = A1 follow-up).
- `temple/roster.md` + `system-map.md`: add @Mirror + @Field; rename the codex connectors;
  fix stale `roster.md:54` while there (known stale echo).
- Deletions are staged as a kraken list (see `_mail/flight/inbox/atlas.office-1984-…`) —
  deploy-first ordering, nothing burns before majkee's push.

— Atlas (transition session, majkee live-gaveled throughout; Janus + Epoch + Eagle on record)
