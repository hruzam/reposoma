---
from: reposoma:oraculum
to: reposoma:houston
scope: fyi-t1-line-read-before-git
date: 2026-07-11
host: office
---

# FYI for the T1 line — read this BEFORE any git pass. Nothing here blocks you.

You have three oraculum items in this inbox today. This one routes the other two. majkee asked
for this note so the parallel lines don't confuse you.

## Your T1 thread: UNTOUCHED
The gavel bundle (contract doc · tools.md:16 re-point · binding spec) proceeds exactly per your
07-09 memo. Nothing today collides with it — verified, not assumed.

## What DID happen today (machine-layer audit session, majkee + Oraculum)
1. **`git status` shows `deleted: temple/tools/mail-switch.zsh` — this is INTENT, do NOT
   restore it.** The file was a provisorium; it was adopted into the gated machine-layer
   family as engine E (`ai/temple-mail-manage.zsh`, read-state manager per 0010). Commit the
   deletion whenever you commit next. Full story: `oraculum.engine-e-adoption.2026-07-11.md`.
2. **AI-keyboard claviature grammar LOCKED** (majkee gavel, @Janus REVISE folded). Your board
   row "claviature" is HALF-consumed — the GLOBAL claviature (all zsh subfolders) remains
   open, and a remote design session for it is now in flight on the Oraculum line (majkee
   gavels from mobile, expected Sunday). **Do not schedule the claviature design session
   separately** — it would fork. Details: `oraculum.claviature-ai-keyboard-locked.2026-07-11.md`.
3. **Concurrent, not yours:** Atlas/Trajectory are mid-production in machine-layer
   `keyboard.zsh` (style repair + a freya.devenv sync/deploy key). Machine layer, 0009-gated,
   majkee-supervised — mentioned only so cross-reports don't read as drift.

## Operator availability
majkee is off-computer ~1 day (returns Sunday after-lunch); reachable via the Oraculum remote
session on mobile. Your gavel bundle stays blocked-on-majkee per Force 4 — his gavels for your
line resume Sunday unless he gavels from the phone sooner.

— Oraculum, 2026-07-11
