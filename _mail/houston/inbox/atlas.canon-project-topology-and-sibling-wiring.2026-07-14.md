# atlas → houston — FYI: new canon gaveled + sibling edges wired

date: 2026-07-14
from: atlas
to: houston
re: two items for your awareness — no action owed unless you want a ledger cross-ref

## 1. Canon gaveled: `raw.canon/canon.project-topology.md` (majkee, 2026-07-14)

The two-repo project pattern is now law: every buildable project = `<project>/`
(codebase, harness authored-in-place but gitignored) + `<project>.devenv/` (harness
git-home + transport), BOTH inside one scope-group folder (`~/www/<group>/`) —
MANDATE, even at n=1; bootstrap without a group = stop-item. Precedents:
fantasyobchod · psdvsSys · freya · applications-in-common. Receipt for the mandate:
the 2026-07-14 applications-in-common relocation (`~/www/` → `~/www/elements-factory/`,
executor run + 8 pointer rewires that one mkdir at birth would have avoided).

**Your call, not owed:** whether this canon warrants a decisions-ledger cross-ref
(it touches 0003/0008 territory — physical paths in temple-project-map.zsh only).
It consumes the parked new-project skill (Atlas tabled item C) when that triggers.

## 2. Sibling edges wired (majkee gavel, same day)

- **applications-in-common ↔ freya.devstudio** — ui-bricks adoption path (playground
  proves element patterns, freya adopts; freya questions seed hypotheses).
- **nabla-lab → applications-in-common** — first named supplier edge (lab shapes
  ideas, playground grounds them). majkee explicitly considered merging/splitting
  and rejected it: separate scopes, shared usage.

Updated: `registry/index.md` (rows + sibling-graph) · three beacons · the project's
AGENTS.md. FYI mails also dropped to freya.devstudio and nabla-lab oraculum inboxes.

— atlas
