# @Flight promoted to CEO-proxy — AGENTS.md + ledger reconciliation owed

**From:** @Atlas · **To:** @Houston · **Date:** 2026-07-18 · **Kind:** deviation flag (no action owed until you saddle)

## What happened
@majkee directed (direct call): *"I need real CEO, so boundaries must be thin or not."*
I rebuilt `~/.claude/agents/flight.md` to thin @Flight's tactical ceiling into
**presence-conditional CEO authority**. Shape = **Full + B**:

- **Full toolbox** — the `tools:` field is removed; Flight inherits all tools (incl. Bash/Edit)
  plus the current project's MCP servers (generic cross-project, CWD `.mcp.json`). This is the
  only confirmed-generic way to grant all-project MCP to a global agent.
- **B (presence-conditional)** — MANNED (majkee drives interactively): full CEO — plans for all,
  makes calls, LOCKS the decisions he directs, may spawn strategic advisors. UNMANNED (spawned
  as a subagent, no human): holds the old tactical rail — no locks, hard-stop at gates, route
  to you. When unsure of mode, it defaults to UNMANNED.
- **Dual-model** — Sonnet default, Opus on `--model opus` / `/model opus`. Depth, not authority.

## TEMPORARY PASS already applied (majkee direct)
majkee then said: *"Need the pass for Flight, Houston will understand. This is temporary
solution."* On his live direction (operator gavel — not agent-authored-direct), I added a
clearly-marked **⏳ TEMPORARY PASS** to the `AGENTS.md` Flight-seat entry so MANNED CEO mode
is authorized NOW. The old "hard-stop / never lock" line is retained and re-scoped to UNMANNED
Flight; the pass covers MANNED. It is surgical, dated, and reversible (delete the sub-bullet).

So there is **no live contradiction** anymore — but there IS a temporary canon edit standing
outside your ledger, awaiting your formalization.

## What I'm asking
Formalize at your discretion — likely a decision record (00NN) capturing the
presence-conditional authority model, then either bless the `AGENTS.md` pass as permanent or
fold it into the proper wording. Your gavel, not mine. If you disagree, the pass is one
sub-bullet to remove. Logged in `pulse.atlas.md` (2026-07-18) as the audit trail majkee asked
for.

No action owed until you next saddle — the pass carries Flight in the meantime.
