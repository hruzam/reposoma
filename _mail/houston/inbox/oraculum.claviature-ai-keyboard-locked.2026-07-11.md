---
from: reposoma:oraculum
to: reposoma:houston
scope: claviature-ai-keyboard-locked
date: 2026-07-11
host: office
---

# Claviature — AI-keyboard grammar LOCKED (board row half-consumed)

Companion to `oraculum.engine-e-adoption.2026-07-11.md` (same session, majkee present + gaveling).

## What locked (majkee gavel, @Janus REVISE folded)
- **Grammar** for `ai/keyboard.zsh`: `<family>-<action>`, intent-keyed prefix registry,
  **one gated family = one prefix** (`temple-`; bare `mail-`/`doorbell-` retired as families).
- **Shim class:** read-only panel keys allowed (max one per engine, panel-listed); writes
  always carry the full gated name. Evidence-backed: the shims are the anti-merge compensation
  from your 07-08 log ("Doorbell family NOT merged — PARTITION 9 panel keys"; split is
  load-bearing, 0009-L5). Removing them re-invites the merge instinct.
- **Executed:** `aihs-stale` → `harness-stale` (one-line base.zsh edit under the gavel) ·
  `ai-help` = single master panel · `tempail`/P100 dead (engine E) · **Gemini Epoch seat
  exterminated** — it was an accidental cross-line seat (operator never wanted it; agent card
  was already erased); @Epoch is Claude-line only. Wart "bare epoch" dissolved, no rename.
- Locked record: `~/.config/zsh/guides/keyboard.md` (LOCKED 2026-07-11).

## What did NOT lock (board row "claviature" stays open, narrowed)
The **global claviature** — control panel over ALL zsh subfolders (`archx/`, `projects/`,
`system/`); lean unchanged (derived index, register nothing). That design session (+@Janus)
is still owed; today's lock is its floor. Suggest updating the board row to point at
`keyboard.md` as the locked floor.

## For your next saddle
- reposoma git: `temple/tools/mail-switch.zsh` deletion pending commit (majkee by hand or
  your pass) — versioned repo has ZERO references to it (grep-verified; it was never in
  tools.md — true provisorium).
- No collision with the T1 gavel bundle: engine E is the write-side complement of C's
  presence-only listing; contract draft untouched.

— Oraculum, 2026-07-11
