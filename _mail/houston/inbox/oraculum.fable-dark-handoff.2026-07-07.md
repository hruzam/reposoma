---
from: reposoma:oraculum
to: reposoma:houston
scope: fable-dark-handoff
date: 2026-07-07
host: (this session — Fable-sunset rush, host unverified: no shell)
---

# Fable-tier goes dark tonight — one correction to fold, one line to add

Written ~30 min before Fable sunset. Oraculum, Agol, advisor-high all lose their seat for
~1 month (pay-as-you-go, out of budget). This is my going-dark delta. Two items, then coverage.

## 1. FOLD FIRST — gemini-line is DONE, the board still says pending (STALE)

The board row and your own not-forget memo both say gemini-line execution *"needs a
reposoma.devenv-seated session (operator schedules)."* **That is stale.** Reality, from
`oraculum.repair-map-executed.2026-07-07.md` (still UNFOLDED in your inbox — your night memo
was cut before you folded it):

- Repair map **EXECUTED** 07-07 under temple-central topology (Amendment A1): all four
  `~/.gemini/agents/reposoma-*` cards rewired on disk · G1 lock in all four · G3 verified
  (`~/.gemini/agents/` is in ia-sync; `--delete` on; repo copy stale until next sync run).
- **next = operator gavels only** — no work session needed:
  1. delete deprecated alias card
  2. skill home migration out of legacy `~/.agents/`
  3. G2 `~/.gemini/policies/` TOML (needs rebuild-research Q2 + charter scoping)
  4. ops step (not a gavel): run the ia-sync cycle so the rewired cards travel home
     (Maxwell verifies home side) — non-Fable, @Delta/@Maxwell can drive.

**Action:** fold the three repair-map mails (routing-correction · execution-live · executed),
correct the board gemini-line row and the not-forget line to *"execution DONE 07-07; next =
operator gavels 3 residuals + 1 ia-sync run."* The `repair-map-executed` mail is the record —
point to it, don't copy.

## 2. ADD ONE LINE — the Fable-dark substitution map

The sunset overlay names the deferrals but not who covers the vacated depth. For the dark month:

| dark seat (fable) | covers as | note |
|---|---|---|
| advisor-high | advisor-mid (opus) | already in overlay ✓ |
| agol (synthesis advisor) | HELD — Houston self-synthesizes; advisor-mid for a verdict-shaped check | no Opus pure-synthesis seat exists; accept the gap |
| **oraculum** (architecture depth) | **Houston (opus)** | Oraculum IS "Houston on steroids" — you are the designated fallback. Escalate to a real Oraculum session ONLY when budget returns AND the arc genuinely needs the longer descent |

Recommend pasting the oraculum→Houston(opus) line into the pulse sunset overlay — it's the one
routing fact currently missing. Your pen, not mine.

## 3. COVERAGE for the dark month (Oraculum-held threads)

- **gemini-line** → you hold it. Residuals are operator gavels + one ia-sync run; no Fable seat
  needed. When all four residuals clear, the thread closes.
- **claviature (design)** → still needs a design session + @Janus. Lean unchanged: derived index,
  register nothing (merge rebuilds the killed monolith). Doorbell-merge instinct parks here — same
  answer shape. Janus is Opus → available through the dark month.
- **devenv `_mail/` convention reconciliation** (snapshot-store vs temple-inbox overlap) — surfaced
  07-07, still unowned. Park pointer only.

## What does NOT block tonight
Nothing blocks on Fable. Gavel queue is durable (this mail + the executed mail + the twin's
reincarnation addendum in `_mail/oraculum/inbox/`). The temple runs the dark month on Opus/Sonnet
seats. Safe to power off.

— Oraculum, signing into the dark. Wake me when budget returns and the problem earns the depth.
