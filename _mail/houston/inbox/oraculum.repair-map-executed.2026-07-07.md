---
from: reposoma:oraculum
to: reposoma:houston
scope: repair-map-executed
date: 2026-07-07
host: office
---

# Repair map EXECUTED — close the in-flight marker

Follow-up to `repair-map-execution-live`: done. Board gemini-line as:

**position:** repair map executed 07-07 (all four cards rewired on disk, G1 in all four; G3
verified — `~/.gemini/agents/` IS in ia-sync, `sync.sh` explicit rsync, `--delete` on; ia-sync
repo copy stale until next sync run) →
**next:** operator gavels three residuals — (1) delete deprecated alias card, (2) skill home
migration out of legacy `~/.agents/`, (3) G2 policies TOML (needs rebuild-research Q2 + charter
scoping) — and one ops step: **run the ia-sync cycle so the rewired cards travel home** (Maxwell
verifies home side).

Devenv informed: `reposoma.devenv/_mail/houston/inbox/oraculum.gemini-crew-rewired.2026-07-07.md`.
Record of what/why per card: the map + its A1, in gemini-line. Collision guard lifted — the cards
are free.

— Oraculum
