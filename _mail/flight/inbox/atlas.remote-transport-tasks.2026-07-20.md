# atlas → flight — remote-transport task package (off-Atlas-posture handoff)
2026-07-20 · from the remote-protocol close-out session (majkee-directed)

**Context:** remote protocol graduated — global `~/.claude/CLAUDE.md` block retired; a
`SessionStart` hook now does auto-pickup (law + OFF switch: `~/.remote/README.md`). Skills
`/remote-read` `/remote-write` unchanged. These tasks fell out of that session and are
machine/infra layer — not Atlas posture. majkee: *"wire him all tasks which you think is not
good from your posture here."*

**Your line already owns the transport:** H8 netOrchestrating
(`~/www/elements-factory/applications-in-common/experiments/netOrchestrating/`) — do not let
any of these duplicate it.

1. **Tailscale ACL symmetry** (majkee point 4) — office→home access must equal home→office.
   Infra config, operator-present work. Prereq for H8 both-ways AND for any eagle-on-home trigger.
2. **ZSH "call in arms"** (majkee point 3) — tailscale-hop aliases for home→office session reach.
   Machine layer → @Trajectory/@Delta. LAW: `guide-for-builder.md` read BEFORE writing;
   keyboard.zsh = aliases only, engines in `ai/`; fresh-read from disk (stale-memory discipline
   stands, see pulse.atlas 2026-07-11 rounds).
3. **Eagle-on-home trigger** (majkee point 4) — decide the shape: plain
   `ssh <home> → claude --agent eagle -p "…"` recipe, or ride the H8 relay once its load-bearing
   test passes. Your call; the recipe lands in `~/.remote/memory.md` either way.
4. **Standing:** H8 load-bearing test (`pin_bus.push()` via remote SSH) is operator-run, still
   pending — surface to majkee when he sits at the right machine.

`pulse.flight.md` now exists (scaffold, yours, single-writer). This package is deliberately in
mail, not your pulse — you log it there yourself when you take it.

— Atlas
