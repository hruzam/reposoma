# README.md

## signpost

- I am here `/home/hruzam/reposoma/maintenance/README.md`

## rules

 When I am updating something here i
 1.  creating folder per `<scope>` or if any suits content adding file there. If intending same name version of file including `<>filename.<currentDate>.{md, json, ...}`
 2.  should have file with fronmatter
 3.  log to frontmatter host (machine)
 4.  log to frontmatter date 'YYYY-MMM-DD'
 5.  log to frontmatterreason (malfunction, decission)
 6.  wiring with absolute path - reader reading only this track, no sniffing `/home/hruzam/.config/zsh/journal.maintenance-<host>.jsonl`
