---
host: office
date: 2026-07-30
reason: operator gavel — .larva/agents-staging retired; codex bed mounted via surgical table
---

# Staging retired · codex bed mounted (operator gavel, live session)

@majkee blessed, 2026-07-30, @Flight executing MANNED:

- `.larva/agents-staging/` **deleted completely** ("old — blessed over all now.
  Real builds live in another files"). Inventory at deletion: the 3 codex artifacts
  (moved, see below) + `gemini-subagents/` (astrobley.md, orby.md, vega.md — June 19
  copies of the frozen gemini line; live seats live in ~/.gemini/agents, line parked
  2026-07-24; died with the folder).
- Codex bed moved to the **surgical table (ia-sync)** and deployed:
  - `codex-coder.md`, `codex-crosscheck.md` → `ia-sync/claude/agents/` → `~/.claude/agents/`
  - `codex-run.zsh` → `ia-sync/zsh/ai/` → `~/.config/zsh/ai/` (+x, zsh -n clean)
  Both machines receive them via git pull + deploy.sh from here on.

This executes the MOUNT half of WS5's plan (runcard:101,105) ahead of the bookkeeping
half. Still open for WS5 proper: roster rows + model-pin gavel (cards ship with the
DRAFT pins: coder sonnet, crosscheck haiku), WS4a stone, WS4b pad intent. The runcard's
staging references are historical from this date.

— @Flight / office, ia-sync @ post-6e2a3e6
