# res/ — reader chapters (served by `/guide meeting <chapter>`)

Empty until a vacuole scenario closes. Each chapter carries `chapter-of: meeting` in its
frontmatter and is a **proven, graduated finding** — NOT an open experiment (those live
in `src/`). Add a chapter only when a `src/` scenario has *repeatedly* proven its finding.
