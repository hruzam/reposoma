# Project intake — the scope I need to draw your per-project bones

`tier: raw.guides · fill one per project · the architect turns this into the team skeleton`
`produces: the seat list · the vendor-team allocation · the PROJECT.yaml contract ·`
`the durable files (plan/flag/pulse/decisions) · the gate · the named deferral thresholds.`
`rule: answer short. a blank means "not promised yet", not a bug. field work refines it.`

> You allocate; I draw bones. One head says "this is mine, this is yours"; the teams are
> full-competence within their lane; canon flows one direction through the gate; you see
> process feedback every few steps. These bones are a start, not a cage.

## A · Identity
- name + one-line purpose:
- which is this — "real reposoma", or a new one:

## B · Nature of the work — this draws the domain seats (I grant none you don't earn)
- what dominates: authoring/research · building/coding · operating live systems · (mix?):
- a narrow correctness/performance-critical heart? → deep core specialist:
- a body of method/math the team must get right? → domain researcher:
- deterministic tools, agentive tools, or both? → the builder split:
- a human-facing surface where the system explains itself? → surface seat:

## C · The contract — becomes PROJECT.yaml (capability terms, not stack terms)
- stack (lang/framework — informational):
- commands: test · lint · build · run:
- docs entry points: architecture · decisions · conventions:
- hard constraints: comment language · forbidden tools · security rails:

## D · Teams & the one head — the allocation
- vendor-teams on this project: Claude basement (always) · Gemini? · Cursor/Codex?:
- the one head (allocates work, holds canon authority): you / a project-Houston:
- the split — what each team OWNS ("mine / yours"). Canon stays at the basement gate:

## E · Volume & cadence — this sets the thresholds I name out loud
- how much work, how fast (rough):
- "every few steps" feedback = how many steps / how often:
- the threshold that would earn a msg-bus / orchestrator / sub-team (or leave to me):

## F · Live state — Model-C sizing (the one untuned string)
- does the work need runtime truth (schema · last error · logs)? how fat:

## G · Out of scope — what this project does NOT promise:
