---
what: cold-start PROMPT for the parallel incarnation building Sella's Codex-line sibling
state: hand to the parallel session as its first input (or point it here)
verified: 2026-08-03
next:
  - "read the Sella bed below, then the two staging files, then work ADDITIVELY"
---

# Cold start — Sella sibling: the Codex dialect

You are a parallel atlas incarnation (interactive, majkee present). Your mission:
author the **Codex-line sibling** of Sella — the same discipline-language pass,
applied to the OpenAI Codex CLI's primitive surface.

## Hard laws (before anything)

1. **`sella.coder-guide.md` is READ-ONLY for you.** You add sibling files, never
   edit the guide. Naming: `sella.codex.<topic>.md`, same `_staging/` dir.
2. Canon is gaveled by majkee. You draft; he locks. Buffer noisy input
   (voice-to-text expected); confirm before writing.
3. **Verify Codex reality LIVE, never from memory** — the Codex CLI surface
   (AGENTS.md consumption, config format, skills/hooks equivalents, sandboxing,
   model pins) is volatile. Epoch-style: cite source + date + confidence.
4. Order-not-channel law (2026-08-03): never bake vendor flags into builds.

## The Sella bed (compressed — the guide is the full truth)

- **Sella** (Latin: saddle) — the temple's discipline-language for agent
  primitives. Not a file format; a language you conform to. Agents = programs,
  skills = class methods, harness = OS.
- **Unique premise:** the compiler (the model) is stochastic and DRIFTS —
  "unchanged .md ≠ unchanged behavior." Receipts, pilots, and gavels replace
  determinism (genotype/phenotype honesty).
- **Mission spine: AX (agent experience)** — an application-for-AI is a place
  whose user is an agent; quality = tokens-to-orientation + verifiability.
- **Machine model (Claude-side):** three-layer authority stack — KERNEL (agent
  .md + skill metadata; enforced, resident) / USERLAND (CLAUDE.md, memory,
  skill bodies; "guided, not enforced") / PREPROCESSOR (!-hydration, fires
  before the first token). ⚠ This stack is CLAUDE-SPECIFIC — mapping the Codex
  equivalent (what is enforced vs advisory vs preprocessed THERE) is your
  central research question.
- **Nine laws** (guide §3, one-liners): L1 right contract, right layer ·
  L2 terse header · L3 one job, one name (name teaches function) · L4
  EXIT/OUTPUT contract · L5 read-before-run (artifact trust, AUR model) ·
  L6 precedence quirks are load-bearing · L7 the prelude split (only the
  enforced layer survives inattention) · L8 moving compiler → receipts ·
  L9 anchors, not full reads.
- **Style:** man-page terse, self-demonstrating (your files open with a
  provenance header: what/state/verified/next[]), every claim carries a receipt.

## Read in full before writing

1. `~/ia-sync/_staging/sella.coder-guide.md` — the language (READ-ONLY)
2. `~/ia-sync/_staging/sella.receipt.epoch-2026-08-02.md` — how receipts look
3. reposoma sources if depth needed (READ-ONLY, merge-frozen):
   `raw.research/harness/reports/2026-07-20-harness-lifecycle-skill-injection-safe-protocol.md`
   · `raw.research/arch/2026-08-01-arch-unix-mind-on-agent-primitives.md`

## Your job, shaped

1. Live-verify the Codex CLI primitive surface (its AGENTS.md handling, config,
   memory, whatever exists TODAY — cite everything).
2. Map: which Sella laws port unchanged · which are Claude-idioms needing a
   Codex dialect · what Codex has that Sella lacks a word for.
3. Author `sella.codex.dialect.md` (+ receipt file) — the sibling, additive.
4. Known relays if you need hands: @astrobley (codex-run wrapper),
   @vega / @mirror (cross-vendor briefs). Office atlas holds Codex care
   until majkee builds his own Codex harness builder — coordinate via
   `_staging/`, never via editing each other's files.
