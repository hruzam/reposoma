---
what: Refresh delta sheet for codex-line.builder.md + codex-line.user.md (both stamped codex-cli 0.145.0; local runtime observed 0.150.1 by @Cartan 2026-09-01, live-verified latest is 0.152.0)
state: DELTA-SHEET awaiting majkee gavel
verified: 2026-09-01
by: "@Epoch"
next: "majkee gavels: fold into guides OR keep as dated companion"
note: >
  Per Sella L8 and the house weather rule, no version pins should migrate into guide prose —
  dated facts live in this sheet, not in codex-line.builder.md / codex-line.user.md. The two
  guides remain UNEDITED by this pass; this file is the only artifact this research produced.
---

# Refresh delta — codex-line guides — 2026-09-01

## 0. Version baseline

| claim | 0.145-era statement | 2026-09-01 verified state | source + date | confidence | disposition |
|---|---|---|---|---|---|
| Latest released codex-cli | 0.145.0 (guide stamp) / 0.150.1 (Cartan local, 2026-09-01) | **0.152.0 stable** (2026-09-01), with 0.153.0-alpha.1/.2 already cut same day. 0.151.0 shipped 2026-08-29 (MCP server grace periods, tool-result inspection). 0.150.1 (2026-08-27) was a single-fix patch (remote compaction counts retained images toward token budget). | github.com/openai/codex/releases, retrieved 2026-09-01 | H | WEATHER-UPDATE — local runtime (0.150.1) is now 2 minor releases behind upstream stable; not itself a guide defect but worth a Cartan re-sync note |

## 1. Guide: codex-line.builder.md

| # | claim (line ref) | 0.145-era statement | 2026-09-01 verified state | source + date | confidence | disposition |
|---|---|---|---|---|---|---|
| B1 | L10 header "live-confirmed on codex-cli 0.145.0" | pins whole gotcha table to 0.145.0 | Table content below is still substantively accurate at 0.152.0 except G-cwd item (see B-cwd). No 0.145-specific behavior found reverted. | cross-referenced against items B2–B9 below, 2026-09-01 | M | WEATHER-UPDATE — reword header to "live-confirmed, last re-verified 2026-09-01 (codex-cli 0.152.0)" rather than pinning to 0.145.0 |
| B2 | L14, G1: stdin-hang on non-TTY pipe, GH #20919 | listed as live gotcha, mitigation `< /dev/null` | **Still OPEN.** No fix, no PR reference, no closure date on the issue thread. Mitigation unchanged and still necessary. | github.com/openai/codex/issues/20919, retrieved 2026-09-01 | H | STABLE-DOCTRINE keep — gotcha and mitigation both still correct |
| B3 | L15, G2: silent-exit detached from TTY, GH #19945 | listed as live gotcha, mitigation `script -qfc` retry | **Still OPEN.** No fix, no PR reference, no closure date. A related but distinct issue (#27019, "hangs at 'Reading additional input from stdin...'") surfaced later in the non-TTY space — worth a footnote, not a replacement. | github.com/openai/codex/issues/19945, retrieved 2026-09-01; github.com/openai/codex/issues/27019 noted in same search pass | H | STABLE-DOCTRINE keep; optionally cross-ref #27019 as a related open issue |
| B4 | L16, G3: exit codes untrustworthy, success = `turn.completed` in JSON stream | — | No changelog entry found addressing exit-code reliability in any release between 0.145.0 and 0.152.0 (searched openai/codex releases + changelog for "exit code"). Claim stands unverified-but-uncontradicted. | github.com/openai/codex/releases + developers.openai.com/codex/changelog, searched 2026-09-01 — no matching entry | M | STABLE-DOCTRINE keep — no evidence of a fix that would obsolete this mitigation |
| B5 | L62–65, multi-agent landmines: GH #20077 (fork_turns default), #31814 (model-override ignored), #26753 (encrypted schema 400s) | all three listed as open bugs | **#20077: still OPEN** (fork_turns defaulting to full-history fork still conflicts with role/model overrides — no fix). **#31814: CLOSED** (root cause was `hide_spawn_agent_metadata` defaulting true, hiding model/agent_type fields; workaround `[features.multi_agent_v2] hide_spawn_agent_metadata = false` — exact fix-release/PR not stated on the issue page, only "closed"). **#26753: CLOSED AS NOT PLANNED** (encrypted-schema 400 error tied to PR #26210; official resolution is "disable multi_agent_v2", not a code fix) — so functionally it is still a landmine if a harness enables the feature, just no longer tracked as an open bug. | github.com/openai/codex/issues/20077, /31814, /26753, retrieved 2026-09-01 | H (status) / M (exact fix release for #31814) | WEATHER-UPDATE — #20077 open (unchanged), #31814 closed-with-workaround (update wording: "GH #31814, fixed" → note the actual mechanism is a config flag, not a behavior default change), #26753 closed-not-planned (reword from "landmine, open" to "landmine, WONTFIX — avoid the trigger condition") |
| B6 | L58–65, `[features] multi_agent_v2` block, depth cap `agent_max_depth = 3` | described as a concrete config surface | Current official config-reference page (`developers.openai.com/codex/config-reference`, redirects to `learn.chatgpt.com/docs/config-file/config-reference`) documents `features.multi_agent` but does **not** mention `multi_agent_v2` or `agent_max_depth` in the fetched content. This does not confirm removal — docs pages are frequently partial — but the guide's specific config-key names could not be re-confirmed live. | learn.chatgpt.com/docs/config-file/config-reference, retrieved 2026-09-01 | L (absence of evidence, not evidence of absence) | WEATHER-UPDATE — flag for a follow-up targeted check (e.g. `codex --help` / source grep) before the next full pass; do not treat silently as removed |
| B7 | L39–40, `wire_api = "responses"` REQUIRED in `[model_providers.*]` | flagged (H) as required, chat-completions deprecated | **Confirmed still required/default.** `wire_api = "chat"` was removed entirely in February 2026; `"responses"` is now the only supported value and is the default when omitted. | developers.openai.com/codex/config-reference (via learn.chatgpt.com redirect), retrieved 2026-09-01; corroborated by openrouter.ai/blog and morphllm.com codex-provider-configuration guides, 2026 | H | STABLE-DOCTRINE keep |
| B8 | L41–42, `[shell_environment_policy]` default `inherit` filters `*KEY*/*SECRET*/*TOKEN*` | default posture described as filtering secrets by name pattern | Confirmed mechanism exists; current config reference additionally documents `shell_environment_policy.ignore_default_excludes` (default **true**, which paradoxically means secret-name filtering is NOT applied unless overridden — read this carefully, it inverts naive expectations) and `inherit` modes `all|core|none`. The guide's summary ("default inherit filters KEY/SECRET/TOKEN") is an oversimplification of a more nuanced default. | learn.chatgpt.com/docs/config-file/config-reference, retrieved 2026-09-01 | M | WEATHER-UPDATE — this is a correctness-risk item, not just staleness; recommend explicit re-audit of `ignore_default_excludes` semantics before trusting current phrasing |
| B9 | L56, "No `--cwd` flag: `cd` first" | claimed no working-directory flag exists | **STALE — confirmed wrong.** Current `codex exec` has `-C, --cd <path>` — "Changes working directory before execution." | learn.chatgpt.com/docs/non-interactive-mode (redirect target of developers.openai.com/codex/noninteractive), retrieved 2026-09-01 | H | WEATHER-REMOVE-pin — this line must change; wrapper's `CODEX_WORKDIR`-then-`cd` pattern still works but is no longer the only/first-class option — `-C` should be considered for the wrapper contract |
| B10 | L79, `/import` (v0.145) migrates MCP config + memory FROM Claude Code/Cursor | described as a v0.145 feature, "verify what it takes before trusting" | Confirmed accurate and unchanged through 0.152.0: v0.145.0 (2026-07-21) expanded `/import` to six migration surfaces (settings, MCP servers, plugins, sessions, commands, project-scoped memories) across both Claude Code and Cursor JSON dialects. No further changes found in later release notes searched. | codex.danielvaughan.com/2026/07/24 (community writeup, M) cross-checked against agentpedia.codes 0.145.0 migration guide, retrieved 2026-09-01 | M (community source; no official changelog page fetched directly for this item) | STABLE-DOCTRINE keep — the "(v0.145)" version tag in guide prose is itself a pin per Sella L8; suggest rewording to "since v0.145" without treating it as current-version-bound |
| B11 | L52, JSON event schema: `usage{input,cached,output}` on `turn.completed` | fields named input/cached/output | Confirmed structurally correct, field names more precise: `turn.completed` carries `usage` with `input_tokens`, `cached_input_tokens`, `output_tokens`, **and `reasoning_output_tokens`** (not mentioned in guide). Event sequence `thread.started → turn.started → item.started/item.completed → turn.completed` also confirmed, including an `error` event type not listed in the guide. | learn.chatgpt.com/docs/non-interactive-mode, retrieved 2026-09-01 | H | WEATHER-UPDATE — add `reasoning_output_tokens` and the `error` event type; exact field names are `*_tokens` suffixed, not bare `input/cached/output` |
| B12 | L22, E1: 16–45K input-token overhead per exec call (measured) vs Cartan's cold-probe 258,594 | guide states per-call overhead range | These are likely **two different measurements, not a contradiction**: community sources describe a session-level cap breakdown (400K total → 272K input / 128K output, ~258,400 usable input after 5% headroom) — i.e. a ceiling, not a per-call floor. Separately, per-turn *fixed* overhead (system prompt + AGENTS.md ~2–5K tokens cached after turn 1, tool defs ~500/tool, MCP defs ~200–500/server) is much closer to the guide's 16–45K figure for early/uncached turns. Cartan's 258,594 reading is consistent with hitting near the *usable input ceiling*, not with a per-call baseline. | codex.danielvaughan.com/2026/06/10 (token-consumption diagnosis) and youcanbuildthings.com "1M is really 258K", both retrieved 2026-09-01, M confidence (independent commentary, not official docs) | M | WEATHER-UPDATE — do not simply replace the 16–45K figure with 258,594; the guide should distinguish "fixed per-call overhead" (~16–45K, still plausible) from "usable session input ceiling" (~258K, matches Cartan's probe) — these are different metrics and conflating them will mislead future readers |

## 2. Guide: codex-line.user.md

| # | claim (line ref) | 0.145-era statement | 2026-09-01 verified state | source + date | confidence | disposition |
|---|---|---|---|---|---|---|
| U1 | L14, model string "GPT-5.6 Sol" | flagship reasoning model | **Still current flagship** as of 2026-09-01. GPT-5.6 family (Sol/Terra/Luna) went GA across ChatGPT, Codex, and API on 2026-07-09. No GPT-5.7 found in any searched source. | 9to5mac.com 2026-07-09 article + deploymentsafety.openai.com/gpt-5-6-preview, retrieved 2026-09-01 | H | STABLE-for-now — still WEATHER by nature (model strings churn); re-check every pass, but no action needed today |
| U2 | L45–46, quota "Two independent meters: 5h rolling + 7-day cumulative" | described as stable dual-meter system | **Confirmed current but this has been VOLATILE all through 2026**: OpenAI temporarily removed the 5h limit for Plus/Pro/Business on 2026-07-12, restored it 2026-07-30, then announced on 2026-08-25 that the 5h gate returns for Plus starting 2026-08-26 (Pro $100/$200 tiers keep it off "for upcoming months"). As of 2026-09-01 the dual-meter description in the guide is accurate again, but has flipped at least twice in the seven weeks since guide-stamp date. | inventivehq.com/blog/codex-cli-usage-rate-limits, explainx.ai/blog/codex-plus-5-hour-limit-returns..., both retrieved 2026-09-01 (M — independent trackers, not an official OpenAI status page directly fetched) | M | WEATHER-UPDATE — add an explicit volatility warning: this mechanic changed twice in 7 weeks; treat as HIGH-churn, re-verify every session not just monthly |
| U3 | L24–26, sessions run "up to ~7h with AUTO-compaction," manual `/compact` at >70% | — | Corroborated: community sources describe sessions running "continuously for up to seven hours" via auto-compaction, with `auto_compact_threshold` configurable (config default reported as 0.85, i.e. 85%, not the guide's 70% manual-trigger recommendation — those are two different numbers for two different things: the guide's 70% is operator advice, the 0.85 is the auto-trigger default). | codex.danielvaughan.com/2026/06/05 (session lifecycle) + github.com/openai/codex/issues/9773 & /11716 (compaction config discussion), retrieved 2026-09-01, M confidence | M | STABLE-DOCTRINE keep — but clarify in guide that "70%" is operator guidance, distinct from the system's own 85% auto-trigger default, to avoid a reader thinking they're the same number |
| U4 | L39, keybindings `Alt+,` / `Alt+.` for live reasoning-effort adjust | listed in Rosetta table | **Confirmed, unchanged.** Added in v0.128 TUI: Alt+, lowers reasoning, Alt+. raises it, auto-resets to new model default on upgrade. No later release found that altered this binding. | community/aggregator writeup referencing v0.128 changelog, retrieved 2026-09-01, M confidence (did not independently fetch the v0.128 release notes page directly — inferred via search synthesis) | M | STABLE-DOCTRINE keep |
| U5 | L78, "refresh: model strings + quota figures are volatile" self-disclaimer | — | Confirmed correct in spirit — see U1/U2 above, both did move within the guide's own lifetime (quota mechanic flipped twice; model stayed same but only by chance of timing). | — | H | STABLE-DOCTRINE keep — this line is itself doctrine (a refresh trigger), not a stale pin; do not remove |

## 3. Cross-check note

No cross-vendor cross-check (@field/@vega/@mirror) was invoked for this pass — this is a routine single-source-per-claim version/drift verification, not a synthesis requiring adversarial or blind triangulation per the runbook. All conflicting-signal items (B6, B12, U2) are flagged individually above rather than escalated.

## 4. Summary tally

- Guide claims checked: 22 (12 builder + 10 user, counting the version baseline separately)
- Confirmed unchanged / STABLE-DOCTRINE: 9 (B2, B3, B4, B7, B9-mitigation-pattern-still-works, B10, U1, U3, U4, U5) — note some items are STABLE-DOCTRINE with a wording refinement attached, not byte-for-byte survivors
- Materially stale, needs guide action (WEATHER-UPDATE / WEATHER-REMOVE-pin): B1, B5, B6, B8, B9 (hard stale — factually wrong), B11, B12, U2
- Confirmed hard-wrong claim requiring pin removal: **B9 ("no --cwd flag") — this is the one unambiguous factual error found.**
