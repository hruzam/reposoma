+++
# TOML front matter — evidence / traceability. This file is a TEMPLATE, not a filled brief.
id            = "research-program-brief"
tier          = "raw.guides"
kind          = "brief-template"
purpose       = "Establish a lead role and a research program: charge · rules · goal+products · process · participants."
status        = "draft"
created       = "2026-08-10"
authored_by   = "Medusa"
form_source   = "worked exemplar, FORM ONLY — no sealed content extracted (charge/rules/products/process/participants skeleton)"
copy_rule     = "COPY this to raw.guides/briefs/<name>.brief.md and fill the COPY — never write into this template."
produces      = "a runnable, role-establishing research-program brief"
sealed_note   = "the §S sealed-program appendix is OPTIONAL — include only for masked programs; delete it for open briefs"
+++

# <PROGRAM NAME> — brief to <LEAD ROLE>

> `fill: COPY to raw.guides/briefs/<name>.brief.md and fill the copy.`
> `rule: answer short. a blank means "not decided yet", not a bug. loop-1 refines it.`
> `form: this skeleton is the reusable half of a research-program brief — charge → rules → products → process → participants, with an optional protocol spine and an optional sealed appendix.`

---

## CHARGE TO <LEAD ROLE>
> Direct address. This is the spine of the mandate; the operational protocol (if any) lives in §P below.

- **What you are looking at.** <the primary object of study, plainly — the scope-1 thing>:
- **The lens — who you learn from.** <which sources/mindset to prefer, defined by their opposite>:
- **The method — how the work proceeds.** <the core move: observe X → produce Y; blind vs. grounded>:
- **Why it matters / why it is run this way.** <the stakes; any inversion of the usual process>:

## RULES
> Reading + conduct discipline. Keep short and enforceable.

1. <what to read first, and ONLY that, in loop-1>:
2. <loop-1 obligation — confirm the frame / plan before going wider>:
3. <read-only-what-you-must; the rest is a partner's job>:
4. <gavel / escalation path — where decisions get filtered>:

## GOAL & PRODUCTS
> The destination and the numbered deliverables. Say what is HELD vs. SHIPPED.

- **Goal (one line):** <the outcome, not the activity>:
- **Product ledger:**
  - **P1 — <name>** · <what it is> · <held / shipped>:
  - **P2 — <name>** · <what it is> · <held / shipped>:
  - **P3 — <name>** · <what it is> · <held / shipped>:
  - **P4 — <name>** · <what it is> · <held / shipped>:
- **Co-emergence / ordering note:** <which products depend on which; what may run parallel>:

## PROCESS
> Loop-1 (confirm) → loop-2+ (do). Sequencing is the lead's competence.

- **loop-1:** <frame confirmed · product ledger gaveled · open items closed> — only then read the `process` files.
- **loop-2+ (study what IS):** <how to fan out by source/scope; which sources carry the most weight>:
- **collect what else is known:** <adjacent corpora, prior research, agents to spawn for ingest>:
- **synthesize → products:** <hypotheses · solution shapes · reports by scope>:

## PARTICIPANTS
> Who authors, who processes, the thematic area, and any composites. Do NOT read their profiles unless needed.

- **Authors (source minds):** <name · pointer>:
- **Processed by (CLI / agent roles):** <lead · planner · pathfinder · researcher · coders>:
- **Thematic area(s):** <the mountains to climb, by scope/weight>:
- **Composites (if any):** <cross-vendor / paired-agent setups>:

## COORDINATOR SPACE
> Reserved for the planner/coordinator to add observations, preflight links, addenda. Keep the seam visible (attributed edits).

- <coordinator additions here>

---

## §P · PROTOCOL SPINE  *(optional — include the devices the program needs; delete the rest)*
> Reusable structural devices. Each is a mechanism, not decoration — keep only those that cut.

- **Product ledger (locked reading):** <re-state P1..Pn precisely if the goal section was loose>:
- **Phases → products table:**

  | Phase | Action | Produces |
  |---|---|---|
  | 1 · <name> | <action> | <product> |
  | 2 · <name> | <action> | <product> |
  | … | … | … |

- **Loop-1 exit gate:** <the exact conditions that end loop-1>:
- **Safety / quality audit gate:** <fresh-eyes check released at a defined point — logic · consistency · <domain check>>:
- **Success metric (two axes, so one number can't fool you):** <axis-1 floor (correctness) · axis-2 ceiling (value); the win = high/high>:
- **Question instrument / lens palette:** <a set of decorrelated framings aimed at the same object, each forcing a different KIND of fact — keep only lenses that extract>:

## §S · SEALED-PROGRAM APPENDIX  *(optional — masked programs ONLY; DELETE for open briefs)*
> Include only if the program must run blind/masked. Describe the MECHANISM of secrecy, never its contents.
> This template carries structure only — no keys, no safe paths, no codenames.

- **Clean-room boundary:** <temporal (session death = burn) + project boundary; what never crosses>:
- **The held cipher/key:** <what is held vs. shipped; where it lives; never promoted into a blind seat>:
- **Airlock / courier discipline:** <who moves artifacts; one-way vs. two-way; what read would break the wall>:
- **Trust topology:** <which seats hold the cipher, which stay blind>:

---

## Template discipline  *(delete this block in a real brief — it is guidance, not content)*
- This is a **brief-template**, not a brief: its filename has no `briefs/` path, and its front matter `kind = "brief-template"`. COPY it to `raw.guides/briefs/<name>.brief.md` to start a real brief.
- **Form only:** the skeleton is extracted from a worked exemplar's *structure*; it carries no program content by design.
- **Half A is mandatory** (charge · rules · goal+products · process · participants). **§P and §S are optional** — a simple open brief may use Half A alone.
- **Keep seams visible:** when a coordinator adds to a brief, attribute the addition (who · when) so the reasoning stays auditable.
