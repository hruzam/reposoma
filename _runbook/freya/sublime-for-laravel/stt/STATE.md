---
updated: <YYYY-MM-DD HH:MM zone>
writer: <seat · vendor>
host: <machine identity>
worktree: <absolute repo · branch · HEAD · clean | dirty paths>
gate: <verbatim from RUNBOOK.md>
checkpoint: <last verified fact · durable evidence or commit pointer>
in_flight: <none | action that may be partially applied + observable side effects>
recovery_probe: <exact read-only check + how to interpret its outcomes>
holds: <none | every current destructive, ownership, or cross-session constraint>
next: <exactly one action; absolute command or unambiguous human action>
expected: <observable result that proves next succeeded>
---

# OUTPUT

# Troubleshooting: LSP-Laravel
## Version
 - LSP: 2.13.0
 - Sublime Text: 4200
## Server Test Run
 - exit code: 0
 - output
```

```
## Server Configuration
 - command
```json
[
  "${server_path}"
]
```
 - shell command
```sh
"/home/hruzam/.cache/sublime-text/Package Storage/LSP-Laravel/v0.0.31/server-v0.0.31-x64-linux"
```
 - selector
```
embedding.php | text.html.blade
```
 - priority_selector
```
embedding.php | text.html.blade
```
 - init_options
```json
{}
```
 - settings
```json
{}
```
 - env
```json
{}
```

## Active view
 - File name
```
/home/hruzam/www/imago_cz/freya/resources/views/components/blog/menu.blade.php
```
 - Settings
```json
{
  "auto_complete_selector": "( meta.tag | meta.embedded.directive ) - comment",
  "lsp_active": true,
  "syntax": "Packages/Laravel Blade/Syntaxes/HTML (Blade).sublime-syntax"
}
```
 - base scope
```
text.html.blade
```

## Project / Workspace
 - folders
```json
[
  "/home/hruzam/www/imago_cz/freya"
]
```
 - is project: False

## LSP configuration

<not found>
## System PATH
 - /home/hruzam/.local/bin
 - /home/hruzam/.npm-global/bin
 - /home/hruzam/google-cloud-sdk/bin
 - /home/hruzam/bin
 - /home/hruzam/.local/bin
 - /usr/local/sbin
 - /usr/local/bin
 - /usr/bin
 - /usr/bin/site_perl
 - /usr/bin/vendor_perl
 - /usr/bin/core_perl