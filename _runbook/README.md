# Legacy RUNBOOK material — retired surface

This directory contains retained historical data only. It has no authoring or runtime role. Do
not create new work here and do not promote files from here into a live session.

Current homes:

- Doctrine: `../raw.guides/runbook/GUIDE.md`
- Inert copy source: `../raw.guides/runbook/ast/RUNBOOK.template.txt`
- Actual RUNBOOK and all process state: the owning project's declared session root

Classify or migrate retained files only through a separately scoped cleanup. Git history preserves
deleted material; this tombstone exists only to prevent the old directory from teaching the retired
architecture while data remains.
