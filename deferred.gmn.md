@AGENTS.md

# GEMINI.md — temple entry for the Gemini stack (boot gate)

> ⚠ **Boot the saddle. Do NOT scan the RAG.** reposoma / the temple is the **EXCEPTION — not a project.**
> Read **`AGENTS.md`** (the saddle) first → it routes you by seat (`temple/decisions/` → `registry/` →
> the `_mail/monkey` memo) → **then STOP.** Do not hunt for `PROJECT.yaml`; do not eager-read the folder.
> The files are continuity — trust them, read only what your seat needs.

> **Cost-gradient (Force 1 — match the model to the task's cognitive demand):**
> deep reasoning (research / architecture / synthesis) → `gemini-3.1-pro` ·
> coding & agentic loops → `gemini-3.5-flash` · high-volume / mechanical → `gemini-3.1-flash-lite`.
> Pro is *not* the default — switch up only when the task earns it (`/model`).

> Status bus: read `pulse.gemini.md` (yours) + `pulse.claude.md`. Deep proposals → `_mail/`.
