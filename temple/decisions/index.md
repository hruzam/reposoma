# Decisions — the standing index of locks (the flag role for this substrate)

`tier: temple · this is the meta-repo's flag: invariants + a one-line index of locks`
`note: the substrate has no volatile session/pulse — that is per-project. Locks live here.`
`rule: ADRs are append-only — superseded, never edited (doctrine §1b).`

| # | Lock | Status |
|---|------|--------|
| 0001 | Sovereign build: **A markdown = canon**, B = thin enforcement layer, C = thin live slice (sized by operator). `agentctl` is the gate (superset of compile-down). Three tiers `[S]/[GC]/[GW]`. Graduation rule. Force-3 knowledge-vs-integration boundary. Two supply chains, never one file. *(full record: `0001-sovereign-build.md`)* | LOCKED 2026-06-17 |
| 0002 | **Force 4 = the gate, not the direction.** Agent drafts → human authors-by-approving a diff; agent-authored-direct canon forbidden. Closed-loop automation conditional on a detection-rate test. *(full record: `0002-force-4-gate-not-direction.md`)* | LOCKED 2026-06-17 |
| 0003 | **Machine-provenance markers.** Host id = `echo $MACHINE_NAME` (per-machine from `~/.config/zsh/config.zsh`; office=`office`). Artifacts carry `host:`; per-repo `.host` via a one-liner (no framework). Machine-layer lighthouse `~/.config/zsh/AGENTS.md` (AGENTS.md, not CLAUDE.md); `substrate.` prefix parks legacy, nothing deleted; §4.7 — never bind its paths into a project surface. Concurrency guard is home-only. *(full record: `0003-machine-provenance.md`)* | LOCKED 2026-06-17 |
| 0004 | **Cross-project lighthouse registry** (plane 3). `registry/` — a thin POINTER beacon per project (`<project>.md`) + the sibling map (`index.md`) + the `README.md` spec. Rules: **point-never-copy** · one-direction (Force 4) · **project-relative anchors only — no `~/` paths; a machine layer is reached via its decision record (0003), §4.7 made structural** · `sibling-of`/`shares` fast-jump · `host:` provenance. Lock the convention, not the still-filling sibling-graph. *(full record: `0004-cross-project-registry.md`)* | LOCKED 2026-06-18 |
| 0005 | **Flat partnership + cooperation protocol.** Two peer architects (Houston/Claude + Vega), one gate, one direction, majkee gavels; challenge big moves only (opt-in per interaction), gavel = absolute tie-break, failover-adoption. **Method:** blind triangulation + "the stone" (impulse → receiver's `Y`/`Z`; triangulate when >1+1) — *the brief must not carry the briefer's answer*. **Runtime:** Tier-S vendor-neutral defs; supervised now, unattended waits for the headless test. **A1 2026-07-24:** peer seat re-bound **Gemini → Codex/GPT** (Vega = the chair, not the vendor — vendor swap, not doctrine change); §5 failover-adoption exercised (Gemini CLI sunset 06-18 — the hedge fired); gemini zsh layer loud-parked; **nabla-lab stone trial = the clean re-blind §Process demanded.** *(full record + amendment: `0005-flat-partnership.md` · program: `maintenance/codex-line/runcard.codex-onboarding.2026-07-24.md`)* | LOCKED 2026-06-19 · AMENDED 2026-07-24 (A1) |
| 0006 | **Model × effort assignment for CLI subagents.** Core finding: `Quality = f(tier, effort, task-class fit)` — effort misconfiguration risk > tier-selection risk; locked 15-seat matrix (2026-06-25 snapshot; `temple/roster.md` is the live home; Amendment A1 2026-07-07 corrects Agol→Fable·high, CapCom→Sonnet, @Hypatia→@Oraculum). *(full record: `0006-model-effort-assignment.md`)* | LOCKED 2026-06-25 · AMENDED 2026-07-07 |
| 0007 | **Tool-lifecycle / publishing doctrine.** Tools are the third registry consumer: Tier-S intent in `PROJECT.yaml tools:`, Tier-G-committed lockfile for executable facts (F4 — trust only from gated columns), cross-project discovery-only by construction; L7 emission at each graduation = part of done, owned by architect seat; seal-gate = two conditions before cross-project lock. *(full record: `0007-tool-lifecycle-publishing.md`)* | LOCKED 2026-06-25 |
| 0008 | **Propagation & transport doctrine.** Re-projection (not broadcast) under F4: flag-by-default (asymmetric risk — judgment-as-lag corrupts upward silently; lag-as-judgment costs a glance) gated by a canon-integrity precondition; governance axis protected (not behavioral); L9 declared protected-set in `PROJECT.yaml`; L10 precedence-as-exception (project-wins default; temple-wins only for mandatory class via notify-then-apply, never silent); two thin transports over one address book; executor topology parked to Stage 1. *(full record: `0008-propagation-transport.md`)* | LOCKED 2026-06-25 |
| 0009 | **Machine-layer `ai/` namespace + `base.zsh` signpost** (extends 0003). AI-bonded scripts live under `~/.config/zsh/ai/`; consumers source the idempotent, side-effect-free `base.zsh` (not individual scripts); L5 verify-it-fires = real trigger firing + artifact, never a hand-run — smoke-probe PAID green + deliberate-red 2026-07-02. *(full record: `0009-ai-namespace-base-signpost.md`)* | LOCKED 2026-06-27 |
| 0010 | **Cross-machine mail transport (direct-inform part 4).** File-native · receiver-owns · no-central-bus: per-writer append-only jsonl partitions (union = mailbox); state transitions = appended events; git = pc-to-pc transport; MariaDB = local read-projection only; failover distributor = notifier not carrier; doorbell = boot-time query. Extends 0008; R-a/R-b/R-c folded. *(full record: `0010-cross-machine-mail-transport.md`)* | LOCKED 2026-07-02 |
| 0012 | **Program-pulse — the octopus task-line contract.** One shared markdown buffer (line-per-task, closed-set status `issued→staged→review→done \| parked \| blocked`), **MD-not-JSON** by consumer analysis; `owns:`-disjointness = parallel-write race guard; pull gate (**1→head, N→Vara**); head owns add/pull/reconcile, Vara flips status in-batch; per-line `gate: assay \| human \| skip` (@Assay = Sonnet fresh-eyes circuit breaker); four non-negotiable headless circuit breakers. Promoted via delegated Houston-stamp. *(full record: `0012-program-pulse-contract.md` · spec: `raw.research/program-pulse.contract.claude.md`)* | LOCKED 2026-07-16 · AMENDED 2026-07-16 (A1) |

> **Gap note — 0011 reserved.** 0011 is held for @Oraculum's in-flight draft (writer-partition-vendor-surfaces, in `_mail/houston/inbox/`), not yet a locked record. 0012 skips ahead to avoid a ledger collision; fill 0011 when that draft gavels.

## Resolved opens (from 0001)
- **O3 — cleaned system map** — RESOLVED 2026-06-17: done in `system-map.md` (Atlas→Zenith added; self-clone and Houston→Delta kept; the "swarm" edge dropped).

## Resolved opens (propagation thread)
- **O3 — temple topology propagation to project-scoped agents** — RESOLVED 2026-06-25 by **decision 0008**. The propagation/transport *doctrine* is now locked (re-projection-not-broadcast · flag-by-default + canon-integrity precondition · declared protected-set · precedence-as-exception · two thin transports · promotion-threshold governance). The standard propagation prompt is written (`temple/tools/propagation-pass.prompt.draft.md`) and ran once on piql (the design evidence). **Residual work is implementation, tracked *inside* 0008 as Stage-1 opens — not open doctrine:** the neutral transport script · the executor-topology decision (pull-by-resident vs temple-dispatch — parked because tokens aren't yet measurable and the write-grant concern isn't live until the pass writes) · the L10 silence-default (opt-in vs opt-out when a mandatory change's objection window lapses). *(First live auto-apply candidate, post-O4-gavel: piql Vara `medium` → canon-settled `high` now passes the L2 canon-integrity precondition.)*

## Resolved opens (direct-inform thread)
- **Part 4 — cross-machine carry** — RESOLVED 2026-07-02 by **decision 0010** (gaveled + committed). File-native · receiver-owns · no-central-bus, via per-writer append-only jsonl partitions + git transport; MariaDB kept as project-local read-projection; failover distributor demoted to notifier. Empirically validated (disposable spike: append-only merges clean, in-place mutation conflicts) + advisor-high whole-scope review. **Residual = implementation, not doctrine:** part 2 doorbell real-fire (PAID — probe verified 2026-07-02) · part 3 twin read-side rollout (draft; must bind to the `temple-mail-inbox` interface, not the layout — amendment R-b) · the addresses-only address-book beacon (L8).

## Still open
- **O1 — live-slice sizing (Model C)** — operator-only; the one untuned string. **Field-work gavel 2026-06-17 (majkee): deferred — adopt-what-is, leave the string untuned; `.mcp.json` stays a placeholder / `mcp_profile` minimal. Not meaningful drift; the lesson the pain teaches is to *log the call*, which this line does.** Settle later with the detection/cost experiment, not more design. **Trigger to un-defer:** the first time a basement hits a real "needed live runtime truth a file couldn't give" wall — that event is the data; then run the cheap one-evening detection/cost test and size the slice to it.
- **O2 — spectral-engine driving challenge/process maps** — parked in the composition seed; out-of-process (engine as local MCP). Do not fold without a fresh frame.
- **O4 — interrupted atlas-ui effort/normalization session (flagged 2026-06-25)** — the session that set effort directly on agent cards + normalized agent groups was cut mid-write (accidental ctrl+z, fatal in-terminal). ~~Vara's effort~~ **SETTLED 2026-06-25 (majkee gavel):** `high` confirmed as intent — research-derived (xhigh falls to high on Sonnet 4.6); monitor performance, reconfigure if needed. **RECONFIGURED 2026-07-16 (majkee gavel):** Vara → Haiku·medium flat-bus per octopus design (0012) — earlier `high` intent superseded; she routes, the head (Flight) / brain (Houston) reasons. Remaining open: **erase unnecessary project agents** + broader normalization pass. Vara pilot runs (home machine, fine from mid-phase, e.g. Laravel); known wall = manual observation-relay; 2 projects nearing task-line phase need her soon. **To solve in a future session loop after clearing/reincarnation**, as a deliberate normalization pass — NOT a blind propagation. Couple with the O3 cleanup loop.
