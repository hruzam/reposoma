# Decision Record — 0013: the precedence stone (`temple/precedence.md`)

`status: GAVELED-BY-OPERATOR 2026-09-03 (@majkee, live, in-session) — codified same day by @atlas-ui`
`under an explicit recorded Force-4 override: "gaveled — please force over 4. you as houston, granting`
`privilege — at least as provisional layer. future will judge me enough." Speed-over-perfectness ruled.`
`date: 2026-09-03 · host: office · author-seat: @atlas-ui (head, gate roster-reform-01-triad)`
`append-only once committed — supersede, never edit.`

## What locked

1. **The apex has a file:** `temple/precedence.md` — the order above all WINS clauses. Location
   argued (evidence: `temple/evidence/0013/base-stone-argument.2026-09-03.md`):
   `raw.canon/` rejected on circularity (the apex cannot live inside a domain it ranks);
   `doctrine.md` rejected on genus (self-declared "advice, not law"). Temple root adopted.
2. **The subordination line** — uniform, appended to each terminal WINS clause:
   *"Terminal within this domain. Across domains, and under a live operator gavel,
   `temple/precedence.md` orders the collision (0013)."*
   Applied at codification to the two on-disk-confirmed clauses (Sella GUIDE · codex-relay
   contract). The two unverified clauses (nablarva flag · the fourth) are amended only after
   on-disk verification — carried by `CS.precedence-tail.2026-09-03.md`.
3. **The Sella ↔ session-shape boundary (sweep finding 2):** Sella governs primitive-BUILDING
   (agents/skills/hooks/relays, stochastic-compiler laws); the session-shape canon
   (`raw.guides/{runbook,status,PAD,bus,gloss}`) governs session SHAPE. Different objects; neither
   wins over the other; a primitive is BUILT under Sella and RUN inside a canon-shaped session.
   Sella's precedence clause narrows explicitly to sibling *builder* guides. Cross-domain
   residue → the stone.

## Why (one paragraph)

Every supreme clause in the corpus was domain-terminal and none acknowledged anything above
itself; the actual global law existed only in the operator's spoken words (sweep, "the deepest
line"). A precedence order that lives in speech is unciteable and dies with the session. The
stone gives every WINS clause one superior citation and gives collisions a resolution by object
and rank instead of by authorship or recency.

## Provenance note (the override itself)

Force 4 (0002) forbids agent-authored-direct canon. The operator overrode it live for this
codification, provisionally granting the Houston-codification hand to atlas-ui, and directed the
override be recorded rather than hidden. Rank 1 of the stone is exactly this power — the record
is the audit trail. Any future re-litigation starts from this paragraph, not from a cleaner myth.

*Detail home: `temple/evidence/0013/base-stone-argument.2026-09-03.md` — point-never-copy. (The
argument was authored as a transient inbox mail, consumed per mail-protocol before commit; the
ADR-GUARD hook correctly refused the inbox citation, and the author reconstructed the argument
verbatim into the evidence home the same day.)*
