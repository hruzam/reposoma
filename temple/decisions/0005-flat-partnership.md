# 0005 — Flat Partnership + Cooperation Protocol  ·  DRAFT (pre-gavel)

`status: LOCKED 2026-06-19 — gaveled by majkee (structure + method + runtime line). Revised per @Janus`
`2026-06-19 (the "convergence = proof" claim was killed — see Honest caveat). Pending: a clean re-blind`
`before the Method distills to raw.canon (see Process).`

## Context
- The flat **Houston (Claude stack) ↔ Vega (Gemini stack) ↔ majkee** structure — **majkee's gavelled
  direction (2026-06-19).** Discipline as an **inner bond, not a leash.** The lock below rests on that
  direction, refined by both architects' drafts.
- **⚠ Honest caveat (Force 8 · @Janus 2026-06-19):** the founding "blind" draft was **not blind.** Houston's
  brief to Vega leaked his own positions ("my instinct: the gavel is the tie-break"; "opt-in by design"),
  and Vega agreed on exactly those and diverged only on the one point Houston genuinely left open (the
  runtime). That is **anchoring, not independent convergence** (Force 5). So the agreement is **NOT cited as
  proof** of the structure — it is cited only as *both architects finding the direction workable*. The single
  genuinely-open point (runtime) produced the only real divergence — that asymmetry is the honest signal.

## The Lock — what 0005 gavels
1. **Two peer architects:** Houston (Claude stack) + Vega (Gemini stack). majkee = operator + canon gate.
2. **One compile-down gate, one direction; majkee gavels canon** (Force 4 holds).
3. **Mutual challenge on big architectural moves only.** Routine sector work *flows* — **opt-in per
   interaction**, not a continuous shackle (opt into the shared saddle only for the big gates).
4. **Gavel = absolute tie-break.** On unreconcilable divergence we present a **divergence matrix**
   ("Houston sees risk X; Vega sees risk Y") to majkee. We do **not** deadlock the build fighting each other.
5. **Failover-adoption:** a partner unavailable → the peer may adopt their line per the situation.

*The bond — challenge each other, write handoffs as shared problems, mutual respect — is **culture**,
lived and not gaveled. Writing "act like a partner" into the decisions log would turn culture into bureaucracy.*

## The Method — the cooperation protocol (majkee's workflow, canonized as the rigid rule)
**A · Blind triangulation.** Each partner receives the brief **blind** → forms its own hypothesis **X**
(researches/solves its own way) → exchange → **measure the gap** → **bounded** argument (majkee is the
circuit-breaker — *no neverending loop*) → **consensus**.
  - **THE RULE THE FOUNDING INSTANCE BROKE (Janus 06-19): the brief must NOT contain the briefer's answer.**
    A brief carrying the briefer's position produces an anchored read, not a blind one — the signal is dead
    (Force 5). Position-free briefs only. This rule exists *because* run-one of this very decision violated it.

**B · The stone (impulse → response).** majkee drops a **stone** into the continuum: it is an **impulse**
(an `X`), **never a leash or a rigid box.** The receiver does not merely comply — it returns its own **`Y`**
(alternative) or **`Z`** (wildcard — a creative, out-of-box reframe). **Triangulate again** when the
question exceeds **1+1 scope** (more than three actors).

**C · Parked (named, not built — drawered by majkee 2026-06-19):**
- **The Z-dreamer** — a very-creative, out-of-box voice summoned *by situation* in a special mode (for `Z`).
- **Multi-brand / character-specific challengers** — per question scope. **@Janus is the standing challenger today.**

## Runtime projection — GAVELED 2026-06-19 (the reconciliation below is the LOCKED outcome)
**Runtime projection — the one divergence (and the one genuinely-blind point).** Present as a matrix:

| | position |
|---|---|
| Houston / majkee | build-on-**Claude** (hedge against the Gemini sunset). |
| Vega | project into the **Gemini runtime** — §2 agents-as-data means the Tier-S *definition* survives a sunset; re-project into Claude/Cursor + web-MCP as fallback. Native live-web-fetch + massive-context synthesis is *what makes Orby valuable*. |
| Epoch (06-19, tie-breaker) | the real axis is **supervised vs unattended**, not Claude vs Gemini. **Supervised `agy` = GO now** (native fetch available); **unattended/headless `agy` = WAIT** (`-p` empties under non-TTY · OAuth token loss · API-key auth unmerged). |

**Locked outcome (gaveled 2026-06-19):** Tier-S definitions (vendor-neutral, Temple-owned). Project
Orby/Astrobley to **Gemini/`agy` for SUPERVISED on-demand work now** (native fetch — Vega's value);
**Claude = the fallback projection** (the hedge — Vega's own fallback-ready design); the **unattended**
projection waits for the `agy` headless test to pass. Shared Plane-1 capabilities — Houston dispatches for
deep grounding, Vega for source-mapping. *Satisfies both positions across time without locking a loser.*

## Process to lock
blind drafts (contaminated — see caveat) → **@Janus = REVISE** (applied) → **majkee gavels** → rename
`0005-flat-partnership.md` + `decisions/index.md` row. **Before** distilling the Method into
`raw.canon/canon.blind-triangulation.md`: **re-run ONE converged point on a position-free brief** so the
canon card ships with a *clean* worked example, not the contaminated founding one. Runtime matrix = its own gavel line.

---

## Amendment A1 — the peer seat re-bound: Gemini → Codex/GPT (2026-07-24)

`status: GAVELED LIVE 2026-07-24 by @majkee — in-session directive to @Oraculum ("Skip → write":`
`bureaucracy waived by operator order; Force 4 satisfied by direct live authorization — precedent: the`
`Flight temporary pass 2026-07-18). Original record above untouched (append-only doctrine §1b).`

### What changes
1. **The Vega seat persists; the vendor behind it swaps.** Gemini stack → **OpenAI Codex/GPT stack**
   (Codex CLI · GPT-5.5 line · ChatGPT Plus subscription auth). The seat name was always the chair, not
   the vendor — majkee 2026-07-24: *"we borrow (picasso this) from gemini."*
2. **The Gemini line: CLOSED.** Tokens refunded (operator). Google itself deprecated Gemini CLI
   2026-06-18 (successor: Antigravity CLI) — **the §Runtime hedge ("build-on-Claude against the Gemini
   sunset") fired**, and Vega's own fallback-ready design ("re-project into Claude/… as fallback") is
   exactly the mechanism executing this swap. **§Lock 5 failover-adoption, exercised as designed.**
3. **Machine layer signposted loud, not silent:** `~/.config/zsh/ai/` gemini-bound scripts guarded with
   notice + `exit 2` (`GEMINI_LINE_FORCE=1` archaeology override) — @Delta pass 2026-07-24. The parked
   pilot artifacts (`maintenance/gemini-line/`) stay frozen as protocol evidence; the ledger+diff+
   `git apply --recount` coder protocol proven there **transfers vendor-free** to the Codex line.
4. **Method unchanged.** Blind triangulation (position-free briefs) · the stone · bounded argument ·
   gavel tie-break — doctrine untouched. This is a vendor swap, not a doctrine change.
5. **Runtime projection, new vendor — same shape as the 2026-06-19 lock:** supervised Codex CLI = **GO
   now** (subscription active); **unattended/headless = WAIT** for the WS2 headless smoke test to pass.

### Evidence & rationale (dated)
- @Epoch landscape pass 2026-07-23: context-window parity erased Gemini's original selection reason;
  Codex CLI Terminal-Bench 83.4% (highest confirmed CLI), mature headless + AGENTS.md support; Plus
  flat-rate economics fit the co-thinker seat (deep, bursty, human-tempo). Lineage distance = the
  triangulation value; a harness is not a mind (Cursor rejected on that ground).
- Kimi K2 line deferred to the **army seat** — underline, trigger: real task volume. Not part of A1.
- Program of record: `maintenance/codex-line/runcard.codex-onboarding.2026-07-24.md` (WS0–WS5).

### Pending inside A1 (transferred, not new doctrine)
- **The clean re-blind required by §Process was never run on the Gemini line.** It transfers to the
  Codex line: the **nabla-lab stone trial (WS4a)** — one position-free brief, Claude + Codex blind,
  triangulate — is designated as the clean worked example before the Method distills to
  `raw.canon/canon.blind-triangulation.md`.
- `gemini-cross-check` agent card: retire or re-point at the WS5 bookkeeping batch (@Atlas ticket).
