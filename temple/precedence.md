# Precedence — the one order above the WINS clauses

`tier: temple (sovereign root) · genus: HARD LAW — the one non-advice text (cf. doctrine.md:9 "advice, not law")`
`gaveled: majkee, live, 2026-09-03 → decision 0013. Codified by atlas-ui under an explicit, recorded`
`Force-4 override ("force over 4 · you as houston, provisional layer") — the operator's gavel IS rank 1 below.`
`amend: supersede-not-edit · majkee gavel only · grows on a real collision event, never by design.`

Every domain document is terminal WITHIN its own domain. Sella wins on primitive-building doctrine;
a project's flag is sole authority inside its project; the relay contract wins on relay plumbing;
the session-shape canon wins on session shape. All TRUE, all LOCAL. This file exists for the one
case they cannot cover: two domains touching the same object and disagreeing.

## The order (highest first)

1. **The operator's live gavel.** A majkee ruling outranks every written clause (0002 · Force 4 —
   and its overrides are his to grant, on the record, as this stone's own birth proves).
2. **This stone + `temple/decisions/` locks** — the sovereign, cross-domain layer.
3. **A project's own `flag.md` / `PROJECT.yaml`** — sole authority within that project's bounds.
4. **A domain guide's WINS clause** (Sella · relay contract · session-shape canon · a project bed's
   local law) — terminal within its domain; across domains subordinate to 1–3.
5. **A live/dated external source** beats any static clause on a volatile fact (Sella L8). Not a
   rank inversion — the freshness carve-out every WINS clause already grants.

## The rule of the stone

- No document below rank 2 declares itself supreme over another DOMAIN. Within-domain "wins"
  language stays and gains one subordination line pointing here.
- Collisions resolve by OBJECT, then by this order — never by authorship, recency, or eloquence.
- Short on purpose. An entry that arrives without a real collision receipt is bloat (raw.canon
  maturity law applied to the apex itself).

## Receipts

- contradiction-sweep 2026-09-03 findings 1–2: four terminal WINS clauses, zero apex; "global law
  above all" existed only in the operator's spoken words.
- Subordinated at codification: `raw.guides/sella/GUIDE.md:9,20` · `~/ia-sync/zsh/guides/
  codex-relay.contract.md:9`. Pending on-disk verification before amendment: the nablarva flag
  "sole authority" clause · the fourth clause (likely the session-shape guide-wins line) — see
  `_cold-start/card/CS.precedence-tail.2026-09-03.md`.
- Boundary companion: Sella = primitive-building · session-shape canon = session shape (decision
  0013 §boundary; both documents carry the paragraph).
