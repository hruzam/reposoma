---
session : `2026-09-25`
---

# 1. muticula — concurrency guard (bed: `~/ia-sync/.dev/session/runbook-upgrade-02-app/`, head: Cartan)

- [x] B0 Claude lane — ACCEPT (deny works; every guard failure = silent fail-open) · `_bus/03.*`
- [x] B0 Codex lane — run done, verdict STOP (global trust persisted, no interactive hashes) · `_bus/04.*`
- [ ] B0 Codex clean re-run — needed before the muticula gate can pass
- [ ] B1 occupation core — gavel Trajectory's packet · `raw/trajectory.b1-packet.2026-09-25.md`
- [ ] B1 decide language: python-stdlib experiment (not a docket-2 vote) or wait for docket 2
- [ ] B1 decide queue: strict FIFO + operator-cancel of dead tickets (proposed)
- [ ] decide worktree-first as operating default (independent tasks → own worktree)
- [ ] B2 runtime adapters + identity binding (Claude + Codex hooks)
- [ ] B3 lifecycle + auto-release on commit (only after B2)
- [ ] B4 dashboard / session client — coordinate `zsh/session/` with ovitmugen owner first
- [ ] B5 worktree / integration client
- [ ] muticula gate — my GO/STOP on the witnessed v0 package
- [ ] B0 evidence retention — 35 ignored `*.jsonl`: manifest in git, raw local or non-git store

# 2. publish-gate — commit buffer + deploy (bed: `~/ia-sync/.dev/session/publish-gate-00-design/`, head: Trajectory)

- [x] gaveled: deploy only committed content
- [x] session opened (RUNBOOK · STATUS · pulse router), Cartan named challenger
- [ ] wake Trajectory on prompt-0 → design file + POINT to Cartan
- [ ] Cartan's challenge (muticula boundary: Git transaction, deploy target)
- [ ] my GO/STOP on the design → then `publish-gate-01-*` builds it (deploy.sh `--ref`, stamp, flock, CLI, dashboard)

# 3. loose ends from these sessions

- [x] precmd presence session closed (superseded) · keeper in muticula `raw/`
- [x] stale `~/.remote` SessionStart hook removed · ia-sync `af35200`
- [x] 5 presence records restored (`git restore`) after the bare `rb-unmark` accident
- [ ] `own.tsv` board defect (bare unmark wipes all) — issue card + fix by `session/runbook.py` owner
- [ ] SYNC_DISCIPLINE "Presence" — correct the stated cause (shared `own.tsv`, not seat)
- [ ] commit: `pulse.md` router line + `publish-gate-00-design/` bed (uncommitted)
- [ ] GUIDE for muticula / publish-gate — only after GO; until then RUNBOOK + STATUS are the memory

---
session : `ff-sync.atlas.cSharp-germline` (Claude · atlas-ui on Fable · atlas-as-houston provisional)
---

# 4. germline — canonical shared home (bed: `~/ia-sync/.dev/session/germline-00-home/`, head: atlas-ui)

- [x] gaveled: name `.germline` · source in reposoma (`~/reposoma/.germline/`) · `~/.germline` = symlink, never a deploy leg · project scope `<root>/.germline/` same layout
- [x] home exists + committed: reposoma `e8c5b30` — `README.md` (rule) · `CHATBOT.md` (chatbot saddle: repo + path + read rules, no index) · `skills/skill.buffering-cycle.md` (pilot port)
- [x] both `chatbot-port` twins → `.germline/skills`, `twin-commit:` in both · ia-sync `4db6745` · deployed 15:26 (live == table) · Cartan RETURN folded (`_staging/codex/germline-home.return.2026-09-25.md`)
- [x] ia-sync `AGENTS.md` — `~/.germline` section · old `.shared/` + `raw.shared-skills/` removed
- [ ] **prompt-2 (gate):** fresh Claude `/chatbot-port buffering-cycle` → no · `--check` equal; fresh Codex `$chatbot-port …` same — paste both receipts to atlas-ui → gate closes, bed prunable. STATUS has the exact lines.
- [ ] home box: `git -C ~/reposoma pull && ln -s ~/reposoma/.germline ~/.germline` on its next session (journal line owed in `journal.host-cleanup.md`)
- [ ] tell the ovitmugen seat its committed zsh brick (`0b95adf`) went live with today's deploy

# 5. invariance — Atlas pilot + what waits behind it (bed: `~/ia-sync/.dev/session/invariance-autonomy/`, master brief in `raw/`)

- [x] subject gaveled: **Atlas**, bounded undeployed measurement (Cartan REVISE adopted; `*bus`/medusa later) · Cartan counter-signed
- [x] entry line: Claude direct (`--agent`) · Codex delegated under Cartan + `--profile` probe · **`-p` never**
- [ ] `res/architecture.md` — target shape + three cases (global-only · global+addendum · local-only) + performance numbers the pilot must hit (agreed, not written yet — after prompt-2)
- [ ] RUNBOOK for the pilot — **Flight MANNED or Houston authors it, not Atlas** (subject ≠ author); Cartan `octopus` sibling after
- [ ] Houston = first user of the project-addendum mechanism (`<project>/.germline/agents/houston/addendum.md` → shadows via `<project>/.claude/agents/`); nablarva `convergence` = the local-only sample (untracked draft, two live seats there — don't touch)
- [ ] ruled in the pilot session: Houston↔Cartan collision · `scope:` word (`agent-portability`?) · Codex `buffering` ≈ Claude `buffering-cycle` near-miss (first twin-slug ruling) · `atlas-auto` as wrapper-variant
- [ ] parked, own session: `.germline` discovery index / connector bus · ptyra migration into `.germline/agents/`
- [ ] FYI only: `~/.remote` retirement (drop-brief isolates its path as one constant) · per-scope deploy → publish-gate line, not here

**Continue here:** run prompt-2 → paste receipts → atlas-ui closes `germline-00-home` → then say "architecture chapter" for §5.
