---
scope: migration, harness
---


# friends note

I don't really care about migration, I try to have nothing to migrate. I keep all my know-how in plain Markdown + code + git (verified project gotchas, one fact = one .md), not in vendor primitives (skills/hooks/MCP/agents) - I see them as renewable glue. Migration then = I point a new tool at the same Markdown, no "burning ships". My team of agents is also application data (JSON prompts + .md), not agents in the CLI.