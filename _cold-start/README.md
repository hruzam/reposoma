# _cold-start — the session-glue vault (`card/` · `routines/` · `archive/`)

Law + schema + lifecycle: `~/reposoma/raw.guides/cold-start-card/GUIDE.md` (or `/guide cold-start-card`).
Folder = state. Write via your brand's `cold-start-card` skill. Frontmatter is the source of truth — point, never copy.
