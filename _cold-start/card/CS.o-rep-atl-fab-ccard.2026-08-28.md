---
kind: cold-start-card
date: 2026-08-28
brand: claude
seat: atlas-ui
project: reposoma
projects: [reposoma, ia-sync]
root: ~/reposoma
commit: 2ae333b (core)
task: cold-start vault system — built, blessed, both brands smoked; session closure card
resume: claude --agent atlas-ui
model: sonnet
dedicated: atlas-ui (shape changes) · flight + krakens (tooling changes)
recommend: thread is CLOSED — read the journal before touching the shape; the palette is the field test, let it bruise before any redesign
pointers:
  - ~/reposoma/raw.guides/cold-start-card/GUIDE.md
  - ~/reposoma/raw.guides/cold-start-card/res/journal.md
  - ~/reposoma/pulse.atlas.md
---

Closure card, written BY the system it closes (session `o-rep.atl-fab.ccard`,
2026-08-27→28, atlas-ui + majkee).

## State at close

Everything shipped and 1:1 on both hosts: vault (`card/`, `routines/`, `archive/` — 24
archived incl. legacy corpus + both smoke/citizen cards) · one law (GUIDE, canon-
reconciled with status/runbook: one-authority boundary, STATUS name surrendered) · both
brand skills rehomed · cs-palette + temple-cs-manage blessed 14/14 and deployed · Codex
smoke PASS (@Cartan, cross-brand D3 proven). No live session bed exists; this card
legally carries its own (tiny) tail.

## Tail (seeds, not tasks — build only when they hurt)

- Codex capture script emits literal /home paths → teach it `~`-anchoring (journal,
  friction item 2).
- D3 runbook brick: grows richer only when runbook practice demands it — explorer stays
  explorer.
- `routines/` is empty — the first real RT card will field-test that third state.

## Session advice

Two lessons this session proved: (1) re-read the guides mid-stream when the operator
says "innovations landed" — the status/runbook canon reversed a naming decision one turn
old, and catching it same-day cost minutes instead of a migration; (2) the existence
check works in both directions — it stopped a redundant closing GUIDE just as it started
the whole build by finding the two diverging skills.
