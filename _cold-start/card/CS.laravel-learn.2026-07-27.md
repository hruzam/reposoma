# CS · 2026-07-27 · Laravel learning — scope-by-scope from freya

commit : 4ae4e429 (develop)   # freya, not committed to — read-only teaching session
task   : Sequential Laravel curriculum, each scope anchored in a real freya file
context: Teaching session, NOT coding. OpenCart→Laravel bridge is the hook (you know OC).
         Two regimes locked: 📱 phone = Deep Dive verbal · 💻 computer = Deep Dive + hands-on.
         I delivered the orientation — the "pulse" (request lifecycle), registry→Service
         Container, Model→Eloquent, Controller (single-action), "wire"→Livewire (no OC equiv).
         Then dropped 4 confirmation questions. ⏳ AWAITING your answers to those 4 checks.
         14-rung ladder is on the table (bootstrap→config→routing→…→AI/MCP); rungs 1–7 spine.
resume : claude --agent flight   → answer the 4 checks, then Phase 1 goes practical:
         boot Freya locally, run what/where/why → confirm → hands-on task, per scope.

next   : when back at computer → (1) answer 4 checks (2) `composer run dev` / octane up
         (3) start rung 1 (bootstrap/app.php) hands-on.
