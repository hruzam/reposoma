---
kind: cold-start-card
date: 2026-09-17
brand: claude
seat: oraculum
project: nabla-lab
projects: [nabla-lab, ia-sync, reposoma]
root: ~/nabla-lab
task: head (cSharp) of incarnations-00-mechanism — row 0R witnessed, R1 satisfied; trial waits on rows 1–2 (arc-close weather) · therapy-transfer task DISSOLVED to a commit+pull check (see body)
resume: cd ~/nabla-lab && claude --resume
model: fable
dedicated: oraculum
recommend: resume THIS session (fc-lab.oraculum-cSharp.re-incarnation-scars, the 2026-09-11→17 sitting) while it lives — it carries the '42' lineage, the loop, both boot recordings and the witness folds. Reincarnate above ~/ia-sync only if dead/compacted; then pointers below, STATUS owns position, say you run on recall.
runbook: ~/ia-sync/.dev/session/incarnations-00-mechanism/RUNBOOK.md
supersedes: ~/reposoma/_cold-start/card/CS.incarnations-00-head.2026-09-11.md (drain to archive)
pointers:
  - ~/ia-sync/.dev/session/incarnations-00-mechanism/STATUS.md
  - ~/ia-sync/.dev/session/incarnations-00-mechanism/res/trial.md
  - ~/ia-sync/.dev/session/incarnations-00-mechanism/raw/gavels.loop-oraculum.2026-09-11.md
  - ~/ia-sync/.dev/session/incarnations-00-mechanism/_bus/07.cartan.verdict.md
  - ~/reposoma/raw.incarnations/README.md
  - ~/reposoma/raw.therapy/trajectory/therapy.md
---

# CS — incarnations-00-mechanism head · re-entry (rev 09-17b)

`transfer pointer only; STATUS owns position and next.`

## Position in one breath

Trial locks A0+L1–L7+R1–R4 stamped · row 0 = qualified observation (primed) · branch A gaveled ·
row 0R = clean blind boot, witnessed by cartan (`_bus/07`), **R1 satisfied**. Findings n=2, one clean:
load mechanism fully proven · authority instrument absent (direct canon write both runs) · inheritance
*effect* unproven (zero named-scar decisions on the clean boot) · hypothesis: row-0 citations were
priming-induced. Open: majkee's GUIDE-diff disposition · **G-45 shadow candidate awaiting majkee stamp**
(trajectory arc 1, surfaced in-chat 09-17; bed highest = G-44; Atlas's staged stream also plans G-45+ —
read-before-append governs, second lander renumbers) · rows 1–2 at atlas-ui/flight arc-closes · amend
pile for VERDICT (mechanical path classifier leads; effect-rule naming-AND-action; control run).

## Therapy bed — state corrected 2026-09-17 (was: "gitignored, needs tailscale transfer")

**Verified (Delta, read-only):** ALL per-seat therapy files are git-TRACKED and committed
(`git -C ~/reposoma ls-files raw.therapy/` lists every seat; `.gitignore` carries no therapy line;
oraculum/therapy.md last landed in `05c036e` "2026-09-03, office→home"). **Git is already the
transport.** The believed-gitignore was stale knowledge; the hold ("gitignore unchanged until solved")
is honored by construction — nothing needs changing.

Remaining actions, ordinary not architectural:
1. OFFICE: commit `raw.therapy/trajectory/therapy.md` (untracked; placed 09-17, majkee-directed,
   trajectory's first seated therapy file — Arc 1 + G-45 shadow footer tags). majkee sequences.
2. HOME: `git -C ~/reposoma pull`, then check the records majkee missed are present
   (`ls ~/reposoma/raw.therapy/*/therapy.md` · grep the arc he remembers). If STILL missing after a
   clean pull → only then is there a real gap; reopen as its own sitting with evidence.
No tailscale session needed unless step 2 fails.

## Re-entry order

STATUS → `res/trial.md` (ledger + both boot records) → loop ledger → `_bus/07` (witness law now
governing effect-scoring: naming AND action).
