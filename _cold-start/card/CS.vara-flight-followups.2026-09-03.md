---
kind: cold-start-card
date: 2026-09-03
brand: claude
seat: atlas-ui (fold hat)
project: ia-sync
projects: [ia-sync, reposoma, applications-in-common]
root: ~/ia-sync
commit: 8c9f230 (main)
task: Possible FUTURE Flight/Vara shaping thought through in the roster-fold session — producer-approved, none built. Pick up only AFTER the roster-reform-01-triad gate closes.
resume: claude --agent atlas-ui  →  read THIS card  →  CHECK THE GATE FIRST (STATUS below) — if still open, STATUS owns position, do not act
model: opus
dedicated: atlas-ui (design) / atlas-auto (once a spec is fixed)
recommend: These are deferred, each gated on an upstream landing (runbook GUIDE · cross-vendor chapter · #43083 · Houston 0012-reconciliation). Build none speculatively — each waits for its unblock + a majkee gavel.
runbook: ~/ia-sync/.dev/session/roster-reform-01-triad/RUNBOOK.md
pointers:
  - ~/ia-sync/.dev/session/roster-reform-01-triad/STATUS.md   # LIVE gate — owns position until it closes
  - ~/ia-sync/.dev/session/roster-reform-01-triad/raw/field.runbook-check-rules.2026-09-03.md  # the 8 rules, substrate
  - ~/ia-sync/claude/agents/flight.md
  - ~/ia-sync/claude/agents/vara.md
  - ~/ia-sync/claude/skills/track-run/SKILL.md
  - ~/reposoma/temple/roster.md   # :63,72 stale Vara "router" language → sweep
  - ~/reposoma/temple/system-map.md   # :28,113,160 same
---

## One-authority — read first

The gate `roster-reform-01-triad` may still be OPEN. If its `STATUS.md` exists with an
unclosed checkpoint, **STATUS owns the position** — this card carries NO competing
next-action. Everything below is POST-CLOSURE future work. Do not act on it while the gate
is live.

## Producer-approved future updates (none built — each gated)

**Vara**
1. **runbook-check skill** — build from the 8 preserved rules (pointer above) when the
   runbook GUIDE lands + defines "caveat". Decide tier at build: haiku + "lever to Sonnet"
   (roster.md:63 already says so) vs sonnet-flat.
2. **Tunnel-driver pointer line** — one line, when the head's `res/cross-vendor-seat.md`
   lands: Bash-capable → can drive an operator-opened tunnel; instrument tuple + cycle-RETURN
   duty live in `/runbook`'s cross-vendor chapter. Point, never restate (Oraculum's law).
3. **Checkpoint-surface convention** — Vara Edit-appends into an existing session surface;
   a project without one is a gap she must flag. Possible: standardize the surface.

**Flight**
4. **flight.md:123 `/program-pulse`** — stays as gaveled until Houston's 0012 reconciliation
   (buffer → per-session TRACK, PAD-class receipt) lands + majkee gavels; then the Phase-2
   pointer may update.
5. **Length watch** — flight.md is 159 lines (bound <160). Any further add must trim or move
   to a pointer, not grow.
6. **'dice' seat** — a temple-only meditative expert, if ever needed, is a SEPARATE Opus seat,
   never an overload of Flight (CS.roster-trim-fold steer).

**Both (cross)**
7. **Advisor collapse (roster-reform-02)** — updates who Flight/Vara escalate to. 2-seat
   (opus+fable) deployable now; 1-seat needs #43083.
8. **vector reshape (#43083)** — updates implementer dispatch in both seats.

**Sweep debt (temple canon, majkee gavel)**
9. `roster.md:63,72` + `system-map.md:28,113,160` still call Vara a "flat-bus router /
   routes tasks" — contradicts the POLYP walker. Fix when the sweep runs.

**Post-deploy precision (cSharp outside-view, cycle 04 — cosmetic, no new cycle)**
10. `flight.md` phase-7 handoff — change "append the dev-journal entry" → "append the
    **project** dev-journal entry" (2 words). Composes today because it IS the project
    dev-journal, but the wording could let a fresh seat read it as license to create a
    session journal — the third-log the runbook GUIDE law ("No journal; history lives in
    git") kills. Deploy is clean without it; apply as a post-deploy follow-up.

## First step

Check `STATUS.md`. Gate open → park this, help the sweep. Gate closed → pick the item whose
upstream has landed, confirm with majkee, then build (atlas-ui) or spec-to-atlas-auto.

## Lesson

The fold succeeded because style stayed in the vendor-neutral guides and the seats POINT at
it — every future item here holds that line: point at the guide/chapter, never bake a copy
into the agent spec.
