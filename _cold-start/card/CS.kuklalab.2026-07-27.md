CS · 2026-07-27 · Medusa — KUKLAlab scene format FROZEN

commit : 3105d9d (core)  ✔ experiment content committed this session
                          ✘ .dev/ deliberately NOT committed (your call — see TODO 2)
task   : SCENE-FORMAT.md frozen · L1 round-trip probe done · playground briefed, not built

context: nablarva-logo → renamed KUKLAlab (larva→kukla→imago). Vega/Gemini's
         nablarva-studio handoff buffered + cut hard: drawing canvas, tablet/evdev,
         JSON-RPC daemon all DEAD (the scene FILE is the socket).
         Base stone = a FORMAT, not a framework. L1 proved the scene separable:
         creature is data-shaped, wordmarks call NO primitive, naive recorder emitted
         ~30x bloat (837KB → ~186 genuine ops). Catalog shrunk to wm_route+scale2 by
         the promotion rule eating its own founding members; name3 stays engine-held.
         SPEC IS FROZEN — do not add to it, every pass that added broke something.
         Track B gaveled: the playground IS the goal, gift-artifact insurance REJECTED.
         13 agents, 3 Assay FAILs — all three found real defects.

YOUR TODO (PC + her Mac):
  1. pad.1-terminal-capability — HIGHEST VALUE. Run on YOUR machine and HERS.
     Steps 0-5 install nothing (pure printf). We designed a tool for a Mac nobody
     has touched. Decides truecolor-vs-256, default backend, font-fallback risk.
  2. .gitignore — modified but UNCOMMITTED. It cuts a hole un-ignoring
     .dev/session/logo-larva.braille-animation.python/ (rest of .dev stays ignored).
     Your "don't commit .dev" vs the hole's purpose (Gemini across machines?) — decide.
  3. GEMINI.md — untracked. Same class as AGENTS.md/CLAUDE.md which ARE ignored.
     Probably belongs on that ignore list, not in git.
  4. experiments/larva_logo-v0.py + -v0-clean.py appeared at floor root mid-session.
     Not mine, unknown. Loose outside any experiment folder.
  5. my unfinished housekeeping: .dev/research/README.md has no terminal-art row;
     R1/R2/R3 filenames are off-convention (my error in the briefs).

next   : L2b engine — VALIDATOR FIRST (its first test: does §7's own example validate?
         two FAILs came from hand-checking a machine-checkable contract). Watch loop
         COLLAPSED IN, no separate line: select() over mtime AND stdin, raw termios for
         the J2 sequencer, 300-500ms debounce. COLORTERM fallback carried unconditionally.

resume : claude --agent medusa   # → .dev/session/logo-larva.braille-animation.python/session.md
