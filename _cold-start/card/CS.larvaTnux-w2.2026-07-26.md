CS · 2026-07-26 · Atlas — larvaTnux W2 (factory zsh layer) + /gavel-loop

commit : 54fe775 (core)  ⚠ STALE — this session's work UNCOMMITTED
                          (new: .dev/zsh/ layer · ~/.claude/skills/gavel-loop/ ·
                           config.zsh shadow note · pulse.atlas.md ×2 · gavel-loop mail archived)
task   : W2 volatile factory zsh layer DELIVERED + /gavel-loop skill DELIVERED

context: gavel-loop = sequential gavel-clearing skill (global, built from oraculum brief).
         W2 = .dev/zsh/ layer (5 files + config.zsh shadow note): _base router ·
         _keyboard · factory.zsh <hokus pokus> · boot.zsh = REAL brick-boot chain
         (bus H7 → wires check), auto-run on `source _base.zsh`. Launcher = `fact`
         (gaveled `!` dropped — zsh history-expansion clash). ALL BLESSED.

YOUR TODO (PC + Polyp):
  1. commit this session's work (hash above is pre-session)
  2. devenv sync.sh — .dev/zsh/ not persisted cross-machine yet
  3. first live boot smoke: `source .dev/zsh/_base.zsh` → expect bus pane brought up,
     `fact-help` for the keyboard. Polyp test seat.

resume : claude --agent atlas-ui   # then release W3 (/boot · /zsh-wire) or W4 (coldstart BRICK)
