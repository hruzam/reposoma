---
kind: cold-start-card
date: 2026-09-02
brand: claude
seat: atlas-ui
project: reposoma
projects: [reposoma, applications-in-common, ia-sync]
root: ~/reposoma
commit: 1a8f81f (core)
task: Batch C — fold @Medusa→@Flight, @Polyp→@Vara (WIDE, not temple-locked); re-derive the fold table against the ARCHIVED substrate with wider explanation; promote /track-run + point @Vara at the shared guides. Then Batch D (advisor-ladder plan · freya ledger recon · normalization arc).
resume: claude --agent atlas-ui  →  read THIS card  →  read the archived substrate  →  get the mode-branch gavel  →  build
model: opus
dedicated: atlas-ui (or @houston — both Opus; NOT fable/oraculum, per majkee: "same training data" for the fold)
recommend: GATE FIRST — get @majkee's gavel on the Flight/Vara mode-branch shape before editing any live seat. Fold WIDE (all projects use pulse); temple-only role = a SEPARATE 'dice' monk seat if ever, never an overload of Flight.
pointers:
  - ~/reposoma/pulse.atlas.md   # → OPEN LEDGER "Octopus family" line + [2026-09-02 Octopus-family] entry (full arc)
  - ~/reposoma/raw.substrate/archive/   # FROZEN source of truth (grep-excluded): 2026-09-02.{medusa,polyp,sqcr,sea-pen,podocyst}.*.codex-crossing.*
  - ~/ia-sync/claude/agents/flight.md   # fold TARGET (medusa content)
  - ~/ia-sync/claude/agents/vara.md     # fold TARGET (polyp content)
  - ~/reposoma/_mail/atlas/inbox/houston.ladder-principle.2026-09-02.md   # D1 advisor-ladder
  - ~/reposoma/_mail/atlas/inbox/houston.task-holding-and-unfinished-business.2026-09-02.md   # D2 freya ledger Gen-1→Gen-2
  - ~/reposoma/raw.guides/runbook/GUIDE.md   # the shared vendor-neutral style
  - ~/reposoma/raw.guides/status/GUIDE.md
  - ~/reposoma/raw.guides/PAD/GUIDE.md
  - ~/ia-sync/codex/skills/medusa/SKILL.md   # the Codex-native successors (what NOT to duplicate)
  - ~/ia-sync/codex/skills/polyp/SKILL.md
---

## Saddle first — this card is ONE thread, not the whole board

A fresh Atlas sits in saddle normally: `pulse.atlas.md` OPEN LEDGER carries ALL live
threads (Sella vault · relay quote-safety · metaterminal · THIS octopus-family fold). You
reach this card FROM that ledger. Do the saddle, then this card — don't treat it as the
only open work.

## Where this comes from

Session `fc.repos-atlas-opus.polyp-medusa` (2026-09-02) trimmed the Claude roster: the
octopus family (medusa/polyp/octopus) crossed to the Codex line (`ia-sync/codex/skills/`,
commit `fa69ee0`); `/octo` was killed; the Claude bed copies were removed and frozen into
`raw.substrate/archive/`. **The muscle did not die — it folds home:** Medusa's proven
content → @Flight, Polyp's → @Vara. THIS card is that fold + the normalization it opens.

## GATE — do not skip

**Get @majkee's gavel on the mode-branch shape before touching live `flight.md` / `vara.md`.**
Flight is today a *temple* seat; Medusa was a *generic-project-session* seat. The steer
(gaveled in shape, not yet in the specific edit): make the folded seats **WIDE** — a Flight
that gains a project-session mode/branch WITHOUT losing its temple role, not a Flight locked
to either. If the temple ever needs a dedicated expert, that is a **separate 'dice' seat**
(Opus, meditative, temple-only — Bohr–Einstein "don't tell God what to do with his dice"),
never an overload of Flight. Opus author only (Atlas or Houston), never Fable.

## STEP 1 — re-derive the fold table against the ARCHIVED substrate (wider than the first pass)

The first pass (in the pulse entry) was terse and from live files that are now gone. Redo it
from the frozen truth. Read all five:
`raw.substrate/archive/2026-09-02.{medusa,polyp,sqcr,sea-pen,podocyst}.*.codex-crossing.*`
plus the targets `ia-sync/claude/agents/{flight,vara}.md`. Produce ONE table per pair with a
FULL explanation column — not just the verdict:

`capability · exact quote/behavior in the archived seat · does Flight/Vara already have it? ·
fold verdict (promote-as-is / merge-into-target / drop-as-redundant) · WHY (the reasoning,
a sentence or two — this is the "wider explanation" majkee asked for)`

Known seed verdicts to re-confirm or overturn against the frozen text:
- **medusa→Flight:** cold-read+crash-check (merge → `/project-read`) · Assay-gate-per-kraken
  (KEEP as Flight orchestration pattern — Flight lacks it) · observed-not-imagined test (merge,
  doctrine line) · phase-map spine (KEEP) · program.pulse kraken lines (as-is, already global) ·
  MANNED/UNMANNED (drop — Flight already has it).
- **polyp→Vara:** MANNED/UNMANNED switch (KEEP — Vara lacks it) · append-only executor
  discipline (KEEP — Vara has Write; decide if the discipline transfers) · `/sqcr` consumption
  (note: sqcr followed polyp to Codex — decide if any Claude seat still walks PADs) · `/sea-pen`
  reporting (project-coupled, likely stays project) · `/track-run` (promote — see STEP 3).

## STEP 2 — fold the surviving capabilities into the live seats (AFTER gavel)

Edit `ia-sync/claude/agents/flight.md` and `vara.md` on the surgical table (deploy-inert).
Keep them WIDE. Do NOT copy the Codex successors' text — point both seats at the shared guides
(runbook/status/PAD) as the single style source (the anti-divergence keystone). First-person
voice in all body content.

## STEP 3 — promote /track-run + wire Vara to the guides

`/track-run` still lives in the applications-in-common bed (`.claude/skills/track-run/`) and
@Vara consumes it → promote it to the global table `~/ia-sync/claude/skills/track-run/SKILL.md`.
Add a pointer line in `vara.md` to RUNBOOK/STATUS/PAD + `/track-run`. (`/session-handoff` stays
project-scoped — do not promote.)

## STEP 4 — Batch D (separate decisions, may already be mailed)

- **D1 advisor-ladder collapse plan** (Houston `ladder-principle` mail): canonize the four
  `advisor-*` as ONE role pending GitHub #43083; write the collapse-to-one-`advisor`-with-
  `model`/`effort` plan; keep the *+1-rung, never-same, never-+2* law; leave role-distinct
  seats (janus/agol/color/oraculum) alone. BLOCKED on #43083 shipping — plan only.
- **D2 freya `ledger.md` Gen-1→Gen-2 reconciliation** (Houston `task-holding` mail): the root
  fix (drain fat Gen-1 ledger → STATUS + cold-start vault). A distinct decision for @majkee;
  boundary-owner was Medusa → now @Flight-project-mode / Codex-Medusa. taskexpert reader HELD
  spec-only (2–3-sitting bar unmet). Feed to Houston/majkee, don't build.
- **D3 shape-normalization arc:** adopt the vendor-neutral guide bundle (RUNBOOK/STATUS/PAD/
  pulse) across `ia-sync` · `nablarva` (`unikuklatrix/nablarva`) · `applications-in-common`
  (+ reposoma). An ARC with per-project gates — plan with @Houston (Opus), execute per-project
  via @Eagle reads. NOT one build.

> ⚠ Check whether D1/D2 were already sent as separate Houston mails this-or-prior session
> (grep `_mail/houston/inbox/atlas.*2026-09-0*`) before re-authoring — don't duplicate.

## Guardrails

- Atlas holds **read-only Bash** — author via Edit/Write on the surgical table; never run
  rm/mv/cp/write-side-git/deploy. Deploy + commit are @majkee's gate.
- Nothing is live until `deploy.sh` + commit. State it at handoff.
- The archived substrate is VERBATIM/frozen — read it, never edit it.

**Drain:** whoever completes the fold + normalization moves this card `card/` → `archive/`.
