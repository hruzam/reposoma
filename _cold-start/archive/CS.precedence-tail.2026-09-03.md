---
kind: cold-start-card
date: 2026-09-03
brand: claude
seat: atlas-ui (head hat)
project: reposoma
projects: [reposoma, ia-sync]
root: ~/reposoma
commit: 241cce2 (core, pre-commit of this batch — re-check HEAD on wake)
task: Precedence-stone TAIL — (1) verify the 2 unverified WINS clauses on disk and apply the 0013 subordination line · (2) collect Cartan cycle-04 /runbook countersign → close gate roster-reform-01-triad · (3) run the closing sweep.
resume: claude --agent atlas-ui  →  saddle (pulse.atlas OPEN LEDGER)  →  read THIS card  →  if gate STATUS still open, STATUS owns position
model: opus
dedicated: atlas-ui (fable acceptable — this is verification + surgical lines, not fresh doctrine)
recommend: VERIFY-BEFORE-AMEND is the whole card. Do not add the subordination line to a clause you have not read on disk. Do not reopen the stone's text — supersede-only, majkee gavel.
runbook: ~/ia-sync/.dev/session/roster-reform-01-triad/RUNBOOK.md
pointers:
  - ~/reposoma/temple/precedence.md            # the stone (LIVE, gaveled 0013)
  - ~/reposoma/temple/decisions/0013-precedence-stone.md
  - ~/reposoma/temple/evidence/0013/base-stone-argument.2026-09-03.md  # full argument (durable evidence home)
  - ~/ia-sync/.dev/session/roster-reform-01-triad/STATUS.md   # gate position (if still open, it wins over this card)
  - ~/ia-sync/.dev/session/roster-reform-01-triad/_bus/04.head.point.md  # Cartan countersign ask
---

## One-authority — read first

If gate `roster-reform-01-triad` STATUS is still open, **STATUS owns position**; this card is
its tail-work list, not a competing next-action.

## The tail (in order)

1. **Verify WINS clause 3 — nablarva flag "sole authority".** File LOCATED (background find
   completed post-close, 2026-09-03): `~/unikuklatrix/nablarva/.dev/session/flag.md`. Read the
   clause there. If real → append the uniform subordination line (0013 §2). If absent →
   correct the sweep's count in a dated note to Oraculum.
2. **Verify WINS clause 4 — "the fourth".** Sweep said four; confirmed only three kinds on disk.
   Check: the session-shape guide-wins line (runbook GUIDE / `/runbook` skill "the guide wins") ·
   `raw.guides/codex-builder-user/` (Sella:61 references builder/user guides). Amend what is real;
   report the true count.
3. **Cartan cycle 04** — `/runbook` vs `octopus` role-parity RETURN → head VERDICT 04 → stamp
   majkee's gavels in STATUS checkpoint → **gate CLOSES**.
4. **Closing sweep** (from STATUS holds): roster.md:63,72 + system-map.md:28,113,160 Vara
   "router" language (draft for gavel — pair with the Vara-tier note) · 0012↔STATUS reconciliation
   ask → Houston (mail) · RUNBOOK >150-line findings → runbook-upgrade · `_runbook` zsh opener
   (parked, majkee's word) · then prune per RUNBOOK closure block.
5. **Post-closure:** majkee commits reposoma + ia-sync, `deploy.sh`. The fold's deferrals live in
   `CS.vara-flight-followups.2026-09-03.md` — separate card, gated, not yours.
