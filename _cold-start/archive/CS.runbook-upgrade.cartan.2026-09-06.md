---
kind: cold-start-card
date: 2026-09-06
brand: codex
seat: cartan-csharp
project: reposoma
root: ~/ia-sync
commit: 0f52f56 (main)
task: Resume the original runbook-upgrade Cartan-cSharp head after moving this same computer
resume: codex resume -C ~/ia-sync 01a06cfd-b74e-7430-b041-992a9d9cea3c
model: astra
dedicated: cartan-csharp
recommend: Resume the exact original thread; current STATUS owns recovery and authority, never a same-name parallel controller
runbook: ~/ia-sync/.dev/session/runbook-upgrade/RUNBOOK.md
pointers:
  - ~/ia-sync/AGENTS.md
  - ~/ia-sync/SYNC_DISCIPLINE.md
  - ~/ia-sync/.dev/session/runbook-upgrade/STATUS.md
  - ~/ia-sync/.dev/session/runbook-upgrade/_bus/24.cartan-csharp.point.md
  - ~/ia-sync/.dev/session/runbook-upgrade/_bus/24.astrobley.return.md
  - ~/reposoma/raw.guides/runbook/res/csharp-head-protocol.md
  - ~/reposoma/raw.guides/runbook/res/fanout-turns.md
session_id: 01a06cfd-b74e-7430-b041-992a9d9cea3c
rollout: ~/.codex/sessions/2026/09/04/rollout-2026-09-04T17-16-02-01a06cfd-b74e-7430-b041-992a9d9cea3c.jsonl
observed_model: gpt-6-astra
observed_effort: xhigh
cli_version: 0.153.4
host: office · hruzam-120922
active_goal: none
worktree_observed: dirty tracked and untracked session/client work; index empty
authority_may_write: Head-owned session controls under the RUNBOOK; delegated bodies remain with their named writers
authority_gated: This pointer grants no commit, push, deploy, prune, worker wake, tunnel or role-substitution authority
---

## Read first

Read the applicable AGENTS chain, then the live RUNBOOK and STATUS. This card is a
transfer pointer, not another doing-state. STATUS owns the recovery probe, pending
verification and one next action; follow its cited POINT and actual later receipts.

The cSharp remains the navigator and sole STATUS owner. Runtime Cartan identity does
not make every Codex window this bed's head; a same-name subagent is not its independent
Astrobley session. POINT 24 records the cycle-23 provenance correction and displaced
STATUS evidence. Do not resurrect that artifact's claimed acceptance through its filename.

## Observed frame

The bonded capture and CODEX_THREAD_ID agree on the exact session ID above; its rollout
exists locally. Installed `codex resume --help` supports a UUID and `-C`; the resume
command was syntax-checked, not executed. Avoid `--last` with parallel sessions.

The registry umbrella remains reposoma; the actual work/resume root is ~/ia-sync.
Moving this same computer changes neither its local saved files nor the intended thread.
Session history is host-local; the card and dirty bed are not cross-host Git delivery.
The archived 2026-09-05 card is historical and has older cycle pointers.

## First tool shot

```sh
hostname -s
git -C ~/ia-sync status --short
git -C ~/ia-sync rev-parse HEAD
git -C ~/ia-sync diff --cached --name-only
sed -n '1,160p' ~/ia-sync/.dev/session/runbook-upgrade/STATUS.md
```

## prompt-0

###### prompt

```text
Resume the original Cartan-cSharp head for ~/ia-sync/.dev/session/runbook-upgrade.
Read RUNBOOK.md, current STATUS.md and POINT 24's provenance correction; run STATUS's
read-only recovery probe before acting. Preserve cycle-23 receipts, but do not treat
its purported VERDICT as this head's independent verification. Reconcile any RETURN 24
and later receipts without assuming they passed. Keep all current ownership, approval
and original-gate boundaries. Do not spawn a replacement seat or replay completed work.
```

## Gates and lifecycle

Saved locally, uncommitted. No Git, deployment, transport or worker-wake operation is
needed merely to shut down and move this same computer. Another computer would require
separate authorized transport of project state; the Codex session does not travel in Git.
After successful consumption, the reader archives this card under the vault lifecycle.
