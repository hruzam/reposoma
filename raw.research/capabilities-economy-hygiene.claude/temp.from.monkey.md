Your own primary proposal adn I took it (now of course is extended from final observations and agreements).

┌───────────────────┬─────────────────┬───────────────────────────┬───────────────────────────────────┐
│       Kind        │      Scope      │         Examples          │      Holds project context?       │
├───────────────────┼─────────────────┼───────────────────────────┼───────────────────────────────────┤
│ Capability agents │ Global          │ Delta, Trajectory, Vector │ ❌ No — orchestrator teaches them │
├───────────────────┼─────────────────┼───────────────────────────┼───────────────────────────────────┤
│ Context agents    │ Session/project │ Houston, Vara             │ ✅ Yes — own the plan + state     │
├───────────────────┼─────────────────┼───────────────────────────┼───────────────────────────────────┤
│ Reference agents  │ Global          │ Color, Janus, Epoch, Agol │ ❌ No — advisory on demand        │
└───────────────────┴─────────────────┴───────────────────────────┴───────────────────────────────────┘

Houston (Opus)              ← architect, planner, gates, challenge-before-lock
  └─ Vara (Sonnet, xhigh)  ← task runner: holds current task list, drives execution session
       ├─ Trajectory (Sonnet) ← senior implementation, pushes back, spawns Delta
       │    └─ Delta (Haiku) ← surgical execution
       ├─ Vector (Sonnet) ← midd.advanced coder implementation, where Delta does not cover context or logic
       ├─ Delta (Haiku) ← surgical execution - Q1: UNCONFIRMED, but I think if not breaking rulles, where is clean, there can be spawned directly.
       │    
       └─ [advisory: Color, Janus, Agol, Epoch on demand]