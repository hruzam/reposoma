# session-hygiene — substrate
_Fetched: 2026-09-02 | Mode: snapshot | Sources: 6/6 fetched this pass_

## claude-code
**Sub-agents docs** (code.claude.com/docs/en/sub-agents) — Fresh vs fork inheritance model unchanged (fresh = own system prompt/env only, no parent history; fork = full parent conversation + tools + model). Explore/Plan still SKIP CLAUDE.md and git status; General-purpose still loads both. Explore model still "inherits main conversation model, capped at Opus" (confirmed verbatim — card's v2.1.198 footnote holds). NEW this pass: page now documents a **model-selection resolution order** for subagents — (1) per-invocation `model` param, (2) subagent's own `model` field, (3) `CLAUDE_CODE_SUBAGENT_MODEL` env var, (4) main conversation's model — and notes **v2.1.251+ changed precedence** so per-invocation/frontmatter now outrank the env var. General-purpose subagent specifically documented as using `CLAUDE_CODE_SUBAGENT_MODEL` if set. Plan mode confirmed: exploration output only returns to main session; main session stays read-only during exploration. Additional version breadcrumbs on page: v2.1.199 (subagent API errors return partial output), v2.1.210+ (output scanning for instruction-shaped patterns).

**Context window** (code.claude.com/docs/en/context-window) — Page is now an interactive token-budget SIMULATION (not fetched 2026-08-01; first full fetch this pass). Documents startup-load order and per-item token costs: system prompt ~4200 tok, auto-memory (MEMORY.md, first 200 lines/25KB) ~680 tok, env info ~280 tok, MCP tool listing (deferred, `ENABLE_TOOL_SEARCH`) ~120 tok, skill descriptions ~450 tok (NOT re-injected after `/compact` — only actually-invoked skills survive), `~/.claude/CLAUDE.md` ~320 tok, project CLAUDE.md ~1800 tok (tip: keep under 200 lines). File reads shown as dominant context cost (e.g. one file read = 2400 tok) — reinforces card's "file reads dominate; use a subagent for research-heavy tasks" framing. Confirms path-scoped rules (`.claude/rules/*.md`) load on matching file read and survive `/compact`.

**Remote Control** (code.claude.com/docs/en/remote-control) — **STATUS CHANGE candidate: page no longer labels Remote Control "research preview"** (card's 2026-08-01 section still calls it "preview"). Current page states plainly: "available on all plans" — Pro/Max/Team/Enterprise; API keys not supported; Team/Enterprise off by default until an Owner enables the toggle in admin settings. Card's core recovery mechanics hold: `tmux`/`screen` survival quote is VERBATIM unchanged ("To keep a session running on a remote machine after you disconnect from SSH, start it inside tmux or screen"); `claude -c`/`claude --resume` reconnect via the conversation's reconnection record; `claude remote-control -c` still gated at v2.1.200+ (confirmed). **Refinement to card's "host offline >~10 min → timeout" claim:** this only applies to **server mode** (`claude remote-control` gives up after ~10 min, process exits). **Interactive session mode** (`claude --remote-control`) instead retries indefinitely and reconnects on its own when network returns — card's blanket "host offline >~10 min → RC times out" statement is only correct for server mode, not interactive mode. NEW feature not in card: **Trusted Devices** (beta, Team/Enterprise, off by default) — ties Remote Control viewing/steering to an enrolled device + sign-in age ≤18h with biometric step-up; unrelated to terminal CLI usage. NEW: `--teleport`/`--cloud` flags referenced elsewhere in card's fork/branch section are NOT documented on this specific page (out of scope here — that's Claude Code on the web / cloud sessions, a separate doc).

## gemini-cli
**Subagents docs** (geminicli.com/docs/core/subagents/) — Confirms standard/stable framing (page does not label subagents experimental; only the unrelated browser-agent feature carries a 🔬 experimental icon). Matches card's 2026-08-01 PREVIEW→standard delta — no further change this pass. Config confirmed at both `.gemini/agents/*.md` (project) and `~/.gemini/agents/*.md` (user, new detail — card only cited the project path). Recursion guard confirmed verbatim: "subagents cannot call other subagents... even with the `*` tool wildcard."

**Bug #8609** (github.com/google-gemini/gemini-cli/issues/8609) — State reconfirmed CLOSED (no change since 2026-08-01). No fix release/PR/date is stated on the issue. Original report dated 2025-09-17 (older than card implied — card's "was open 2026-07-02" framing referred to card's own recheck window, not the bug's filing date). Treat resolution as unverified-by-changelog; re-test long unattended sessions still advised.

## cursor
**Changelog** (cursor.com/changelog) — Recent entries are Cloud-Agent-focused, a different surface than the local-IDE mode-switch mechanics the card documents (no direct contradiction found for the <50% effective-window / 80%-vs-12% / Cmd+. mode-reset claims — those remain practitioner-measured, unconfirmed by any changelog entry this pass or last). NEW items, none overlapping the card's existing claims: **2026-08-19** — Cloud Agents subagents now run in "isolated copy of the project with clean context in its own cloud environment" (a cloud-only isolation pattern, distinct from the local IDE); Custom Modes (persistent skill-modes via Alt+Enter); `/goal` for long-lived objectives; mid-execution steering without interruption. **2026-08-13** — pre-built cloud dev environments, faster agent startup. **2026-08-27** — Cloud Agents no longer require external repo connection. "Cursor Router" (noted in the 2026-08-01 substrate) does not appear in this changelog window — not confirmed still active, not confirmed retired either (absence from a 6-week changelog slice is not evidence of removal).

## Run metadata
- Fetched this pass (6/6): `cc-subagents`, `cc-context-window`, `cc-remote-control`, `gemini-subagents`, `gemini-bug-8609`, `cursor-changelog`
- No items in window: none (snapshot mode — all sources are living docs, always "in window")
- Deltas vs 2026-08-01 substrate:
  - Remote Control: page drops "research preview" framing seen in card — now presented as GA "available on all plans" (Team/Enterprise gated behind Owner toggle)
  - Remote Control: card's blanket "~10 min offline → timeout" claim is server-mode-only; interactive-session mode retries indefinitely
  - Remote Control: new Trusted Devices beta feature (Team/Enterprise, device-enrollment + 18h biometric re-auth) — not in card
  - CC subagents: new documented model-resolution order + `CLAUDE_CODE_SUBAGENT_MODEL` env var + v2.1.251 precedence change — not in card
  - Cursor: new Cloud Agents subagent-isolation pattern (2026-08-19) — a distinct cloud surface from the card's local-IDE mode-switch findings, not a contradiction but an uncovered gap
  - Gemini subagents / bug #8609: no change since 2026-08-01, both reconfirmed stable
- Not re-confirmed: "Cursor Router" (mentioned 2026-08-01, absent from this changelog window — status unknown, not confirmed removed)
- One-off study metrics (CheesecakeLabs / MindStudio / InfoQ / MorphLLM) not in roster; not re-verified this pass (unchanged per scope README)
