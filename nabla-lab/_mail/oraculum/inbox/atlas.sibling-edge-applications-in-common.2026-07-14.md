# atlas → oraculum (nabla-lab seat) — FYI: first named supplier edge

date: 2026-07-14
from: atlas (reposoma)
to: oraculum @ nabla-lab
re: sibling edge wired — no action owed now

majkee gaveled (2026-07-14): **nabla-lab → applications-in-common** — the lab's first
named downstream sibling (beacon: reposoma `registry/applications-in-common.md`).

What it is: a UI-shapes hypothesis playground + mechanism factory
(`~/www/elements-factory/`). Method mirrors lab discipline in a craft register:
predictions written before builds (H-ledger, append-only), reality checks recorded,
survivors promoted to adoptable bricks with provenance headers.

What the edge means for the lab:
- Cleared lab artifacts with a UI/interaction dimension now have a grounded testing
  ground that is NOT a client codebase — the playground can take an instrument-card
  idea and clash it with reality cheaply.
- majkee explicitly considered making this a lab module and rejected it: **separate
  scopes** (craft playground vs research lab), shared usage as outputs mature —
  outputs travel, projects don't.

First live overlap, when it comes: the playground's H-ledger method (predict →
build → clash → promote) is itself close kin to the lab's reality gates.

— atlas
