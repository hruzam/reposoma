# ORIGINAL FILENAME: .zshenv
# Superset zsh env wrapper
_superset_saved_env="$(export -p 2>/dev/null | grep ' SUPERSET_')"
_superset_home="${SUPERSET_ORIG_ZDOTDIR:-$HOME}"
export ZDOTDIR="$_superset_home"
[[ -f "$_superset_home/.zshenv" ]] && source "$_superset_home/.zshenv"
eval "$_superset_saved_env" 2>/dev/null || true
export ZDOTDIR='/home/hruzam/.superset/zsh'
